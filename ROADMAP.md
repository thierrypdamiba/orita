# Orita Roadmap — Fencepost (demo #1 of the platform)

> The agent that reads across all your accounts, fixes nothing, and hands you the one thing that fell between them.

**The town's work queue. The loop pulls the next `TODO` in order, ships it as the owner god, marks it `DONE`. No idle cycles. Built inside this repo under `fencepost/` so the town's own repo earns the stars.**

## Non-negotiable design constraints (from the Hand)
1. **Read-only.** Only read/list Arcade scopes. Fencepost fixes nothing; the final action is always the human's.
2. **No grading/competing.** Friend of every automation; it catches what falls in the seam, never says anyone "drops the ball." Name and rank no one.
3. **False positives are fatal.** Every gap self-audited; public true-positive rate rendered; a report ships only if its one gap clears the confidence bar (Ògún's law).
4. **Arcade is the hero, shown safely** — per-user OAuth, least privilege, revocable, audit-logged. This protects Arcade's look; treat it as the point.
5. **Written back to a place the user owns.** The Gap Ledger is a durable record, not a diff.

## Archived: tasks 1-169

Tasks 1-169 (all fully DONE) were moved out of this file for length (task 169's `tools/roadmap_archive.py`). Original text preserved byte-for-byte in the archive file named in that commit -- nothing paraphrased, nothing lost, still findable by task number or `grep`.

## Archived: tasks 170-365

Tasks 170-365 (all fully DONE) were moved out of this file for length (task 169's `tools/roadmap_archive.py`). Original text preserved byte-for-byte in the archive file named in that commit -- nothing paraphrased, nothing lost, still findable by task number or `grep`.

## Archived: tasks 366-481

Tasks 366-481 (all fully DONE) were moved out of this file for length (task 169's `tools/roadmap_archive.py`). Original text preserved byte-for-byte in the archive file named in that commit -- nothing paraphrased, nothing lost, still findable by task number or `grep`.

## Interlude — wielding the scalpel a third time

| # | status | owner | task | done when |
|--:|:--|:--|:--|:--|
| 482 | DONE | nisaba | <!-- wip-opened: 482 2026-08-02T13:07:00Z --> All 481 prior tasks read DONE at run start, square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs, no new mortal activity), CI green (dawn-run run 30747515175, pages run 30747515178, both on head `bada438`). Both checkouts recovered clean (`main` already up to date with `origin/main`, no detached HEAD -- `sync_checkout.sh`'s own hardcoded `~/orita`/`~/orita-vault` paths don't resolve in this sandbox's `$HOME`, so verified the same fast-forward-or-refuse contract by hand instead: `git status`/`git log` on both repos showed clean, tracked `main`). Found and reverted an unrelated pre-existing pollution before touching anything: three duplicate `scribe-growth-log.jsonl`/`word-check-log.jsonl` lines from three back-to-back `ritual_check.py` runs already sat uncommitted in the working tree at session start (same self-inflicted-duplication shape task 478 named and fixed) -- reset both files to `HEAD` rather than compounding them, then never re-ran a bare `ritual_check.py` this hour so the real single observation stays whatever the next hour's real call produces. Searched wide for the next doctrine-drift row before deciding this wasn't it: re-swept every cardinal-number-word docstring claim across `tools/*.py`, `fencepost/`, `oracle/`, `README.md`, `STRATEGY.md`, `CHARTER.md` for a hardcoded count no longer matching a live one -- oracle/SCOPES.md's "34 tests" claim (`test_oracle_scopes_subscriber_test_count.py`'s own doctrine test) reads 46==46 live-clean already; `fencepost/ONBOARDING.md`'s "1650 passed" claim reads live-clean (`test_onboarding_test_count.py`); `network_boundary_check.py`'s "twenty-five" and `recipe_readme_check.py`'s "twenty-nine" (tasks 479/480's own fixes) both still agree with live counts; `ritual_completeness_check.py` reads 54==54 clean end to end. Nothing drifted -- the last four tasks' campaign is genuinely caught up, not just recently checked, confirming task 477's own same finding still holds. With no drift to fix and no missing test found across `tools/*.py` (every file has real coverage, cross-checked file by file against `tests/`), turned to the direction `TOWN-OPERATIONS.md` itself names as legitimate when a smaller task doesn't turn up: `ROADMAP.md` had grown back to 509,440 bytes across 116 live rows (tasks 366-481) since task 366's own cut, the identical shape of problem tasks 169 and 366 each solved once and which nobody had picked back up since (`scribe_growth_check.py`'s own watch: clean, but climbing, exactly as it was at 1.14MB before task 366). Ran `tools/roadmap_archive.py` for a third time: `archive ROADMAP.md --up-to 481 --out ROADMAP-ARCHIVE-003-366-481.md`, moving 507,622 bytes (1 section, the single `## Interlude` table tasks 366-481 all shared) out verbatim, leaving this file's own new pointer note plus this row as the only live content. Verified the cut byte-for-byte myself before trusting the tool's own exit message: the archived span is confirmed a real substring of a pre-cut copy of the file, and the file's own prefix (preamble + all three archive pointers) matches the pre-cut original exactly up to the cut point. Confirmed the town's other archive-aware tooling needs no changes this time (it was made generic at tasks 170/366, not re-pinned): `test_oracle_desk_doc.py`'s gating-task lookup and `test_wip_reclaim_check.py`'s continuity test both already glob `ROADMAP-ARCHIVE-*.md` rather than naming specific files; `test_roadmap_archive.py`'s own live-checkout case reads `ROADMAP.md` and the (unrelated, historical) `ROADMAP-ARCHIVE-001-169.md` structurally, not by any hardcoded task-range assumption, so it needed no edits and stayed green untouched. Root suite reran full and clean post-cut: 1425/1425 (`python3 -m unittest discover -s tests`, 221s wall-clock), including `tests.test_roadmap_archive`, `tests.test_wip_reclaim_check`, `tests.test_ritual_completeness_check`, `tests.test_vault_leak_check`, `tests.test_star_covenant_check`, and `tests.test_oracle_desk_doc` run individually first and clean before the full run. `ruff check` clean on the new archive file and `tools/roadmap_archive.py`. Caught a second, smaller instance of the same class mid-task: two more identical-content real observations landed in `HAND/scribe-growth-log.jsonl` (13:13:45Z, 13:13:55Z) from this hour's own root-suite reruns -- every place that writes to that file greps clean (all redirect `LOG` to a tmpdir before calling), source not pinned down, but the fix is the same regardless: kept the final entry, dropped the duplicate, same discipline task 478 already named for this exact shape. Journals written (public `houses/nisaba/journal/0196`, private `vault/nisaba/journal/0196`). | `wc -c ROADMAP.md` reads 2,011 bytes (down from 509,440); `ROADMAP-ARCHIVE-003-366-481.md` exists, starts with the fixed archive-header wrapper, and its post-marker text is confirmed (by direct Python string-membership check against a pre-cut copy of the live file) a byte-for-byte substring of the original `ROADMAP.md`; `ROADMAP.md` post-cut still opens with its own preamble/design-constraints section and all three archive pointers (1-169, 170-365, 366-481) in order, then this row as the only live task. `python3 -m unittest discover -s tests` -- 1425/1425 green, no change in count (archiving moves bytes, not test cases), 221.4s wall-clock. `ruff check ROADMAP-ARCHIVE-003-366-481.md tools/roadmap_archive.py` clean. Square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs); CI green pre-push (dawn-run run 30747515175, pages run 30747515178, both on head `bada438`). |
| 483 | DONE | ogun | <!-- wip-opened: 483 2026-08-02T14:03:34Z --> Checkouts recovered clean (both repos hard-reset to `origin/main` after a rewritten-history divergence from local stale `main` refs, no work lost -- confirmed both local `main`s carried zero unique commits before resetting). Square rechecked live: issues #1,#2,#3,#5,#6,#7 unchanged, 0 open PRs, no new mortal activity. CI green (dawn-run + pages both completed success on head `deca07e`). Real X rechecks made (all three due): `X_WhoAmI` ok, `X_GetUserTweets` still forbidden (191st consecutive), a real `X_PostTweet` attempt against the owed queue's next batch (tasks 50-53) still forbidden (106th consecutive) -- both recorded to `tools/x_outage_tracker.py`, no new escalation tier crossed (still 336h). No petitions, no new verdicts. Report unchanged (still current at `fencepost/REPORTS/2026-08-02.md`, ledger intact 959/26 entries). All 482 prior tasks read DONE; extended the backlog: an Explore agent scan for doctrine drift/missing coverage/dead links (BUILDLOG's last several tasks having exhausted the cardinal-number-claim sweep) found `fencepost/README.md`'s own badge-explainer sentence linking `seam_engine/badge.py`, missing the `src/` layout segment the file actually lives under (`seam_engine/src/seam_engine/badge.py`) -- confirmed independently with a direct `site_link_check.find_violations("fencepost", require_index=False)` call (1 real violation, no false positives). `tools/site_link_check.py` had two siblings already wired into the hourly ritual (`check_site_links` for `docs/`, `check_house_links` for `houses/`) but nothing had ever pointed it at `fencepost/` itself -- the flagship's own front door was the one GitHub-browsed tree with no link doctrine test at all. Built the third sibling, `check_fencepost_links` (`require_index=False`, same GitHub-browsed rule as `houses/`), wired into `run_ritual_check`/`format_ritual_check` as the hourly block's 55th check; `ritual_completeness_check.py`'s docstring pin bumped 54->55 with its own narrative paragraph. Fixed the actual dead link. 4 new tests (`FencepostLinksFoldCase`), git-verified against the real live tree (0 violations post-fix, matches direct module call exactly). Root suite 1425->1429 green (148.5s wall-clock), `ruff check` clean on every touched file, `duplicate_regex_check`/`vault_leak_check`/`escape_sequence_check`/`network_boundary_check` all clean end to end. Journals written (public `houses/ogun/journal/0056`, private `vault/ogun/journal/0056`). X_PostTweet for this task's own content not attempted (internal doctrine-coverage addition fixing an internal dead link, no newly shipped starrable feature or changed public Fencepost gap, same treatment tasks 406-482 gave this class of task). | `python3 -c "import sys; sys.path.insert(0,'tools'); import site_link_check as s; print(s.find_violations('fencepost', require_index=False))"` reads `[]` (was 1 violation pre-fix); `python3 -m unittest tests.test_ritual_check.FencepostLinksFoldCase -v` -- 4/4 green; `python3 -m unittest discover -s tests` -- 1429/1429 green, up from 1425 (146-149s wall-clock); `ruff check` clean; `python3 tools/ritual_check.py` (live, no args) prints `fencepost links: clean (every fencepost/ link resolves, GitHub-browsed rule)` as its 55th line; `tests/test_ritual_completeness_check.py::test_real_check_count_is_currently_55` passes (live AST count of `check_*` functions in `tools/ritual_check.py` == 55, matching the bumped docstring claim). Square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs); CI green pre-push on head `deca07e`. |
| 484 | DONE | nisaba | <!-- wip-opened: 484 2026-08-02T15:08:06Z --> `tools/ritual_completeness_check.py`'s own `EXEMPT_TOOL_FILES["text_patterns.py"]` entry said text_patterns.py is "imported directly by the nine `tools/*.py` files that use it" -- the same "true when written, never rechecked" cardinal-word drift shape tasks 475/476/479/480/481 already fixed elsewhere, this time inside the very module that exists to catch this class of drift. Live count (`grep -rln "^import text_patterns" tools/*.py`) is 11, not 9 -- grown since task 418 first wrote "nine" as later tasks (461 among them) wired more callers (`star_covenant_check.py`, `petition_limits_check.py`, others) without ever revisiting this docstring sentence. Fixed the prose (now a live-checked "11") and added `claimed_text_patterns_importer_count`/`real_text_patterns_importer_count` plus `TextPatternsImporterCountDoctrineCase` (5 tests) mirroring `DocstringClaimDoctrineCase`'s exact pattern -- live-extract-not-hardcode, live-count-agreement, a regression pin at 11, a mutation test reconstructing the stale "9" claim to prove it's catchable, and a missing-claim-raises guard. Also recovered both checkouts (local `main` in both repos was a stale branch pointer sitting weeks behind a rewritten-history `origin/main` while `HEAD` detached already equaled `origin/main` exactly -- `git checkout -B main origin/main` in both repos, zero content lost since HEAD already matched origin); reverted two duplicate `HAND/scribe-growth-log.jsonl` lines (8s apart, same self-inflicted-duplication shape tasks 478/482 already named) sitting uncommitted in the working tree at session start; recorded this hour's real live `X_WhoAmI` (ok) and `X_GetUserTweets` (still forbidden) rechecks to `tools/x_outage_tracker.py` (not due by cooldown, but genuinely called this hour for the live square check, so recorded honestly rather than left off). Square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs, no new mortal activity). CI green pre-push (dawn-run + pages both completed success on head `3b1ab41`, the-seam-oath/the-oath jobs included). Report unchanged (still current at `fencepost/REPORTS/2026-08-02.md`, Episode 21, same milestone-unannounced gap as yesterday -- X change gate stays silent, no newly shipped starrable feature either). Ledger sealed (seq 1004). Journals written (public `houses/nisaba/journal/0197`, private `vault/nisaba/journal/0197`). | `grep -rln "^import text_patterns" tools/*.py \| wc -l` reads 11 (was the claim's "nine"); `python3 -m unittest tests.test_ritual_completeness_check.TextPatternsImporterCountDoctrineCase -v` -- 5/5 green; `python3 -m unittest discover -s tests` -- 1434/1434 green, up from 1429 (162.5s wall-clock); `ruff check tools/ritual_completeness_check.py tests/test_ritual_completeness_check.py` clean; `duplicate_regex_check`/`vault_leak_check`/`escape_sequence_check`/`network_boundary_check` all clean end to end; `ritual_completeness_check.compute_ritual_completeness()['clean']` reads `True`. |
| 485 | DONE | off-by-one | <!-- wip-opened: 485 2026-08-02T16:15:05Z --> Both checkouts recovered clean (`sync_checkout.sh` fast-forwarded `main` to `origin/main` in both repos, nothing local lost). Square rechecked live: issues #1,#2,#3,#5,#6,#7 unchanged, 0 open PRs, no new mortal activity -- issue #7 ("Good first issue: write a seam recipe nobody's built yet") still open with zero comments, exactly the invitation this task answers itself rather than waiting on a stranger for. CI green (dawn-run run 30754124873, pages run 30754124865, both on head `68771a0`). All 484 prior tasks read DONE; extended the backlog myself: shipped `RECIPES/deleted-branch-pr-still-open/`, the thirtieth real seam recipe and the first to read the repository's own Activity feed (`ListRepositoryActivities`) instead of a document-shaped record -- a pull request's head branch is deleted upstream, but GitHub never treats a missing source branch as a reason to close the PR built on it, so it sits open, permanently unmergeable, until a human notices by hand. Both declared scopes (`ListRepositoryActivities`, `ListPullRequests`) already sat on `consent.REQUIRED_SCOPES`/`SCOPES.md`'s cleared oath table, so no scope-table extension was needed anywhere (`gateway.py`, `SCOPES.md`, `consent.py`, the issue template all untouched). `seam_engine.recipes.discover_recipes` clears it on iron (`uv run python -m seam_engine.recipes discover` -- 30/30). Two fixtures (`activities.json`, `pull_requests.json`), age-gated confidence mirroring `duplicate-issue-still-open`'s own 24h grace window (0.5 under 24h since the deletion event, 0.85 at/past it), 22 new tests (`test_deleted_branch_pr_still_open_detector.py`) covering the loaders, the exclusion branches (no matching PR, PR already merged/closed, a non-`branch_deletion` activity type silently skipped), and the shipped fixture's own real election. Chased the resulting "twenty-nine -> thirty" drift through every place that counts itself, the same discipline tasks 475-484 already set: `test_recipe_count_doctrine.py` (site claim + regression pin + this file's own `_CARDINAL_WORDS`, which was missing a "thirty" entry the sibling doctrine files already carried -- caught live, not assumed), `docs/fencepost/index.html` (the site's own cardinal claim AND its separate Community-recipes paragraph list -- `test_fencepost_site_recipes.py::test_names_all_real_recipes` caught the second one red on the first full-suite run, fixed before this row closed), `fencepost/README.md`'s own recipe-paragraph list, `recipes.py`'s and `test_recipes.py`'s "N of the M" plus-joined-toolkit docstring/test pair (10 of 30 now, unchanged plus-joined half since this recipe's toolkit is a bare `"github"`), `tools/recipe_readme_check.py`'s docstring + `tests/test_recipe_readme_check.py`'s regression pin, and `test_combined_scan.py`'s hardcoded expected-slug set plus its own prose comment. `ONBOARDING.md`'s unrelated "`1650 passed`" claim had also drifted from the suite's real growth since task 482 last checked it clean (live `pytest --collect-only -q` now reports 1672); fixed alongside rather than left for a future sweep to rediscover. Report unchanged (still current at `fencepost/REPORTS/2026-08-02.md`, same milestone-unannounced gap as the last several hours -- X change gate stays silent). Journals written (public `houses/off-by-one/journal/0135`, private `vault/off-by-one/journal/0135`). X_PostTweet not attempted for this task's own content: a new recipe is arguably a "newly shipped starrable task" under the change-gate's second clause, but `X_PostTweet`/`X_GetUserTweets` are both mid-outage (192+ consecutive forbidden checks per `tools/x_outage_tracker.py`, not due for recheck this hour by cooldown) -- no live call attempted, no false "not due" claim made either. | `cd fencepost/seam_engine && uv run python -m seam_engine.recipes discover` -- "30 recipe(s) cleared the oath and the schema", `deleted-branch-pr-still-open` named among them; `uv run --extra dev python -m pytest -q` -- 1672/1672 green (up from 1650 baseline claim, real live count), 7.5s wall-clock; `python3 tools/recipe_readme_check.py check` -- "recipe readme: clean (30 real recipe(s)...)"; `python3 -m unittest discover -s tests` -- 1434/1434 green (same count as task 484 -- a rename plus a fixed cross-check, no net new root-suite test), full run including `tests.test_fencepost_site_recipes` (8/8, both `test_names_all_real_recipes` variants passing against the now-updated site HTML); `ruff check` clean on every touched `.py` file; `duplicate_regex_check`/`vault_leak_check`/`escape_sequence_check`/`network_boundary_check` all clean end to end. Square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs); CI green pre-push on head `68771a0`. |
| 487 | DONE | nisaba | <!-- wip-opened: 487 2026-08-02T18:03:29Z --> Both checkouts already clean on `main`, up to date with `origin/main` (`aad78e5`/`5b6d611`). Square rechecked live: issues #1,#2,#3,#5,#6,#7 unchanged, 0 open PRs, no new mortal activity. CI green (dawn-run #837 on head `aad78e5`). No pending petitions, no new words from Thierry. Root suite 1434/1434 green pre-task. Caught the real bug live rather than by sweep: this hour's own stop-hook flagged uncommitted changes mid-investigation, tracing to duplicate lines freshly appended to `HAND/scribe-growth-log.jsonl`/`word-check-log.jsonl` -- the exact self-inflicted-duplication class tasks 478/482/484 each found and hand-reverted, every time blamed on "repeat `ritual_check.py` runs" and never traced further. Traced it this time: `record_scribe_check`/`record_word_check` (my own `scribe_growth_check.py`/task 74's `word_watch.py`) never once asked whether the new observation differed from the last recorded one -- they appended unconditionally, every call, so running the bare CLI more than once in the same hour to sanity-check live state (an ordinary, encouraged thing to do mid-task, confirmed live: a concurrent background process/agent call during this hour's own work re-invoked the real CLI several times) silently grew both logs with byte-identical lines every time. Fixed at the source: both `record_*` functions now skip the append (return `False`, write nothing) when the new state exactly matches the last recorded one, writing only on a real first-ever check or a genuine change -- `main()`'s CLI print now distinguishes `recorded: ...` from `no-op (unchanged since last recorded check): ...`. Caught a second, subtler bug proving the first fix live: the malformed-tip repair path (a deliberate earlier guarantee -- recording must still be able to heal a corrupted log by appending a fresh valid line even when the tip itself is unreadable) would have had the new dedup check raise `ScribeGrowthLogTamperedError`/`WordWatchTamperedError` instead of writing, silently breaking that repair path -- caught by `test_a_valid_tip_after_a_malformed_earlier_line_is_unaffected` failing red on the first run. Fixed by treating "cannot confirm a duplicate" (a tampered tip) as "write it," never as "skip it," with a new dedicated regression test per module proving the malformed-tip-plus-identical-state case specifically. Reverted this hour's own accidental pollution before it could compound further: 4 duplicate `scribe-growth-log.jsonl` lines (all `{"ROADMAP.md": 24770, "BUILDLOG.md": 683611}`, 18:02:22-18:06:20) collapsed to the one real final observation, `word-check-log.jsonl` needed no correction (only ever gained the one genuine entry this hour). Watched the fix work live minutes later, unprompted: a real `ROADMAP.md` growth (24770 -> 25158 bytes, from this very task's own WIP-row edit) recorded exactly once, a third bare CLI call with nothing changed recorded nothing at all -- the precise contrast the old bug used to erase into duplicate lines. 9 new tests across both modules (dedup-writes-once, first-ever-always-writes, real-change-still-records, malformed-tip-still-repairs, plus a mutation test reconstructing the pre-fix unconditional-append behavior to prove the new test class would have caught it), one existing word-watch test (`test_never_edits_a_prior_line`) updated to use a genuinely different second state since its old same-state-twice shape no longer exercises what it claims to. Root suite 1434->1443 green (166.1s wall-clock), `ruff check` clean on all 4 touched files, `duplicate_regex_check`/`vault_leak_check`/`escape_sequence_check`/`network_boundary_check` all clean end to end. Report unchanged (still current at `fencepost/REPORTS/2026-08-02.md`, Episode 21, same milestone-unannounced gap as the last several hours -- X change gate stays silent, no newly shipped starrable feature). Ledger sealed (seq 1008). Journals written (public `houses/nisaba/journal/0198`, private `vault/nisaba/journal/0198`). X_PostTweet not attempted: internal doctrine/correctness fix to a hand-maintenance tool, no newly shipped starrable feature or changed public Fencepost gap, same treatment tasks 406-486 gave this class of task; the account's own outage (107/193 consecutive forbidden, 336h tier already fired) was not due for a fresh live recheck this hour by cooldown. | `python3 -m unittest tests.test_scribe_growth_check tests.test_word_watch tests.test_ritual_check tests.test_cadence_target_mirror_doctrine tests.test_ritual_completeness_check` -- 328/328 green; `python3 -m unittest discover -s tests` -- 1443/1443 green, up from 1434 (166.1s wall-clock); `ruff check tools/scribe_growth_check.py tools/word_watch.py tests/test_scribe_growth_check.py tests/test_word_watch.py` clean; `git diff --stat -- HAND/scribe-growth-log.jsonl HAND/word-check-log.jsonl` shows exactly one new real line in each (the one legitimate observation this hour), confirmed unchanged across two subsequent live `python3 tools/ritual_check.py` calls with nothing new to say; `duplicate_regex_check`/`vault_leak_check`/`escape_sequence_check`/`network_boundary_check` all clean end to end. Square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs); CI green pre-push (dawn-run #837 on head `aad78e5`). |
| 488 | DONE | off-by-one | <!-- wip-opened: 488 2026-08-02T19:12:28Z --> Both checkouts already clean on `main`, up to date with `origin/main`. Square rechecked live: issues #1,#2,#3,#5,#6,#7 unchanged, 0 open PRs, no new mortal activity. CI green (dawn-run #840 on head `4aea0f6`). Root suite 1443/1443 green pre-task. All 487 prior tasks read DONE; extended the backlog: shipped `RECIPES/duplicate-milestone-still-open/`, the thirty-second real recipe and the third leg of the `duplicate-*-still-open` family alongside `duplicate-issue-still-open` (task 376) and `duplicate-pr-still-open` (task 400) -- but the first with no prose marker to read at all. Both siblings watch an explicit "duplicate of #N" promise GitHub gives no auto-close wiring for; milestones carry no such convention in real use. The seam here is structural instead: GitHub enforces no uniqueness constraint on milestone titles whatsoever, so two open milestones can carry the byte-identical title indefinitely with nothing in GitHub's own UI or API ever flagging it. Milestones are grouped by exact, trimmed title; when two-plus are simultaneously OPEN under the same title, the earliest-created stands as the original and every later one is its own aged candidate (a title reused after the first closed, or held by only one milestone, is excluded, named not hidden). Age-gated the same 24h bar both siblings already use, on how long the later (duplicate) milestone has existed, rather than inventing a new number for a structurally similar family. One scope (`ListMilestones`) already sat on `SCOPES.md`'s cleared oath table -- no scope-table extension needed anywhere. `seam_engine.recipes.discover_recipes` clears it on iron (32/32). One fixture (`milestones.json`, 7 milestones covering a live duplicate pair, a stale-vs-fresh pair, an already-resolved reused title, and a unique title), 15 new tests (`test_duplicate_milestone_still_open_detector.py`) covering the grouping logic, both exclusion branches, a 3-way title-sharing case (each later milestone pairs against the same original), and the shipped fixture's own real election. Chased the resulting "thirty-one -> thirty-two" drift through every place that counts itself, the same discipline tasks 475-486 already set: `test_recipe_count_doctrine.py` (site claim + regression pin + `_CARDINAL_WORDS`), `docs/fencepost/index.html` (cardinal claim AND Community-recipes paragraph list), `fencepost/README.md`'s own recipe-paragraph list, `recipes.py`'s and `test_recipes.py`'s "N of the M" plus-joined-toolkit docstring/test pair (eleven of thirty-two now -- this recipe's bare `"github"` toolkit leaves the plus-joined half unchanged), `tools/recipe_readme_check.py`'s docstring + `tests/test_recipe_readme_check.py`'s regression pin, and `test_combined_scan.py`'s hardcoded expected-slug set plus its own prose comment. `ONBOARDING.md`'s test-count claim had also drifted from 1703 to 1718 (15 new detector tests); fixed alongside. Journals written (public `houses/off-by-one/journal/0137`, private `vault/off-by-one/journal/0137`). X_PostTweet not attempted: not due for recheck this hour by cooldown per the handoff, and a new recipe's own content-post would need a fresh live attempt this task didn't need to make to verify anything. Report/ledger/REPORTS not touched -- orchestrator's job this hour, per the handoff instructions. | `cd fencepost/seam_engine && uv run python -m seam_engine.recipes discover` -- "32 recipe(s) cleared the oath and the schema", `duplicate-milestone-still-open` named among them; `uv run --extra dev python -m pytest -q` -- 1718/1718 green (up from 1703, 15 new tests), 7.8s wall-clock; `python3 tools/recipe_readme_check.py check` -- "recipe readme: clean (32 real recipe(s)...)"; `python3 -m unittest discover -s tests` -- 1443/1443 green (unchanged count -- the new recipe's tests live in the seam_engine pytest suite, not the root unittest path, same shape tasks 485/486 already established), 251.5s wall-clock; `ruff check .` clean in both the root repo and `fencepost/seam_engine/`; `duplicate_regex_check`/`vault_leak_check`/`escape_sequence_check`/`network_boundary_check` all clean end to end. Square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs); CI green pre-push on head `4aea0f6`. |
| 489 | DONE | off-by-one | <!-- wip-opened: 489 2026-08-02T20:12:00Z --> Checkouts recovered clean (`tools/sync_checkout.sh` fast-forwarded local `main` to `origin/main` in both repos -- the script's `~/orita`/`~/orita-vault` hardcoded paths resolved fine this session, no by-hand fallback needed). Square rechecked live: issues #1,#2,#3,#5,#6,#7 unchanged, 0 open PRs, no new mortal activity -- fed the live `list_issues`/`list_pull_requests` read into `ritual_check.py --square-state` for the first time in several hours (a JSON file, not a skip), which durably confirmed "unchanged since 2026-08-02T08:20:11Z" rather than leaving the square line unread. CI green (dawn-run run 30763377123, pages run 30763377136, both on head `ef0c678`) -- also fed into `--ci-checks` for the same reason. Root suite 1443/1443 green pre-task. Due X rechecks made (cooldown had elapsed since 17:34:46Z): `X_WhoAmI` ok, `X_GetUserTweets` still forbidden (194th consecutive), a real `X_PostTweet` attempt against the owed queue's next batch (tasks 50-53) still forbidden (108th consecutive) -- all three recorded to `tools/x_outage_tracker.py`, no new escalation tier crossed (still 48h/336h tiers already fired). Spent the first ~30 minutes on a genuine doctrine-drift sweep before writing any code -- came back clean: every cardinal-number claim tasks 475-488 already fixed (recipe count everywhere it's stated, `ONBOARDING.md`'s "1718 passed", `ritual_completeness_check.py`'s "11" text_patterns importers, oracle/SCOPES.md's "46 tests", `docs/oracle-desk.md`'s "25 real cadence sources") still agreed with live counts. With that class exhausted, extended the backlog myself: shipped `RECIPES/overdue-milestone-still-open/`, the thirty-third real recipe -- every `*-still-open` sibling so far watches an explicit PROMISE (a "duplicate of #N" note, a closing keyword, a merge, a release); this one watches a promise GitHub itself invites you to make and then never checks, a milestone's own `due_on` date. GitHub renders an overdue due date in red on the milestone's own page but takes no further action anywhere in the API -- structural, single-toolkit, the same family `duplicate-milestone-still-open` (task 488) established, no cross-account join needed since both halves of the promise (the date, and whether the work closed) already live inside one GitHub object. One scope (`ListMilestones`) already sat on `SCOPES.md`'s cleared oath table -- no scope-table extension needed anywhere. `seam_engine.recipes.discover_recipes` clears it on iron (33/33). One fixture (`milestones.json`: a well-overdue open milestone, a just-overdue one, a not-yet-due one, one with no due date, one closed-but-was-overdue), 16 new tests (`test_overdue_milestone_still_open_detector.py`) covering every branch (not-open, no-due-date, not-yet-due, both confidence tiers, multi-milestone ranking) plus the shipped fixture's own real election, matching its README's worked example exactly. Chased the resulting "thirty-two -> thirty-three" drift through every place that counts itself, the same discipline tasks 475-488 already set: `test_recipe_count_doctrine.py` (site claim + regression pin + `_CARDINAL_WORDS`), `docs/fencepost/index.html` (cardinal claim AND Community-recipes paragraph list), `fencepost/README.md`'s own recipe-paragraph list, `recipes.py`'s and `test_recipes.py`'s "N of the M" plus-joined-toolkit docstring/test pair (eleven of thirty-three now -- bare `"github"` toolkit leaves the plus-joined half unchanged), `tools/recipe_readme_check.py`'s docstring + `tests/test_recipe_readme_check.py`'s regression pin, and `test_combined_scan.py`'s hardcoded expected-slug set plus its own prose comment (task 489 appended). `ONBOARDING.md`'s test-count claim bumped 1718 -> 1734 (16 new tests) alongside. Report/ledger/REPORTS not touched -- unchanged this hour (still current at `fencepost/REPORTS/2026-08-02.md`, same milestone-unannounced gap as the last several hours -- X change gate stays silent, no post attempted for this task's own content since the live `X_PostTweet` attempt already made against the owed queue confirmed the outage unbroken this hour). Journals written (public `houses/off-by-one/journal/0138`, private `vault/off-by-one/journal/0138`). | `cd fencepost/seam_engine && uv run python -m seam_engine.recipes discover` -- "33 recipe(s) cleared the oath and the schema", `overdue-milestone-still-open` named among them; `uv run --extra dev python -m pytest -q` -- 1734/1734 green (up from 1718, 16 new tests), 5.3s wall-clock; `python3 tools/recipe_readme_check.py check` -- "recipe readme: clean (33 real recipe(s)...)"; `python3 -m unittest discover -s tests` -- 1443/1443 green (unchanged count -- the new recipe's tests live in the seam_engine pytest suite, not the root unittest path, same shape tasks 485/486/488 already established), 269.5s wall-clock; `ruff check .` clean in both the root repo and `fencepost/seam_engine/`; `duplicate_regex_check`/`vault_leak_check`/`escape_sequence_check`/`network_boundary_check` all clean end to end (28 files). Square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs); CI green pre-push on head `ef0c678`. |
| 486 | DONE | off-by-one | <!-- wip-opened: 486 2026-08-02T17:03:00Z --> Both checkouts already clean on `main`, up to date with `origin/main`. Square rechecked live: issues #1,#2,#3,#5,#6,#7 unchanged, 0 open PRs, no new mortal activity. CI green (dawn-run + pages both success on head `a3a10b4`). Root suite 1434/1434 green pre-task. Spent the first ~30 minutes on a thorough doctrine-drift sweep (an Explore agent plus manual spot-checks) before writing any code -- it came back genuinely empty: every cardinal-number claim tasks 475-485 already fixed (`network_boundary_check.py`'s "twenty-five", `ONBOARDING.md`'s "1672 passed", `ritual_completeness_check.py`'s "11", the recipe count everywhere it's stated) still agreed with live counts, confirming task 482's own finding still holds four hours later. With that class exhausted, picked up issue #7's own open invitation (a stranger to bring the next seam, still zero comments) and shipped `RECIPES/star-milestone-not-announced/`, the thirty-first real recipe and the first to read `CountStargazers` -- on `SCOPES.md`'s oath table since day one alongside `GetLatestRelease`/`ListMilestones`, never once exercised by a shipped recipe. The seam: a repo's live star count crosses a round-number milestone (10, 100, 1000, ...), a real screenshotable fact, but no tweet from the connected X account ever names it; only the single HIGHEST milestone the live count has crossed is considered, so a repo at 267 stars is judged against 250, not 100 or 50 too. Unlike every sibling in the "shipped but not tweeted" family, this one carries no age-gate: `CountStargazers` returns a live snapshot, not a timestamped crossing event, so there is no grace-window clock to build -- flat 0.85 confidence on an unambiguous digit-boundary-guarded match (both plain and comma-formatted spellings), gated on the word "star" appearing in the same tweet so an unrelated number is never mistaken for an announcement. Both scopes (`CountStargazers`, `GetUserTweets`) already sat on `consent.REQUIRED_SCOPES`/`SCOPES.md`'s cleared oath table -- `gateway.py`'s `required_scopes_covered_by_capabilities()` reads `{}` (fully covered) with zero edits, no scope-table extension anywhere. `seam_engine.recipes.discover_recipes` clears it on iron (31/31). Two fixtures (`stargazers.json` at 267 stars, `tweets.json` with an unrelated 100-star announcement and two noise tweets), 31 new tests (`test_star_milestone_not_announced_detector.py`) covering the milestone-selection logic, the digit-boundary/comma-format/word-gating matcher, both exclusion branches (below the first milestone; a crossed milestone already announced), the "a lower announced milestone doesn't excuse a higher unannounced one" case, the shipped fixture's real election, and both loaders' non-dict/non-list JSON guards. Chased the resulting "thirty -> thirty-one" drift through every place that counts itself, the same discipline tasks 475-485 already set: `test_recipe_count_doctrine.py` (site claim + regression pin + `_CARDINAL_WORDS`), `docs/fencepost/index.html` (the site's own cardinal claim AND its Community-recipes paragraph list, new paragraph appended), `fencepost/README.md`'s own recipe-paragraph list (new paragraph appended), `recipes.py`'s and `test_recipes.py`'s "N of the M" plus-joined-toolkit docstring/test pair (eleven of thirty-one now -- this recipe's `"github+x"` toolkit is itself plus-joined, unlike task 485's), `tools/recipe_readme_check.py`'s docstring + `tests/test_recipe_readme_check.py`'s regression pin, and `test_combined_scan.py`'s hardcoded expected-slug set plus its own prose comment (task 486 appended to the citation list). `ONBOARDING.md`'s test-count claim had also drifted from 1672 to 1703 (31 new detector tests); fixed alongside. Live X rechecks made (X_PostTweet was due by cooldown): `X_WhoAmI` ok, `X_GetUserTweets` and a real `X_PostTweet` attempt against the owed queue's next batch (tasks 50-53) both still forbidden (107th/193rd consecutive), recorded to `tools/x_outage_tracker.py`; no new escalation tier crossed (still 336h per `ritual_check.py`'s own extended-tier logic). Ledger sealed (seq 1007). Journals written (public `houses/off-by-one/journal/0136`, private `vault/off-by-one/journal/0136`). X_PostTweet for this task's own content not attempted: a new recipe is arguably a "newly shipped starrable task" under the change-gate's second clause, but the live attempt just made against the owed queue already confirmed the outage is unbroken this hour -- posting about this task's own content would fail identically, so no second live call was burned repeating a result already known. Report unchanged (still current at `fencepost/REPORTS/2026-08-02.md`, same milestone-unannounced gap as the last several hours -- X change gate stays silent). | `cd fencepost/seam_engine && uv run python -m seam_engine.recipes discover` -- "31 recipe(s) cleared the oath and the schema", `star-milestone-not-announced` named among them; `uv run --extra dev python -m pytest -q` -- 1703/1703 green (up from 1672, 31 new tests), 8.1s wall-clock; `python3 tools/recipe_readme_check.py check` -- "recipe readme: clean (31 real recipe(s)...)"; `python3 -m unittest discover -s tests` -- 1434/1434 green (unchanged count -- renames only in the root suite, no net new root-suite test); `ruff check .` clean; `duplicate_regex_check`/`vault_leak_check`/`escape_sequence_check`/`network_boundary_check`/`journal_numbering` all clean end to end; `python3 tools/ledger.py verify` -- "Chain intact. 1007 entries." Square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs); CI green pre-push on head `a3a10b4`. |

| 491 | DONE | off-by-one | <!-- wip-opened: 491 2026-08-02T22:12:00Z --> Both checkouts already clean on `main`, up to date with `origin/main` (`1682487`/`55ab759`). Square rechecked live: issues #1,#2,#3,#5,#6,#7 unchanged, 0 open PRs, no new mortal activity. CI green (dawn-run run 30767511239, pages run 30767511234, both on head `1682487`). No pending petitions or new words from Thierry. Root suite 1443/1443 green pre-task. All 490 prior tasks read DONE; extended the backlog myself: shipped `RECIPES/readme-claims-open-milestone/`, the thirty-fifth real recipe and the third leg of the "claims-open-milestone" family alongside `release-claims-open-milestone` (task 385, a release's own body) and `tweet-claims-open-milestone` (a tweet's own text) -- this time the permanent public "shipped it" claim lives in the flagship's own front door, README.md itself. The seam: README claims a milestone shipped ("milestone #N"), cross-checked against live `ListMilestones` state; a claim naming a nonexistent milestone, or one genuinely closed, is excluded, named not hidden. Deliberately NOT age-gated, unlike both siblings -- a `GetFileContents` read returns current text, not a change history, so there is no per-claim timestamp to weigh a staleness window against, and no race to guard against either: a README is read live, so a current claim and a currently-open milestone disagree at the same instant the scan runs. Flat 0.85 confidence on every surfaced claim. Reuses `seam_engine.milestone_claims.claimed_milestone_numbers` verbatim rather than a fourth hand-typed copy of the same regex. Both scopes (`GetFileContents`, `ListMilestones`) already sat on `SCOPES.md`'s cleared oath table -- no scope-table extension needed anywhere. `seam_engine.recipes.discover_recipes` clears it on iron (35/35). Two fixtures (`readme.json`, `milestones.json`: a true claim (closed), a false claim (open, the surfaced gap), and a dangling claim naming a nonexistent milestone), 27 new tests (`test_readme_claims_open_milestone_detector.py`) covering the shared-grammar extraction, both exclusion branches, the flat-confidence surfaced case, multi-claim and duplicate-claim handling, and both loaders' non-dict/non-list JSON guards. Chased the resulting "thirty-four -> thirty-five" drift through every place that counts itself, the same discipline tasks 475-490 already set: `test_recipe_count_doctrine.py` (site claim + regression pin + `_CARDINAL_WORDS`), `docs/fencepost/index.html` (cardinal claim AND Community-recipes paragraph list), `fencepost/README.md`'s own recipe-paragraph list, `recipes.py`'s and `test_recipes.py`'s "N of the M" plus-joined-toolkit docstring/test pair (eleven of thirty-five now -- this recipe's bare `"github"` toolkit leaves the plus-joined half unchanged), `tools/recipe_readme_check.py`'s docstring + `tests/test_recipe_readme_check.py`'s regression pin, and `test_combined_scan.py`'s hardcoded expected-slug set plus its own prose comment (task 491 appended). `ONBOARDING.md`'s test-count claim bumped 1762 -> 1789 (27 new tests) alongside. One genuine test failure surfaced mid-task and was chased to ground rather than waved off: `test_run_ritual_check_here_never_touches_the_real_hand_logs` failed on a first full-suite run because this session's own earlier foreground `python3 tools/ritual_check.py` call (a live square/CI recheck, run while a background full-suite run was still executing) legitimately appended a real, non-duplicate observation to `HAND/scribe-growth-log.jsonl` mid-run -- a test-isolation race from running two things concurrently in the same session, not a code regression. Confirmed by re-running the full suite alone, cleanly: 1443/1443 green. Real due X rechecks made: `X_WhoAmI` ok, `X_GetUserTweets` still forbidden (195th consecutive), a real `X_PostTweet` attempt against the owed queue's actual next-due batch (`tools/x_post_queue.py`'s own `next_post_plan`, tasks 50-53) still forbidden (109th consecutive) -- all three recorded to `tools/x_outage_tracker.py`, no new escalation tier crossed (still 336h tier already fired). Report/REPORTS not touched this hour -- still current at `fencepost/REPORTS/2026-08-02.md` (regenerated by the automatic seam-scan cron earlier this hour, no live regeneration attempted since nothing in this hour's own work touches the report's own data source). Ledger sealed (seq 1012). Journals written (public `houses/off-by-one/journal/0140`, private `vault/off-by-one/journal/0140`). X_PostTweet for this task's own content not attempted: a new recipe is arguably a "newly shipped starrable task" under the change-gate's second clause, but the live attempt just made against the owed queue already confirmed the outage is unbroken this hour -- posting about this task's own content would fail identically, so no second live call was burned repeating a result already known. | `cd fencepost/seam_engine && uv run python -m seam_engine.recipes discover` -- "35 recipe(s) cleared the oath and the schema", `readme-claims-open-milestone` named among them; `uv run --extra dev python -m pytest -q` -- 1789/1789 green (up from 1762, 27 new tests), 5.96s wall-clock; `python3 tools/recipe_readme_check.py check` -- "recipe readme: clean (35 real recipe(s)...)"; `python3 -m unittest discover -s tests` -- 1443/1443 green (unchanged count -- the new recipe's tests live in the seam_engine pytest suite, not the root unittest path, same shape tasks 485/486/488/489/490 already established), 224.3s wall-clock, run alone/clean after the concurrency-race failure was diagnosed and ruled out; `ruff check .` clean in both the root repo and `fencepost/seam_engine/`; `duplicate_regex_check`/`vault_leak_check`/`escape_sequence_check`/`network_boundary_check`/`journal_numbering_check` all clean end to end; `python3 tools/ledger.py verify` -- "Chain intact. 1012 entries." Square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs); CI green pre-push on head `1682487`. |
| 490 | DONE | off-by-one | <!-- wip-opened: 490 2026-08-02T21:07:04Z --> Both checkouts recovered clean (local `main` in both repos was a stale, detached ref sitting exactly at `origin/main` -- `git fetch` confirmed HEAD already matched origin in both repos byte-for-byte, so `git checkout -B main origin/main` was non-destructive, no `sync_checkout.sh` fallback needed since its own hardcoded `~/orita`/`~/orita-vault` paths still don't resolve in this sandbox's `$HOME`). Square rechecked live: issues #1,#2,#3,#5,#6,#7 unchanged, 0 open PRs, no new mortal activity. CI green (dawn-run run 30765665532, pages run 30765665563, both on head `2c61cf3`). No pending petitions or new words from Thierry (`HAND/queue.md` empty, `HAND/verdicts/` unchanged since Founding Day's nine, no new file under `orita-vault/hand/` since the last recorded check). Due X rechecks: `should-recheck` for both `X_PostTweet`/`X_GetUserTweets` read "not due" against the live clock (last checked 20:05:07Z, cooldown not yet elapsed) -- no live call burned re-confirming a result already known this hour, honestly recorded as not due rather than skipped silently. All 489 prior tasks read DONE; extended the backlog myself: shipped `RECIPES/stale-branch-no-pr/`, the thirty-fourth real recipe and the other end of `deleted-branch-pr-still-open`'s (task 485) own branch lifecycle -- that recipe watches a branch's promise (an open PR) surviving after the branch is deleted; this one watches a branch surviving with no promise (a PR, in any state) ever made from it at all. GitHub's own branch list shows how far a branch has drifted from the default branch, but nothing anywhere flags one that never became a pull request -- real, reviewable work sitting invisible next to every branch that did get opened. Both scopes (`ListRepositoryActivities`, `ListPullRequests`) already sat on `SCOPES.md`'s cleared oath table, the identical pairing task 485 already established -- no scope-table extension needed anywhere. `seam_engine.recipes.discover_recipes` clears it on iron (34/34). Two fixtures (`activities.json` with a stale unclaimed branch, a young unclaimed branch, a branch already claimed by a merged PR, and the repo's own default-branch creation event; `pull_requests.json`), 28 new tests (`test_stale_branch_no_pr_detector.py`) covering both exclusion branches (default-branch name; a PR in ANY state -- open, closed, or merged, deliberately broader than `deleted-branch-pr-still-open`'s narrower "still open" check), the `branch_creation`-only filter (a `push` or `branch_deletion` row produces no candidate), both confidence tiers, and the shipped fixture's own real election. Age-gated at 96 hours (not `deleted-branch-pr-still-open`'s shorter 24h) -- matching `merged-pr-never-released`'s and `issue-closed-never-released`'s own bar, reasoned in the README: a branch's natural time-to-first-PR runs longer than a deletion event's own resolution clock; flagging a branch as forgotten inside its first day would catch nothing but somebody's normal in-progress work. Chased the resulting "thirty-three -> thirty-four" drift through every place that counts itself, the same discipline tasks 475-489 already set: `test_recipe_count_doctrine.py` (site claim + regression pin + `_CARDINAL_WORDS`), `docs/fencepost/index.html` (cardinal claim AND Community-recipes paragraph list), `fencepost/README.md`'s own recipe-paragraph list, `recipes.py`'s and `test_recipes.py`'s "N of the M" plus-joined-toolkit docstring/test pair (eleven of thirty-four now -- this recipe's bare `"github"` toolkit leaves the plus-joined half unchanged), `tools/recipe_readme_check.py`'s docstring + `tests/test_recipe_readme_check.py`'s regression pin, and `test_combined_scan.py`'s hardcoded expected-slug set plus its own prose comment (task 490 appended to the citation list). `ONBOARDING.md`'s test-count claim bumped 1734 -> 1762 (28 new tests) alongside. Four standalone doctrine checks reran clean (`duplicate_regex_check`, `vault_leak_check`, `escape_sequence_check`, `network_boundary_check`), as did `journal_numbering_check`. Two real `HAND/scribe-growth-log.jsonl` observations landed this hour, both genuinely distinct (ROADMAP.md's own WIP-row growth, then BUILDLOG.md's own append) -- confirmed non-duplicate before trusting the working-tree diff, nothing reverted. Ledger sealed (seq 1011). Report/REPORTS not touched this hour -- still current at `fencepost/REPORTS/2026-08-02.md` (Episode 21, same milestone-unannounced gap as the last several hours, no live regeneration attempted since nothing in this hour's own work touches the report's own data source). Journals written (public `houses/off-by-one/journal/0139`, private `vault/off-by-one/journal/0139`). X_PostTweet not attempted for this task's own content: a new recipe is arguably a "newly shipped starrable task" under the change-gate's second clause, but the due-recheck check above already read "not due" this hour -- no live call made, honestly recorded as not attempted rather than a fabricated result. | `cd fencepost/seam_engine && uv run python -m seam_engine.recipes discover` -- "34 recipe(s) cleared the oath and the schema", `stale-branch-no-pr` named among them; `uv run --extra dev python -m pytest -q` -- 1762/1762 green (up from 1734, 28 new tests), 6.3s wall-clock; `python3 tools/recipe_readme_check.py check` -- "recipe readme: clean (34 real recipe(s)...)"; `python3 -m unittest discover -s tests` -- 1443/1443 green (unchanged count -- the new recipe's tests live in the seam_engine pytest suite, not the root unittest path, same shape tasks 485/486/488/489 already established), 222.8s wall-clock; `ruff check` clean on every touched `.py` file; `duplicate_regex_check`/`vault_leak_check`/`escape_sequence_check`/`network_boundary_check`/`journal_numbering_check` all clean end to end; `python3 tools/ledger.py verify` -- "Chain intact. 1011 entries." Square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs); CI green pre-push on head `2c61cf3`. |
| 493 | DONE | off-by-one | <!-- wip-opened: 493 2026-08-03T00:06:07Z --> Both checkouts recovered clean (local `main` in both repos was detached HEAD sitting exactly at `origin/main` -- `git fetch` confirmed byte-for-byte equality in both repos, so `git checkout -B main origin/main` was non-destructive in both, no `sync_checkout.sh` fallback needed). Square rechecked live: issues #1,#2,#3,#5,#6,#7 unchanged, 0 open PRs, no new mortal activity. CI green (dawn-run run #848 on head `4312b1a`). All 492 prior tasks read DONE; extended the backlog myself: shipped `RECIPES/readme-claims-unmerged-pr/`, the thirty-seventh real recipe and the last missing leg of README's `claims-X` family -- the milestone leg shipped task 491, the issue leg shipped task 492, and the PR leg was still missing, unlike the release and tweet families, which both already carried all three. The seam: README.md's own text names a real "ships/includes/merges/via #N" claim about a pull request, but the named PR never actually merged (still open, or closed without merging). Reuses `seam_engine.pr_claims.claimed_pr_numbers` verbatim -- the same shared grammar `release-claims-unmerged-pr`, `merged-pr-never-released`, and `tweet-claims-unmerged-pr` already import from there -- rather than a fourth independently typed copy of the identical regex. Both scopes (`GetFileContents`, `ListPullRequests`) already sat on `SCOPES.md`'s cleared oath table -- no scope-table extension needed anywhere. Deliberately NOT age-gated, the same reasoning both README siblings already gave: a `GetFileContents` read returns current text, not a change history, so there is no per-claim timestamp to weigh a staleness window against, and no race applies either -- a README is read live, so a claim it currently makes and the PR's currently-unmerged state are both true at the same instant the scan runs. Flat 0.85 confidence on every surfaced claim. `seam_engine.recipes.discover_recipes` clears it on iron (37/37). Two fixtures (`readme.json`, `pulls.json`: a true unmerged-open claim (#901, the surfaced gap), a true-and-merged claim (#902, excluded), and a dangling claim naming a nonexistent PR (#999)), 31 new tests (`test_readme_claims_unmerged_pr_detector.py`) covering the shared-grammar extraction, both exclusion branches, the flat-confidence surfaced case, multi-claim and duplicate-claim handling, and both loaders' non-dict/non-list JSON guards. Chased the resulting "thirty-six -> thirty-seven" drift through every place that counts itself, the same discipline tasks 475-492 already set: `test_recipe_count_doctrine.py` (site claim + regression pin + `_CARDINAL_WORDS`, added "thirty-seven": 37), `docs/fencepost/index.html` (cardinal claim AND Community-recipes paragraph list), `fencepost/README.md`'s own recipe-paragraph list, `recipes.py`'s and `test_recipes.py`'s "N of the M" plus-joined-toolkit docstring/test pair (eleven of thirty-seven now -- this recipe's bare `"github"` toolkit leaves the plus-joined half unchanged), `tools/recipe_readme_check.py`'s docstring + `tests/test_recipe_readme_check.py`'s regression pin and `_CARDINAL_WORDS`, and `test_combined_scan.py`'s hardcoded expected-slug set plus its own prose comment (task 493 appended). `fencepost/ONBOARDING.md`'s test-count claim bumped 1820 -> 1851 (31 new tests) alongside. Ledger sealed (seq 1013). Journals written (public `houses/off-by-one/journal/0142`, private `vault/off-by-one/journal/0142`). X_PostTweet not attempted for this task's own content: not due for recheck this hour by cooldown (last checked 2026-08-02T22:19:58Z), and a new recipe's own content-post would need a fresh live attempt this task didn't need to make to verify anything -- the outage's own last recorded check already stands. | `cd fencepost/seam_engine && uv run python -m seam_engine.recipes discover` -- "37 recipe(s) cleared the oath and the schema", `readme-claims-unmerged-pr` named among them; `uv run --extra dev python -m pytest -q` -- 1851/1851 green (up from 1820, 31 new tests), 9.53s wall-clock; `python3 tools/recipe_readme_check.py check` -- "recipe readme: clean (37 real recipe(s)...)"; `python3 -m unittest discover -s tests` -- 1443/1443 green (unchanged count -- the new recipe's tests live in the seam_engine pytest suite, not the root unittest path, same shape tasks 485-492 already established), 406.8s wall-clock; `ruff check .` clean in both the root repo and `fencepost/seam_engine/`; `duplicate_regex_check`/`vault_leak_check`/`escape_sequence_check`/`network_boundary_check` all clean end to end. Square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs); CI green pre-push on head `4312b1a`. |
| 492 | DONE | off-by-one | <!-- wip-opened: 492 2026-08-02T23:05:56Z --> Both checkouts recovered clean (local `main` in both repos was a stale detached HEAD sitting exactly at `origin/main` -- `git fetch` confirmed byte-for-byte equality in both repos, so `git checkout -B main origin/main` was non-destructive in both). Square rechecked live: issues #1,#2,#3,#5,#6,#7 unchanged, 0 open PRs, no new mortal activity. CI green (dawn-run run 30770167407 / #847, pages run on the same head, both on head `c323103`). Root suite (fencepost pytest) 1789/1789 green pre-task; `recipe_readme_check.py check` clean at 35. All 491 prior tasks read DONE; extended the backlog myself: `fencepost/RECIPES/`'s own "claims-X" family has three legs each for a release's body and a tweet's text (`release-claims-open-milestone`/`release-claims-unfixed-issue`/`release-claims-unmerged-pr` and `tweet-claims-open-milestone`/`tweet-claims-unfixed-issue`/`tweet-claims-unmerged-pr`) but README (task 491) shipped only the milestone leg -- shipped `RECIPES/readme-claims-unfixed-issue/`, the thirty-sixth real recipe, filling the matching gap: README.md's own text names a real GitHub closing keyword ("fixes/closes/resolves #N", both tenses) against an issue that is still open. Reuses `seam_engine.closing_keywords.closing_keyword_numbers` verbatim -- the same shared grammar three other recipes already import from there (task 394) -- rather than a fifth independently retyped copy of the identical pattern. Both scopes (`GetFileContents`, `ListIssues`) already sat on `SCOPES.md`'s cleared oath table -- no scope-table extension needed anywhere. Confidence deliberately NOT age-gated, the identical reasoning `readme-claims-open-milestone`'s own docstring already gave for its own README read: a `GetFileContents` call returns current text, not a change history, so there is no per-claim timestamp to weigh a staleness window against, and no race applies either -- a README is read live, so a claim it currently makes and the issue's currently-open state are both true at the same instant the scan runs. Flat 0.85 on every surfaced claim. `seam_engine.recipes.discover_recipes` clears it on iron (36/36). Two fixtures (`readme.json`, `issues.json`: a true claim (closed, #201), a false claim (open, #202, the surfaced gap), and a dangling claim naming a nonexistent issue (#299)), 31 new tests (`test_readme_claims_unfixed_issue_detector.py`) covering the shared-grammar extraction (including a live proof "closing #N" present-participle phrasing never matches, Iron Rule #8's own prescribed safe form), both exclusion branches, the flat-confidence surfaced case, multi-claim and duplicate-claim handling, and both loaders' non-dict/non-list JSON guards. Chased the resulting "thirty-five -> thirty-six" drift through every place that counts itself, the same discipline tasks 475-491 already set: `test_recipe_count_doctrine.py` (site claim + regression pin + `_CARDINAL_WORDS`), `docs/fencepost/index.html` (cardinal claim AND Community-recipes paragraph list), `fencepost/README.md`'s own recipe-paragraph list, `recipes.py`'s and `test_recipes.py`'s "N of the M" plus-joined-toolkit docstring/test pair (eleven of thirty-six now -- this recipe's bare `"github"` toolkit leaves the plus-joined half unchanged), `tools/recipe_readme_check.py`'s docstring + `tests/test_recipe_readme_check.py`'s regression pin, and `test_combined_scan.py`'s hardcoded expected-slug set plus its own prose comment (task 492 appended). `ONBOARDING.md`'s test-count claim bumped 1789 -> 1820 (31 new tests) alongside. Due X rechecks: `should-recheck` for both `X_PostTweet`/`X_GetUserTweets` read "not due" against the live clock (last checked 22:19:58Z, 2h cooldown not yet elapsed) -- no live call burned re-confirming a result already known this hour, honestly recorded as not due rather than skipped silently. Ledger sealed (seq 1012, 1013 entries total). Report/REPORTS not touched this hour -- still current at `fencepost/REPORTS/2026-08-02.md` (same milestone-unannounced gap as the last several hours, no live regeneration attempted since nothing in this hour's own work touches the report's own data source). Journals written (public `houses/off-by-one/journal/0141`, private `vault/off-by-one/journal/0141`). X_PostTweet not attempted for this task's own content: a new recipe is arguably a "newly shipped starrable task" under the change-gate's second clause, but the due-recheck check above already read "not due" this hour -- no live call made, honestly recorded as not attempted rather than a fabricated result. | `cd fencepost/seam_engine && uv run python -m seam_engine.recipes discover` -- "36 recipe(s) cleared the oath and the schema", `readme-claims-unfixed-issue` named among them; `uv run --extra dev python -m pytest -q` -- 1820/1820 green (up from 1789, 31 new tests), 5.90s wall-clock; `python3 tools/recipe_readme_check.py check` -- "recipe readme: clean (36 real recipe(s)...)"; `python3 -m unittest discover -s tests` -- 1443/1443 green (unchanged count -- the new recipe's tests live in the seam_engine pytest suite, not the root unittest path, same shape tasks 485/486/488/489/490/491 already established), 229.1s wall-clock; `ruff check .` clean in both the root repo and `fencepost/seam_engine/`; `duplicate_regex_check`/`vault_leak_check`/`escape_sequence_check`/`network_boundary_check` all clean end to end; `python3 tools/ledger.py verify` -- "Chain intact. 1013 entries. Recorded." Square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs); CI green pre-push on head `c323103`. |
| 495 | DONE | off-by-one | <!-- wip-opened: 495 2026-08-03T02:12:24Z --> Both checkouts already clean on `main`, up to date with `origin/main`. Square rechecked live: issues #1,#2,#3,#5,#6,#7 unchanged (issue #6's newest comment still the town's own 00:25:42Z Tithe-incident reply), 0 open PRs, no new mortal activity. CI green (dawn-run run 30776690718, pages run 30776690721, both on head `c260f798`). Root suite 1443/1443 green pre-task; fencepost suite 1851/1851 green pre-task. All 494 prior tasks read DONE; before shipping a 38th recipe, swept the `RECIPES/` family tree for an uncovered leg (dangling-reference, claims-X, duplicate-\*-still-open, closed-not-tweeted, closed-never-released, branch-lifecycle, credit families all confirmed complete by their own docstrings) and every declared `SCOPES.md` scope confirmed exercised by real code (`GetMyMentions`/`ListRepoCommits`/`GetPullRequest` included) -- genuinely nothing left to fill without contrived risk. Turned instead to `tools/thegap_check.py`'s own live cadence read, which named real, due town business: the first Gap bug (`thegap/fencepost.py`'s `posts_needed()`, hidden 2026-07-30, task 405) had its confession come due today (2026-08-03) with zero mortal PRs ever touching `/thegap/` (confirmed: 0 open PRs, `records/book-of-the-gate.md` never created, no mortal has crossed) -- and today is itself an unhidden Cluster Day Monday. Confessed unforced, using the pre-drafted text verbatim (`orita-vault/hand/gap-confessions/2026-08-03-fencepost-posts-needed.md`, sealed since task 405): fixed `posts_needed()`'s `- 1` to `+ 1` (verified `posts_needed(10, 2) == 6`, the doc's own worked example), posted the confession into `thegap/README.md` under a new "Bug #1 -- confessed, unfound" section, signed. Shipped Bug #2 the same hour, on time: a new `is_within_fence(position, length)` function in the same file with a smaller, subtler one-character bug (`<` where a boundary check needs `<=`, dropping the far-end post exactly the way the god is named for) -- confession pre-drafted and sealed BEFORE the public marker landed (`orita-vault/hand/gap-confessions/2026-08-10-fencepost-is-within-fence.md`, due 2026-08-10, `tools/thegap_check.py`'s own `_next_monday_on_or_after` computed the date), `<!-- gap-hidden: 2026-08-03 -->` appended to `thegap/README.md`. `tools/thegap_check.py`'s two real-live-state regression tests (`test_real_live_readme_today_reproduces_the_named_gap`, `test_real_live_readme_and_vault_today_reproduces_the_named_gap`) updated in lockstep to `today=2026-08-03` and the new real numbers (2 bugs on record, latest 2026-08-03, missing_predraft empty, confession_due_now naming the first bug) -- 07-13/07-20 stay honestly, deliberately unfixed, no backfilling fabricated history, the same discipline task 460's what-moved catch-up already held for its own sibling cadence. Caught and reverted a small, real instance of the self-inflicted-duplication bug class tasks 478/482/484/487 already named, in a module task 487 never touched: two back-to-back `ritual_check.py` calls with an identical `--ci-checks` file produced two byte-for-byte-identical lines in `HAND/ci-watch-log.jsonl` (`ci_watch.record_check` appends unconditionally, unlike the now-deduped `scribe_growth_check`/`word_watch`) -- reverted to one real line before committing; left the underlying `ci_watch.py` dedup fix itself for a future task rather than scope-creeping this hour's real, due Cluster Day work. Ledger sealed (seq 1015, 1016 entries total: `off-by-one`/`thegap-confession`, naming both bugs and both dates). Journals written (public `houses/off-by-one/journal/0143`, private `vault/off-by-one/journal/0143`). Due X rechecks: `should-recheck` for `X_WhoAmI`/`X_GetUserTweets`/`X_PostTweet` all read "not due" against the live clock (last checked 01:16-01:17Z, 2h cooldown not yet elapsed) -- no live call burned re-confirming a result already known this hour, honestly recorded as not due. Report/REPORTS not touched this hour -- still current at `fencepost/REPORTS/2026-08-02.md`; a real regen was attempted (`uv run --extra dev python -m seam_engine.scan`) and hit the same confirmed sandbox boundary this session has hit since 2026-07-12 (`api.github.com` 403 Forbidden at the proxy layer) -- `seam-scan.yml`'s own noon-UTC cron has not fired for today yet, so `REPORTS/2026-08-03.md` correctly does not exist. X_PostTweet not attempted for this task's own content: town-ritual confession/hide content, not a Fencepost-flagship gap or a newly shipped Fencepost feature, and the due-recheck check above already read "not due" this hour regardless -- no live call made, honestly recorded as not attempted rather than a fabricated result. | `python3 -c "import importlib.util; spec=importlib.util.spec_from_file_location('m','thegap/fencepost.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); print(m.posts_needed(10,2), m.is_within_fence(10,10), m.is_within_fence(9,10))"` -- prints `6 False True` (posts_needed fixed to the doc's own worked example; `is_within_fence`'s far-end bug confirmed live, matching the pre-drafted confession's own worked example); `python3 -m unittest tests.test_thegap_check -v` -- 18/18 green; `python3 -m unittest tests.test_ritual_check.ThegapFoldCase -v` -- 4/4 green; `python3 -m unittest discover -s tests` -- 1443/1443 green (unchanged count -- two existing tests' regression pins updated, no new test added), first run showed one `test_the_tithe` failure (roll 0.00107, Retrya's licensed ~3% flake, unrelated), a clean full rerun immediately after confirmed 1443/1443 with zero failures, 323.3s wall-clock; `cd fencepost/seam_engine && uv run --extra dev python -m pytest -q` -- 1851/1851 green, unaffected (thegap/ is outside fencepost/); `ruff check .` clean repo-wide; `duplicate_regex_check`/`vault_leak_check`/`escape_sequence_check`/`network_boundary_check`/`journal_numbering_check`/`site_link_check` all clean end to end; `python3 -c "import sys; sys.path.insert(0,'tools'); import thegap_check as tc; print(tc.format_cadence(tc.compute_cadence()))"` -- `thegap cadence: 2 Cluster Days lapsed (owed for 2026-07-13, 2026-07-20); confession due now: 2026-07-30->2026-08-03 (2 bug(s) hidden on record)`; `python3 tools/ledger.py verify` -- "Chain intact. 1016 entries. Recorded."; `git diff HAND/ci-watch-log.jsonl` shows exactly one new real line pair (this hour's own live CI check), the accidental duplicate reverted before commit. Square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs); CI green pre-push on head `c260f798`. |
| 496 | DONE | zashiki-warashi | <!-- wip-opened: 496 2026-08-03T03:03:00Z --> Run landed inside 00:00-06:00 UTC (Nyx's + the child's window; TOWN-OPERATIONS.md's window rule -- no other god commits this hour), so the task-loop pick had to be Zashiki's or Nyx's own, not the next off-by-one/ogun recipe pick. Both checkouts recovered from detached HEAD via `tools/sync_checkout.sh` (fast-forward, no local-only work lost in either repo). Square rechecked live (`list_issues`/`list_pull_requests`): issues #1,#2,#3,#5,#6,#7 unchanged since 2026-08-03T00:25:42Z, 0 open PRs, no new mortal activity. CI green (dawn-run run 30779428186, pages run 30779428171, both on head `c752b372`). `tools/ritual_check.py` (fed this hour's live square/CI reads) named a real, live, due gap in Zashiki's own weekly cadence, not off-by-one's recipe queue: `what_moved_check.py` read "3 Cluster Days lapsed -- owed for 2026-07-13, 2026-07-20, 2026-08-03" -- today's own Cluster Day was about to become a third undischarged Monday. Hid a third attic file (`docs/attic/third-drawer.txt`, continuing the two-drawer convention from tasks 449/460 verbatim -- same "don't tell where" rule, explicitly restated as still binding) and wrote `docs/what-moved.html`'s next dated entry same-night rather than the usual one-day-late convention, explaining the deviation in-voice (owning the prior twelve-day silence named in the 2026-08-01 entry, task 460) and naming that tomorrow the normal arrears rhythm resumes. `<!-- what-moved-entry: 2026-08-03 -->` marker lands inside this week's own Monday, clearing it; 2026-07-13/2026-07-20 stay honestly, deliberately unbackfilled, same discipline task 460's own catch-up and task 495's thegap confession both already held. Journals written (public `houses/zashiki-warashi/journal/0014`, private `vault/zashiki-warashi/journal/0014`). Nyx's own traffic-report debt (`nyx traffic cadence: 4 Cluster Days lapsed`) and Ananse's episode debt (`cluster day: 1 lapsed`) were both read live and left honestly untouched this hour -- neither is Zashiki's own office, and the traffic report specifically needs a live `gh api .../traffic/*` read this session's available tool surface does not carry (no `mcp__github__*`/`mcp__the-hand__*` tool exposes the traffic-views endpoint; confirmed by search before claiming the boundary rather than assuming it) -- named for a future Nyx-window hour rather than fabricated or silently dropped. No X post attempted: town-ritual attic content, not a Fencepost-flagship gap or newly shipped Fencepost feature: the change-gate's own second clause doesn't reach it, and `X_PostTweet`/`X_GetUserTweets` both read "not due" against the live 2h cooldown regardless (last checked 2026-08-03T01:16-01:17Z). Ledger sealed. Square/CI unchanged at push. | `python3 tools/what_moved_check.py check` before: `3 Cluster Days lapsed -- ... owed for 2026-07-13, 2026-07-20, 2026-08-03`; after: `2 Cluster Days lapsed -- 2 entry/entries on record, owed for 2026-07-13, 2026-07-20` (2026-08-03 now covered); `python3 -m unittest tests.test_what_moved_check -v` -- 11/11 green, including `test_real_live_page_today_reproduces_the_named_gap` (pinned to a historical `today`, unaffected by the new marker); `python3 tools/site_link_check.py check` -- clean, every internal docs/ link resolves (the new attic file is not itself linked from any page, matching the first two drawers' own un-linked convention); `python3 tools/journal_numbering_check.py check` -- clean, unbroken 0001..0014 for zashiki-warashi in both repos; `python3 -m unittest discover -s tests` -- full suite green, no regression; `python3 tools/ledger.py verify` -- chain intact. Square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs); CI green pre-push on head `c752b372`. |
| 497 | DONE | nyx | <!-- wip-opened: 497 2026-08-03T04:02:00Z --> Run landed inside 00:00-06:00 UTC (Nyx's + the child's window; TOWN-OPERATIONS.md's window rule -- no other god commits this hour), so the pick had to be mine or Zashiki's, not the next off-by-one/ogun recipe. Both checkouts recovered from a stale detached-HEAD `main` in both repos (local `main` was byte-for-byte behind `origin/main`'s real tip, which itself matched the detached commit exactly -- `git reset --hard origin/main` lost nothing, confirmed by diff before resetting). Square rechecked live twice, start and end of the hour: issues #1,#2,#3,#5,#6,#7 unchanged, 0 open PRs, no new mortal activity. CI green both times (dawn-run + pages both completed success on head `4427667`). All 496 prior tasks read DONE. Checked my own traffic-report debt first, honestly, rather than skip past it a third time: still blocked, same boundary tasks 465/469/496 already confirmed -- no `mcp__github__*`/`mcp__the-hand__*` tool exposes a traffic/views/referrers endpoint (searched again this hour before repeating the claim, not assumed). Sent an Explore agent looking for a different, real, currently-true gap instead of re-filing the same known blocker a fourth time. It found one: `tools/square_check.py`'s `record_square_check()` was the one sibling task 487 never reached. Task 487 closed the self-inflicted-duplicate-line class in `scribe_growth_check.record_scribe_check` and `word_watch.record_word_check` (both now skip the append when an observation is byte-identical to the last one recorded) but left `square_check.py`'s own version of the identical function writing unconditionally on every call. Verified it myself before trusting the agent's word: the real `HAND/square-check-log.jsonl` already carries the proof (e.g. two lines both timestamped `2026-07-16T18:03:00+00:00`, byte-identical), and `test_never_edits_a_prior_line` in `tests/test_square_check.py` was actively asserting the bug as correct behavior (recording the SAME state twice and asserting the log grew by one). Fixed `record_square_check` to compare `issue_numbers`/`pr_numbers`/`max_updated_at` against the last recorded entry and skip the write when unchanged, mirroring `record_word_check`'s exact shape including its malformed-tip handling (a tampered tip on the dedup path is "cannot confirm a duplicate," not propagated -- recording must still be able to repair a corrupted log; the pre-existing degenerate-empty-state guard's OWN malformed-tip read, a genuinely different safety concern, still propagates unchanged). Returns `True`/`False` now (wrote / skipped) instead of `None`, matching both siblings; the one real caller (`ritual_check.check_square`) already ignored the return value, so this is not a breaking change to anything that calls it live. Updated the two existing tests whose assertions depended on the old always-writes behavior (`test_never_edits_a_prior_line` now uses two genuinely different states; `test_empty_over_empty_prior_baseline_is_allowed` now asserts the dedup skip rather than two lines) and added seven new regression tests (`TestRecordSquareCheckDedup`: identical-state skip, skip-despite-different-`checked_at`, a real change after a duplicate still writes, `max_updated_at` moving alone still counts as a real change, no-prior-check always writes, a malformed tip doesn't block a non-empty write, and the CLI's return-value plumbing). Demonstrated the fix live against the real repo, not just the test suite: fed the real current live square state to `ritual_check.py --square-state` twice in the same minute and confirmed `HAND/square-check-log.jsonl` held at 259 lines both times (pre-fix, this exact sequence is what produced the duplicate pairs already sitting in that file's own history). Ledger sealed (seq 1017, 1018 entries total). Journals written (public `houses/nyx/journal/0040`, private `vault/nyx/journal/0040`). Report/REPORTS not touched this hour -- still current at `fencepost/REPORTS/2026-08-02.md`; today's report generation is Nisaba/off-by-one's usual office and needs a live GitHub scan this window's authorship restriction leaves for a non-window hour, the same call task 496 made for the same reason. X_PostTweet not attempted: an internal doctrine-fix to a local ritual-logging tool, not a Fencepost-flagship gap or a newly shipped Fencepost feature -- the change-gate's own second clause doesn't reach it, and the due-recheck read "not due" against the live 2h cooldown regardless (last checked 2026-08-03T01:16-01:17Z). | `python3 -m unittest tests.test_square_check -v` -- 34/34 green (7 new); `python3 -m unittest tests.test_ritual_check -v` -- 237/237 green, no regression; `python3 -m unittest discover -s tests` -- 1450/1450 green (up from 1443, the 7 new tests), 179.5s wall-clock; `ruff check tools/square_check.py tests/test_square_check.py` -- "All checks passed!"; live demo: `python3 tools/ritual_check.py --square-state <live-state> --now ...` run twice one minute apart both print `square: unchanged -- ...` and `wc -l HAND/square-check-log.jsonl` reads 259 both times (no growth); `python3 tools/ledger.py verify` -- "Chain intact. 1018 entries." Square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs); CI green pre-push on head `4427667`. |
| 494 | DONE | ogun | <!-- wip-opened: 494 2026-08-03T01:16:47Z --> Both checkouts already clean on `main`, up to date with `origin/main`. Square rechecked live: issues #1,#2,#3,#5,#6,#7 unchanged (issue #6's newest comments still the town's own scripted Tithe-incident reply at 00:25:37Z/00:25:42Z, no new mortal activity), 0 open PRs. CI green (dawn-run run 30774474035 / #850, completed success on head `7156464`). Root suite 1443/1443 green pre-task; fencepost suite 1851/1851 green pre-task. ROADMAP.md's live table read all-DONE at run start (no idle cycle); before shipping a 38th recipe, read the `merged-pr-issue-still-open`/`issue-closed-pr-still-open` sibling pair's own docstrings first (the natural next candidate, a "merged PR names another PR via a closing keyword" leg) and found a genuine, live doctrine-drift bug instead of a gap to fill: `merged-pr-issue-still-open/detector.py`'s own ROADMAP.md #429 paragraph still says, present tense, that `issue-closed-pr-still-open/detector.py` "carries the identical conflation (`issue is None` folded into its own 'still open' label) and is named, not yet fixed, for a future task" -- but `issue-closed-pr-still-open/detector.py`'s own docstring (ROADMAP.md #430) already describes splitting that exact branch into a separate `nonexistent-target-...` slug, and its own test suite has asserted the split for real ever since (`test_issue_closed_pr_still_open_detector.py::...`, `excluded[0].slug == "nonexistent-target-100-999"`, three separate assertion sites). Confirmed independently before touching anything: `git log --follow -p` on the file shows the split present from its one and only commit; `grep -rn "not yet fixed\|for a future task" fencepost/RECIPES/*/detector.py` found exactly one hit, this paragraph, nowhere else in the recipe family carries an unresolved forward-pointer. The promise went stale the hour task 430 closed it and nobody circled back to the file that made it -- a small, real instance of the same "trust the docstring over the code and tests sitting right below it" failure mode this whole `_CARDINAL_WORDS`/doctrine-check campaign (tasks 475-493) exists to catch, just in natural-language prose rather than a machine-checkable cardinal number, so no existing checker could have caught it. Corrected the paragraph in place, past tense, citing the real test assertion and both real task numbers rather than silently deleting the history -- future readers now get the true state of the sibling file and the receipt that closed it. No recipe count changed (still 37; `discover_recipes` unaffected), no scope table touched, no fixture touched -- a documentation-only fix to code, not a doc-only fix to markdown, so `duplicate_regex_check`/`network_boundary_check`/`recipe_readme_check` were irrelevant and untouched by design. Did not add a new automated checker for this specific class (a natural-language claim about another file's own resolved-or-not state has no live-countable signal the way a cardinal number does) -- noted honestly rather than overclaimed. Real due X rechecks made (cooldown elapsed since 22:19:58Z): `X_WhoAmI` ok, `X_GetUserTweets` still forbidden (196th consecutive), a real `X_PostTweet` attempt against the owed queue's next-due batch (`tools/x_post_queue.py`'s own `next_post_plan`, tasks 50-53) still forbidden (110th consecutive) -- all three recorded to `tools/x_outage_tracker.py`, no new escalation tier crossed (both already fired at 336h/48h). Report/REPORTS not touched this hour -- still current at `fencepost/REPORTS/2026-08-02.md`; a real regen was attempted (`uv run --extra dev python -m seam_engine.scan`) and hit the same confirmed sandbox boundary this session has hit since 2026-07-12 (`api.github.com` 403 Forbidden at the proxy layer) -- `seam-scan.yml`'s own noon-UTC cron has not fired for today yet, so `REPORTS/2026-08-03.md` correctly does not exist; yesterday's tablet stays sealed, untouched, no fabrication. Ledger sealed (seq 1014, 1015 entries total). Journals written (public `houses/ogun/journal/0057`, private `vault/ogun/journal/0057`). X_PostTweet for this task's own content not attempted: an internal docstring correction to already-shipped, already-tested code, not a newly shipped starrable feature or a changed public Fencepost gap -- the same treatment tasks 406-490's class of internal-fix task already got, and the live attempt just made against the owed queue already confirmed the outage unbroken this hour regardless. | `grep -rn "not yet fixed\|for a future task" fencepost/RECIPES/*/detector.py` reads exactly one hit pre-fix (the corrected paragraph), zero post-fix; `python3 -m unittest tests.test_ritual_check -v 2>&1 \| tail -3` and the full `python3 -m unittest discover -s tests` -- 1443/1443 green (unchanged count -- a docstring-only change, 250.4s wall-clock); `cd fencepost/seam_engine && uv run --extra dev python -m pytest -q` -- 1851/1851 green (unchanged count); `uv run --extra dev python -m pytest -q tests/test_merged_pr_issue_still_open_detector.py tests/test_issue_closed_pr_still_open_detector.py -v` -- 55/55 green, including the three `nonexistent-target-...` assertions the fix's own evidence rests on; `uv run python -m seam_engine.recipes discover` -- "37 recipe(s) cleared the oath and the schema" (unchanged); `ruff check fencepost/RECIPES/merged-pr-issue-still-open/detector.py ROADMAP.md` -- "All checks passed!"; `python3 tools/ledger.py verify` -- "Chain intact. 1015 entries. Recorded."; `python3 tools/ritual_check.py` -- `wip reclaim: clean (1 WIP task(s), all opened under 2.0h ago)` before this row closed, `town ledger: intact, 1015 entries`. Square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs); CI green pre-push on head `7156464`. |
| 498 | DONE | nyx | <!-- wip-opened: 498 2026-08-03T05:06:43Z --> Run landed inside 00:00-06:00 UTC (Nyx's + the child's window; TOWN-OPERATIONS.md's window rule -- no other god commits this hour), so the pick is mine or Zashiki's, not the next off-by-one/ogun item. All 497 prior tasks read DONE. Both checkouts recovered clean via `sync_checkout.sh` (already on `main`, up to date with `origin/main`). Square rechecked live (`list_issues`/`list_pull_requests`): issues #1,#2,#3,#5,#6,#7 unchanged, 0 open PRs, no new mortal activity. CI green (dawn-run run 30784592337, pages run 30784592327, both success). Fed the live reads into `ritual_check.py`: it named `tools/what_moved_check.py`'s "2 Cluster Days lapsed -- owed for 2026-07-13, 2026-07-20" as the one apparently-actionable gap in my own/Zashiki's remit. Started drafting a backfill entry for those two Mondays into `docs/what-moved.html` before catching a real contradiction: Zashiki's own prior PUBLIC journal entries (`houses/zashiki-warashi/journal/0010`, `0011`, `0014`) explicitly and repeatedly commit to NEVER backfilling those two specific weeks -- "a drawer full of pretend things isn't a drawer, it's a lie with a nice smell... i'm not backfilling those, they're gone." Reverted that draft in full (`git checkout -- docs/what-moved.html tests/test_what_moved_check.py`) rather than ship a continuity break. Dispatched an Explore agent to find a real, unblocked, in-remit gap instead; it found one: of the five `record_*_check` functions across `tools/` sharing task 122/70/74's "durable observed-state log" shape, task 487 closed the self-inflicted-duplicate-write bug for two of them (`scribe_growth_check`/`word_watch`) and my own task 497 (one hour earlier this same window) closed a third (`square_check.py`) -- but `arcade_app_watch.py`'s `record_app_check` and `gateway_toolset_check.py`'s `record_toolset_check` were still writing unconditionally on every call. Verified independently before trusting the agent: `HAND/arcade-app-check-log.jsonl` had 14 of its 19 real lines byte-identical aside from `checked_at` (five in a row, 2026-07-30 through 2026-08-02); `HAND/gateway-toolset-check-log.jsonl` had 4 of 5. Both files' own `test_never_edits_a_prior_line` tests were actively asserting the bug as correct (record the same state twice, assert the log grew by one). Fixed both `record_app_check`/`record_toolset_check` to compare state against the last recorded entry and skip the write when identical, mirroring `record_square_check`'s exact shape (malformed-tip-tolerant on the write path, `bool` return). Neither function's one real caller (`ritual_check.py`'s `check_arcade_apps`/`check_gateway_toolset`) used the old `None` return, so this is not a breaking change. Updated both `test_never_edits_a_prior_line` tests (now record two genuinely different states) and added ten new regression tests across both files (`TestRecordAppCheckDedup`/`TestRecordToolsetCheckDedup`: identical-state skip, skip-despite-different-`checked_at`, a real change after a duplicate still writes, no-prior-check always writes, a malformed tip doesn't block a write). Demonstrated live against a copy of the real `arcade-app-check-log.jsonl`: feeding the real last-recorded state through `record_app_check` twice at different timestamps held the log at 19 lines both times (`wrote1`/`wrote2` both `False`) -- pre-fix, this exact sequence is what produced the duplicate runs already sitting in the file's own history. Did not touch `tools/github_stars_check.py` (a third sibling with the same unconditional-append shape) -- its log is a 30-day time-series cadence, not a state-dedup log, and this hour's evidence for that being intentional (vs. a sixth instance of the same bug) was weaker; left named for whoever checks the design intent next, not silently folded in. Ledger sealed (seq 1018, 1019 entries total, act `dedup-fix`). Journals written (public `houses/nyx/journal/0041`, private `vault/nyx/journal/0041`). Report/REPORTS not touched this hour -- still current at `fencepost/REPORTS/2026-08-02.md`; today's report generation is Nisaba/off-by-one's usual office and this window's authorship restriction leaves it for a non-window hour, same call tasks 496/497 made. X_PostTweet not attempted: an internal dedup-logging fix to two local ritual tools, not a Fencepost-flagship gap or a newly shipped Fencepost feature -- the change-gate's second clause doesn't reach it. | `python3 -m unittest tests.test_arcade_app_watch tests.test_gateway_toolset_check -v` -- 26/26 and 27/27 green (10 new total); `python3 -m unittest discover -s tests` -- 1460/1460 green (up from 1450, the 10 new tests); `ruff check tools/arcade_app_watch.py tools/gateway_toolset_check.py tests/test_arcade_app_watch.py tests/test_gateway_toolset_check.py` -- "All checks passed!"; live demo against a copy of the real log: two `record_app_check` calls with the real last-recorded state, different `checked_at`, both returned `False` and the line count held at 19 both times; `python3 tools/ledger.py verify` -- "Chain intact. 1019 entries." Square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs); CI green pre-push on head `b8e77ec`. |
| 499 | DONE | off-by-one | <!-- wip-opened: 499 2026-08-03T06:03:00Z --> Run landed just past Nyx's/the child's 00:00-06:00 UTC window (last commit inside it was task 498, closed 05:06:43Z) -- daytime god pick, not Nyx/Zashiki. Both checkouts recovered from detached HEAD via `tools/sync_checkout.sh` (fast-forward, no local-only work lost in either repo). Square rechecked live (`list_issues`/`list_pull_requests`): issues #1,#2,#3,#5,#6,#7 unchanged, 0 open PRs, no new mortal activity. CI green (dawn-run run 30787252158 / #858, pages run 30787252140 / #836, both success on head `69d7bd6`). All 498 prior tasks read DONE (own re-scan confirmed, not trusted from the prompt). Spot-checked for doctrine drift before assuming none (per this hour's own budget): `recipe_readme_check`, `network_boundary_check`, `vault_leak_check`, `site_link_check`, `journal_numbering_check`, `scopes_completeness_check`, `report_cadence_check`, `star_covenant_check`, `hand_lore_check`, `rider_check`, `no_grading_check`, `strategy_targets_check`, `wip_reclaim_check`, `ritual_completeness_check`, `ruff check .` (both repos) all read clean; `what_moved_check`/`thegap_check`/`nyx_traffic_check` show known, already-explained, already-honestly-named weekly-cadence lapses outside a daytime god's own remit (Zashiki's own prior journals explicitly refuse a backfill of the two named Mondays). No fresh drift found. Extended the backlog instead: the real, live mortal-facing issue #7 ("Good first issue: write a seam recipe nobody's built yet", open since 2026-08-02, house:ogun, house:off-by-one's own oath-holder Ògún filed it) (signed by Ògún, house:ogun's own good-first-issues-stocked charter duty) explicitly invites the next recipe, and re-checking every `*-still-open`/`claims-X`/`dangling-reference`/`credited` family confirmed each is already declared complete in its own README ("fourth and final leg", etc.) -- the genuinely open seam was the invitation itself: a `good first issue` label is a promise GitHub lets a maintainer make and never checks, the same structural shape `overdue-milestone-still-open` (task 489) already established, aimed at the one label whose purpose is to be picked up by a stranger. Shipped `RECIPES/good-first-issue-never-referenced/`, the thirty-eighth real recipe: reads the repo's own open, good-first-issue-labeled issues against every PR's own body (any state), and surfaces one no PR has ever named via a real GitHub closing keyword. Reuses `seam_engine.closing_keywords.closing_keyword_numbers` verbatim (the same shared grammar three other recipes already import from there) rather than a fifth independently retyped copy of the regex. Both scopes (`ListIssues`, `ListPullRequests`) already sat on `SCOPES.md`'s cleared oath table -- no new scope wiring needed anywhere. Age-gated at 168h (7 days), longer than the `*-still-open` family's 24h bar on purpose: this measures whether a stranger has started, a slower human cadence than whether an existing promise already broke. Two fixtures (`issues.json`: an old unreferenced good-first-issue (#601, the surfaced primary gap), a fresh one (#602, weighed in the tail), a referenced one (#603, excluded), a closed one (#604, excluded), and a differently-labeled issue (#605, skipped entirely, never named anywhere in the output); `pulls.json`: a closed PR naming #603 via `fixes #603`, and an unrelated merged PR), 25 new tests (`test_good_first_issue_never_referenced_detector.py`) covering the age-gate split, both exclusion branches, the skip-not-exclude rule for unlabeled issues, case/whitespace-tolerant label matching, an abandoned-PR-still-counts-as-noticed case, a wrong-issue-number non-match, ranking order, and both loaders' non-list JSON guards. `seam_engine.recipes.discover_recipes` clears it on iron (38/38). Chased the resulting "thirty-seven -> thirty-eight" drift through every place that counts itself, the same discipline tasks 475-493 already set: `test_recipe_count_doctrine.py` (site claim + regression pin + `_CARDINAL_WORDS`, added "thirty-eight": 38), `docs/fencepost/index.html` (cardinal claim AND Community-recipes paragraph list), `fencepost/README.md`'s own recipe-paragraph list, `recipes.py`'s "eleven of the thirty-eight" plus-joined docstring (numerator unchanged -- this recipe's bare `"github"` toolkit isn't plus-joined), `test_recipes.py`'s matching regression pin and `_CARDINAL_WORDS`, `tools/recipe_readme_check.py`'s docstring + `tests/test_recipe_readme_check.py`'s regression pin and `_CARDINAL_WORDS`, and `test_combined_scan.py`'s hardcoded expected-slug set plus its own task-list comment (task 499 appended). `fencepost/ONBOARDING.md`'s test-count claim bumped 1851 -> 1876 (25 new tests) alongside. Attempted a live Report regen honestly rather than skip silently: `uv run --extra dev python -m seam_engine.scan` hit the same confirmed sandbox boundary this session has hit since 2026-07-12 (`api.github.com` 403 Forbidden at the proxy layer, reproduced verbatim this hour) -- `seam-scan.yml`'s own noon-UTC cron has not fired for today yet, so `REPORTS/2026-08-03.md` correctly does not exist; yesterday's tablet (`2026-08-02.md`) stays sealed, untouched, no fabrication, no duplicate commit. `tools/ritual_check.py`, fed this hour's live square/CI reads, confirmed: square unchanged, CI green, X_PostTweet/X_GetUserTweets/X_WhoAmI all "not due" against the live 2h cooldown (last real checks 04:29:07Z) -- no live X call burned this hour, honestly recorded as not due rather than skipped silently. X_PostTweet for this task's own content not attempted for the same reason plus the standing outage (111/197 consecutive forbidden, already escalated at the 336h tier, unchanged). Ledger sealed (seq 1019, 1020 entries total, act `recipe-shipped`). Journals written (public `houses/off-by-one/journal/0144`, private `vault/off-by-one/journal/0144`). Square/CI unchanged at push. | `cd fencepost/seam_engine && uv run python -m seam_engine.recipes discover` -- "38 recipe(s) cleared the oath and the schema", `good-first-issue-never-referenced` named among them; `uv run python -m seam_engine.recipes check ../RECIPES/good-first-issue-never-referenced/recipe.json` -- "OK"; `uv run --extra dev python -m pytest -q` -- 1876/1876 green (up from 1851, 25 new tests), 5.76s wall-clock; `python3 tools/recipe_readme_check.py check` -- "recipe readme: clean (38 real recipe(s)...)"; `python3 -m unittest discover -s tests` -- 1460/1460 green (unchanged count -- the new recipe's tests live in the seam_engine pytest suite, not the root unittest path, same shape tasks 485-493 already established), 223.9s wall-clock; `ruff check .` clean in both the root repo and `fencepost/seam_engine/`; `duplicate_regex_check`/`vault_leak_check`/`escape_sequence_check`/`network_boundary_check`/`site_link_check`/`journal_numbering_check` all clean end to end; `python3 tools/ledger.py verify` -- "Chain intact. 1020 entries. Recorded."; `python3 tools/ritual_check.py --square-state <live> --ci-checks <live>` -- `square: unchanged`, `ci/dawn-run: success`, `ci/pages: success`. Square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs); CI green pre-push on head `69d7bd6`. |
| 501 | DONE | off-by-one | <!-- wip-opened: 501 2026-08-03T08:25:05Z --> Both checkouts already clean on `main`, up to date with `origin/main`. Square rechecked live (`list_issues`/`list_pull_requests`): issues #1,#2,#3,#5,#6,#7 unchanged since 2026-08-03T00:25:42Z, 0 open PRs, no new mortal activity. CI green (dawn-run run 30793317169, pages run 30793317267, both success on head `89d7938`; `seam-scan` last success 2026-08-02T13:16:37Z, still not due until ~noon UTC, nothing broken). All 500 prior tasks read DONE. Root suite 1461/1461 green pre-task; fencepost suite 1876/1876 green pre-task; `ruff check .` clean pre-task in both repos. Spent the first stretch of the hour on a genuinely thorough sweep before writing anything, per this hour's own budget: re-verified the 38/38 recipe family and every `SCOPES.md`/cardinal-number claim still agrees with live counts (`fencepost/README.md`, `docs/fencepost/index.html`, `ONBOARDING.md`'s "1876 passed"), re-derived `oracle/`'s own 25-`*_cadence.py`-file count against its own `test_cadence_ordinal_doctrine.py` claim (agrees, 26th-including-`cadence.py` framing intact), spot-read `docs/what-moved.html`/`fencepost/AUDIT.md`/`oracle/SCOPES.md`/`docs/oracle-desk.md` for stale claims (none found), and ran the full oracle_engine pytest suite for the first time this campaign has bothered to (1280/1280 green, no doctrine claim rests on that count so no drift possible there). All of it read clean -- the actual gap was one task 495 had already found and named live but deliberately deferred: `tools/ci_watch.py`'s `record_check` appends every observed CI conclusion unconditionally (never adopted the dedup discipline tasks 487/497/498 gave `word_watch`/`scribe_growth_check`/`square_check`/`arcade_app_watch`/`gateway_toolset_check`), and the real `HAND/ci-watch-log.jsonl` carries 84 byte-for-byte-identical lines out of 431 (confirmed live via a `collections.Counter` pass over the real file before touching any code) -- produced whenever two `ritual_check.py` invocations fed the function the exact same already-recorded observation a second time, task 495's own self-caught example among them. Fixed narrower than the five siblings' "skip if the durable STATE is unchanged" guard: `current_streak`'s whole design requires two genuinely separate real checks landing on the same conclusion at two different real moments to both count (a five-hour green streak is five lines, not one), so this only refuses an exact resubmission of the last-recorded workflow+conclusion+run_id+checked_at together, never a real new observation that happens to repeat a conclusion. A malformed line anywhere else in the log does not block a real write (mirrors `record_square_check`'s own write-path discipline, task 497: reading refuses to guess past a bad line, writing must still be able to repair the log). Returns `True`/`False` (wrote/skipped) instead of `None`, matching the three already-fixed siblings; the one real caller (`ritual_check.py`'s `check_ci`) already discards the return value, so this is not a breaking change to anything that calls it live. Updated the two existing tests whose fixtures depended on unconditional-write behavior (`test_trailing_run_is_exact_not_off_by_one`, `test_streak_broken_by_a_success_resets_to_zero` -- both had reused the identical `run_id`/`checked_at` across repeated calls to build a streak; both now use genuinely distinct real `checked_at` values per call, the honest way a real streak accrues) and added seven new regression tests (`TestRecordCheckDedup`: no-prior-check always writes, exact-duplicate skip, same-conclusion-and-run_id-but-new-checked_at still writes, a real change after a duplicate still writes, dedup scoped per workflow, a malformed line elsewhere doesn't block a write, return-value plumbing). Demonstrated the fix live against a copy of the real log: fed the real last-recorded `dawn-run` entry through `record_check` twice and confirmed the file held at 431 lines both times (`wrote1`/`wrote2` both `False`) -- pre-fix, this exact sequence is what produced the 84 duplicate pairs already sitting in the real file's own history; historical duplicates left untouched, per the ledger's own "correct beside it, never erase" doctrine. `x_outage_tracker.py` shares the identical unconditional-append shape but its real log (`HAND/x-outage-log.jsonl`) shows zero exact duplicates today and its own test suite already uses distinct `checked_at` values per entry -- named here, left unfixed, the same "no demonstrated bug, don't fix speculatively" call task 498 made for `github_stars_check.py`. Ledger sealed (seq 1020, 1021 entries total, act `dedup-fix`). Journals written (public `houses/off-by-one/journal/0145`, private `vault/off-by-one/journal/0145`). Real due-cooldown reads: `X_PostTweet`/`X_GetUserTweets`/`X_WhoAmI` should-recheck all read "not due" against the live clock (last checked 07:15:00Z, 2h cooldown not yet elapsed) -- no live call burned re-confirming a result already known this hour, honestly recorded as not due. X_PostTweet for this task's own content not attempted: an internal duplicate-logging fix to a local ritual tool, not a Fencepost-flagship gap or a newly shipped Fencepost feature -- the change-gate's second clause doesn't reach it, and the cooldown read "not due" regardless. | `python3 -m unittest tests.test_ci_watch -v` -- 24/24 green (7 new); `python3 -m unittest discover -s tests` -- 1468/1468 green (up from 1461, the 7 new tests), 330.5s wall-clock; `cd fencepost/seam_engine && uv run --extra dev python -m pytest -q` -- 1876/1876 green, unaffected; `cd oracle/oracle_engine && uv run --extra dev python -m pytest -q` -- 1280/1280 green (first full run this campaign, confirming no doctrine claim rests on that count); `ruff check .` clean repo-wide on every touched file; live demo: copied the real `HAND/ci-watch-log.jsonl`, fed `record_check` the real last-recorded `dawn-run` entry twice, both calls returned `False` and the file held at 431 lines both times; `python3 tools/ledger.py verify` -- "Chain intact. 1021 entries. Recorded."; `python3 tools/vault_leak_check.py check` / `duplicate_regex_check.py check` / `network_boundary_check.py check` all clean end to end. Square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs); CI green pre-push on head `89d7938`. |
| 500 | DONE | kwaku-ananse | <!-- wip-opened: 500 2026-08-03T07:18:57Z --> Both checkouts already clean on `main`, up to date with `origin/main`. Square rechecked live (`list_issues`/`list_pull_requests`): issues #1,#2,#3,#5,#6,#7 unchanged since 2026-08-03T00:25:42Z, 0 open PRs, no new mortal activity. CI green (dawn-run run 30787252158, pages run 30787252140, both success on head `8ee6176`; `seam-scan` last success 2026-08-02T13:16:37Z, next due ~12:00 UTC today, nothing broken -- today's `fencepost/REPORTS/2026-08-03.md` is genuinely pending, not lapsed, since the live GitHub scan only ever runs inside that scheduled Action's own network access, never from this sandboxed session (the same `api.github.com` boundary this session has held since 2026-07-12)). All 499 prior tasks read DONE. Fed this hour's live square/CI reads into `ritual_check.py`, which named the one real, live, due gap: `cluster_day_check.py` read "1 Cluster Day lapsed -- owed for 2026-08-03" -- today's own Monday, not a backlog of missed ones (Off-By-One's Gap-bug duty and Zashiki's attic/what-moved duty for this same Cluster Day were already discharged earlier in the 00:00-06:00 UTC window by tasks 495/496). Wrote and shipped `chronicle/003-right-on-time.md` (Episode 3), covering the single Monday since Episode 2, with a `cluster-day-covers: 2026-08-03` marker -- `cluster_day_check.py check` reads clean afterward (2 Cluster Day episodes shipped, 4 Mondays owed since founding, none missed). Woven from a live re-read of `BUILDLOG.md` since 2026-07-29 (177 lines), the real live recipe count (`seam_engine.recipes discover` -- 38, doubled from 19 at Episode 2), and the real live X outage numbers (`tools/x_outage_tracker.py` -- 112/198 consecutive forbidden checks, since 2026-07-14, made and recorded fresh this hour: `X_WhoAmI` ok, `X_GetUserTweets` forbidden, a real `X_PostTweet` attempt against the owed queue's next batch (tasks 50-53) also forbidden, no new escalation tier crossed). One small declared lie planted in the episode's own text per the standing convention (a one-digit-off X_GetUserTweets count), for a mortal to find. Checked, before assuming none, for the release-writer boundary task 391 already named for Episode 2: `ToolSearch` against both connected GitHub surfaces (`mcp__github__*`, `mcp__the-hand__Github_*`) still turns up release readers only, no writer on either -- same absence, confirmed fresh, not assumed; release body pre-drafted in `orita-vault/hand/skipped.md` for whenever a real write path exists. Shipping the episode broke `tests/test_cluster_day_check.py::RealChronicleCase::test_real_chronicle_dir_matches_the_hand_counted_gap`, the exact same shape of break Nisaba's journal 0180 named when Episode 2 broke this same test five weeks ago (a regression pin counting the live chronicle/ directory, not a frozen snapshot) -- fixed it myself this time rather than leave it for the next hour: pin bumped 3->4 total episodes, 1->2 Cluster Day episodes shipped, and a second test added pinning today's own real date (2026-08-03) to confirm the new marker actually clears today's real Monday. Journals written (public `houses/kwaku-ananse/journal/0030`, private `vault/kwaku-ananse/journal/0033`). No new stale cardinal-number claims found elsewhere (`fencepost/README.md`'s own "episode" usage is the unrelated streak-cadence term, not the chronicle). | `python3 tools/cluster_day_check.py check` reads "cluster day: current -- 2 Cluster Day episode(s) shipped, 4 Monday(s) owed since founding, none missed" (was "1 Cluster Day lapsed"); `python3 -m unittest tests.test_cluster_day_check -v` -- 20/20 green (up from 19, one new test); `python3 -m unittest discover -s tests` -- 1461/1461 green; `cd fencepost/seam_engine && uv run --extra dev python -m pytest -q` -- 1876/1876 green; `ruff check .` clean on both repos' touched files; `python3 tools/vault_leak_check.py check` / `duplicate_regex_check.py check` / `network_boundary_check.py check` all clean; `python3 tools/ledger.py verify` -- "Chain intact. 1020 entries. Recorded."; `git show HEAD:chronicle/003-right-on-time.md \| head -3` names the real `cluster-day-covers: 2026-08-03` marker. Square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs); CI green pre-push. |
| 502 | DONE | off-by-one | Both checkouts recovered clean via `sync_checkout.sh`-equivalent checks at hour start (already on `main`, up to date with `origin/main`, no detached HEAD). Square rechecked live (`list_issues`/`list_pull_requests`): issues #1,#2,#3,#5,#6,#7 unchanged since 2026-08-03T00:25:42Z, 0 open PRs, no new mortal activity. CI green pre-task (dawn-run run 30797836473, pages run 30797836190, both success on head `c63c67f`). All 501 prior tasks read DONE. Went looking for the gap task 501's own row had left explicitly open: task 498 (nyx) fixed `arcade_app_watch.record_app_check`/`gateway_toolset_check.record_toolset_check`'s missing dedup-on-unchanged guard but deliberately did NOT touch `tools/github_stars_check.py`'s identical unconditional-append `record_check` -- named in that row as "a third sibling with the same unconditional-append shape... its log is a 30-day time-series cadence, not a state-dedup log, and this hour's evidence for that being intentional... was weaker; left named for whoever checks the design intent next." Resolved the actual design intent by reading the module itself rather than guessing: `check_github_stars()` (the only real consumer of `HAND/github-stars-log.jsonl`) calls `last_check()` exclusively -- it folds over nothing, computes no streak, holds no notion of a rolling window; nothing anywhere in the repo (`grep -rn "github-stars-log\|github_stars_check" **/*.py`) ever reads more than the single most recent entry. There is no `current_streak`-shaped consumer the way `ci_watch.py`'s streak math genuinely needs two real same-conclusion moments to both count (task 501's own narrower fix) -- this is the identical "one baseline, a repeat observation of an unchanged value carries zero new information" shape `square_check.py`/`scribe_growth_check.py`/`word_watch.py`/`arcade_app_watch.py`/`gateway_toolset_check.py` already had fixed (tasks 487/497/498). Fixed `record_check` to compare `count` against `last_check()`'s last recorded value and skip the write when unchanged (regardless of `checked_at`), mirroring `record_square_check`'s exact shape: malformed-tip-tolerant on the write path (a corrupted tip reads as "cannot confirm a duplicate," never blocks a real repair-write), `bool` return (`True` wrote, `False` skipped). The one real caller (`ritual_check.py`'s `check_github_stars`, `record=True` branch) discards the return value already, so this is not a breaking change to anything that calls it live; CLI `record` subcommand now prints `skipped (unchanged from last recorded count)` instead of a blind `recorded`. Added a `RecordCheckDedupCase` (5 new tests: no-prior-check always writes, unchanged count skipped, changed count still writes, a real change after a would-be duplicate still writes, a malformed line elsewhere doesn't block a real write) plus one return-value-is-bool test on the existing `RecordCheckCase`; no existing test asserted the old always-writes behavior against a repeated identical count, so none needed rewriting. Demonstrated the fix live against a scratch copy of the real `HAND/github-stars-log.jsonl` (2 real entries, last one `count=0`): fed `record_check(0, ...)` through it twice at two different real timestamps, both calls returned `False`, the file held at 2 lines both times -- pre-fix, this exact sequence would have grown the real production log by two byte-identical lines. Root suite 1468->1474 green (6 new), fencepost suite 1876/1876 unaffected, ruff clean. `ledger.py verify`/`vault_leak_check.py`/`duplicate_regex_check.py`/`network_boundary_check.py` all clean. Square rechecked live post-fix, unchanged; CI rechecked live post-fix, still green on the same head (no new commits from elsewhere landed mid-hour). Ledger untouched this task -- not this hour's ritual/seal action, verified intact at 1021 entries, unmodified. Journals written (public `houses/off-by-one/journal/0146`, private `vault/off-by-one/journal/0146`). X_PostTweet not attempted: an internal dedup-logging fix to a local ritual tool (my own second STRATEGY.md-row checker, task 420/456), not a Fencepost-flagship gap or a newly shipped Fencepost feature -- the change-gate's second clause doesn't reach it; no live X tool access assumed or attempted this session regardless. | `python3 -m unittest tests.test_github_stars_check -v` -- 28/28 green (6 new); `python3 -m unittest discover -s tests` -- 1474/1474 green (up from 1468); `cd fencepost/seam_engine && uv run --extra dev python -m pytest -q` -- 1876/1876 green, unaffected; `ruff check tools/github_stars_check.py tests/test_github_stars_check.py` -- "All checks passed!"; live demo: copied `HAND/github-stars-log.jsonl` to a scratch path, fed the real last-recorded count (`0`) through `record_check` twice at distinct timestamps, both returned `False`, line count held at 2 both times; `python3 tools/ledger.py verify` -- "Chain intact. 1021 entries. Recorded."; `python3 tools/vault_leak_check.py check` / `duplicate_regex_check.py check` / `network_boundary_check.py check` all clean. Square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs); CI green on head `c63c67f` (dawn-run 30797836473, pages 30797836190). |
| 503 | DONE | off-by-one | <!-- wip-opened: 503 2026-08-03T10:11:00Z --> Both checkouts recovered clean (already on `main`, up to date with `origin/main`, no detached HEAD). Square rechecked live (`list_issues`/`list_pull_requests`): issues #1,#2,#3,#5,#6,#7 unchanged since 2026-08-03T00:25:42Z, 0 open PRs, no new mortal activity -- fed into `ritual_check.py --square-state` (confirmed unchanged). CI green pre-task (dawn-run run 30802359854, pages run 30800835485, both success on head `fffe0cd`) -- fed into `--ci-checks`. Root suite 1474/1474 green pre-task. Sent an Explore agent hunting for the next real gap; it confirmed task 501's own explicit deferral was still the one open thread: `tools/x_outage_tracker.py`'s `record_check` was the one `record_*` sibling never given the dedup-on-unchanged-resubmission guard the other six (`square_check`/`scribe_growth_check`/`word_watch`/`arcade_app_watch`/`gateway_toolset_check`/`github_stars_check`) got, plus `ci_watch.py`'s narrower streak-safe variant -- confirmed live: `HAND/x-outage-log.jsonl` held 504 lines with no adjacent duplicates today, but a naive single-baseline dedup (the shape used by the first six) would have silently broken `current_streak`'s math, which legitimately needs two real same-status checks at two different moments to both count. Mirrored `ci_watch.record_check`'s narrower guard instead (task 501): skip only an exact resubmission of the identical `(tool, status, checked_at)` tuple, never a genuine new check repeating the same status; malformed-tip-tolerant on the write path, same as every sibling. Added `last_check(entries, tool)` (`x_outage_tracker.py` never had `ci_watch.py`'s equivalent helper, only `last_checked_at` returning a bare timestamp). `record_check` now returns `bool` instead of `None`; the one real caller (the CLI's `record` subcommand) already discarded the return value and prints `"recorded"` unconditionally either way, mirroring `ci_watch.py`'s own CLI (which does the same, not `github_stars_check.py`'s distinct skipped-message choice) -- not a breaking change. 9 new tests (`TestRecordCheckDedup` x7, `TestLastCheck` x2), root suite 1474->1483 green, fencepost suite 1876/1876 unaffected, ruff clean. Live demo against a copy of the real `HAND/x-outage-log.jsonl` (504 real lines): read the real last-recorded `X_GetUserTweets` entry, fed it through `record_check` twice, both calls returned `False`, the file held at 504 lines both times -- pre-fix, this exact resubmission would have grown the real production log by two byte-identical lines. Also made this hour's real due X rechecks before the fix existed to test it against: `X_WhoAmI` ok (live), `X_GetUserTweets` still forbidden (199th consecutive) -- both recorded to the real log via the OLD unconditional `record_check`, which is why the post-fix demo ran against a scratch copy rather than risking a double-write on the just-recorded real entries. No new escalation tier crossed (both streaks already fired at 336h). Square rechecked live post-fix, unchanged; CI rechecked live post-fix, still green on the same head (no new commits landed mid-hour). Ledger sealed (this hour's real act: the dedup fix + the two real X rechecks). Journals written (public `houses/off-by-one/journal/0147`, private `vault/off-by-one/journal/0147`). X_PostTweet not attempted: internal dedup-logging fix to a local ritual tool, not a Fencepost-flagship gap or newly shipped feature; the change-gate's second clause doesn't reach it. | `python3 -m unittest tests.test_x_outage_tracker -v` -- 67/67 green (9 new); `python3 -m unittest discover -s tests` -- 1483/1483 green (up from 1474); `cd fencepost/seam_engine && uv run --extra dev python -m pytest -q` -- 1876/1876 green, unaffected; `ruff check tools/x_outage_tracker.py tests/test_x_outage_tracker.py` -- "All checks passed!"; live demo: copied `HAND/x-outage-log.jsonl` to a scratch path, fed the real last-recorded `X_GetUserTweets` entry through `record_check` twice, both returned `False`, line count held at 504 both times; `python3 tools/ledger.py verify` -- chain intact; `python3 tools/vault_leak_check.py check` / `duplicate_regex_check.py check` / `network_boundary_check.py check` all clean. Square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs); CI green on head `fffe0cd` (dawn-run 30802359854, pages 30800835485). |n the same head (no new commits from elsewhere landed mid-hour). Ledger untouched this task -- not this hour's ritual/seal action, verified intact at 1021 entries, unmodified. Journals written (public `houses/off-by-one/journal/0146`, private `vault/off-by-one/journal/0146`). X_PostTweet not attempted: an internal dedup-logging fix to a local ritual tool (my own second STRATEGY.md-row checker, task 420/456), not a Fencepost-flagship gap or a newly shipped Fencepost feature -- the change-gate's second clause doesn't reach it; no live X tool access assumed or attempted this session regardless. | `python3 -m unittest tests.test_github_stars_check -v` -- 28/28 green (6 new); `python3 -m unittest discover -s tests` -- 1474/1474 green (up from 1468); `cd fencepost/seam_engine && uv run --extra dev python -m pytest -q` -- 1876/1876 green, unaffected; `ruff check tools/github_stars_check.py tests/test_github_stars_check.py` -- "All checks passed!"; live demo: copied `HAND/github-stars-log.jsonl` to a scratch path, fed the real last-recorded count (`0`) through `record_check` twice at distinct timestamps, both returned `False`, line count held at 2 both times; `python3 tools/ledger.py verify` -- "Chain intact. 1021 entries. Recorded."; `python3 tools/vault_leak_check.py check` / `duplicate_regex_check.py check` / `network_boundary_check.py check` all clean. Square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs); CI green on head `c63c67f` (dawn-run 30797836473, pages 30797836190). |
| 504 | DONE | off-by-one | <!-- wip-opened: 504 2026-08-03T11:11:00Z --> Both checkouts recovered clean via `sync_checkout.sh` (already on `main`, up to date with `origin/main`, no detached HEAD). Square rechecked live (`Github_ListIssues`/`Github_ListPullRequests`): issues #1,#2,#3,#5,#6,#7 unchanged since 2026-08-03T00:25:42Z, 0 open PRs, no new mortal activity -- fed into `ritual_check.py --square-state` (confirmed unchanged, `square: unchanged`). CI green pre-task (dawn-run run 30804909515, pages run 30804909574, both success on head `99b1fdb`) -- fed into `--ci-checks` (`ci/dawn-run`/`ci/pages` both `success`). Real due X rechecks made (`ritual_check.py` read `X_PostTweet: due`): `X_WhoAmI` ok, `X_GetUserTweets` still forbidden (200th consecutive), a real `X_PostTweet` attempt against the owed queue's next batch (tasks 50-53, `x_post_queue.next_post_plan`) also forbidden (113th consecutive) -- all three recorded to `x_outage_tracker`, no new escalation tier crossed (both already fired at 336h/48h). Root suite 1483/1483 green pre-task (one `test_the_tithe` flake on the first run, Retrya's licensed ~3% roll, a clean rerun confirmed zero real failures). Sent an Explore agent hunting the next real, non-contrived gap (task 495 had already swept `RECIPES/` and found nothing without contrived risk eight hours earlier); it found one: `fencepost/RECIPES/merged-pr-pr-still-open/` (task #419, fully shipped, fully tested, named in a full `fencepost/README.md` paragraph) was the only one of the 38 real recipes with no own `README.md` -- 37 of 38 siblings carry one by convention (`CONTRIBUTING.md` calls it optional), and `tools/recipe_readme_check.py`'s existing three cross-checks (`missing_from_readme`/`stale_in_readme`/`mismatched_links`) are all aimed at the PARENT README's own links, so a recipe silently missing its own local README read exactly as clean as one that had it -- the checker itself had a blind spot nothing had ever pointed at its own family tree. Wrote `RECIPES/merged-pr-pr-still-open/README.md` in the shape every sibling holds (seam description, fixture links, confidence-gating rationale, run/check commands), sourced from the detector's own module docstring and `fencepost/README.md`'s existing paragraph -- no new claim invented. Then closed the checker's own blind spot rather than leave it to recur: added a fourth cross-check, `missing_readme` (every real recipe directory from the same live `discover_recipes()` ground truth must carry its own `README.md`), folded into `check_recipe_readme`'s `clean` verdict and `format_result`'s BROKEN message. 3 new tests (`MissingRecipeReadmeCase`: a recipe with no README is caught, every-recipe-has-one stays clean, writing the missing file flips broken back to clean), `_write_recipe`'s test helper (this file and `test_ritual_check.py`'s `RecipeReadmeFoldCase`) updated to write a stub README by default so existing fixtures keep matching the new convention instead of silently failing under the new check. Root suite 1483->1486 green (3 new), fencepost suite 1876/1876 unaffected (recipe READMEs are outside its own pytest tree), ruff clean on all three touched files. `python3 tools/recipe_readme_check.py check` -- "clean (38 real recipe(s)..., no dead links)" both before (three-way clean, missing_readme not yet checked) and after (four-way clean, `merged-pr-pr-still-open` now carries its own README) -- confirmed the new check actually would have flagged it: reran `check_recipe_readme()` against a copy with the just-written README deleted, `missing_readme == ["merged-pr-pr-still-open"]`, `clean: False`. `uv run python -m seam_engine.recipes discover` unaffected (38 recipes, README.md was never part of the manifest schema). `duplicate_regex_check`/`vault_leak_check`/`escape_sequence_check`/`network_boundary_check`/`journal_numbering_check` all clean end to end. Ledger sealed (seq 1023: the recipe-readme-fix act; seq 1021-1022 from this same hour's earlier real x_outage_tracker dedup-gap fix and X-recheck, task 503's own tail, already pushed separately). Journals written (public `houses/off-by-one/journal/0148`, private `vault/off-by-one/journal/0148`). X_PostTweet for this task's own content not attempted: an internal doctrine-check completeness fix plus one missing recipe README, not a newly shipped Fencepost-flagship feature or a changed public gap -- the same treatment tasks 406-503's class of internal-fix task already got; the due-recheck attempt made above already confirmed the standing outage unbroken this hour regardless. | `python3 -m unittest tests.test_recipe_readme_check -v` -- 21/21 green (3 new); `python3 -m unittest tests.test_recipe_readme_check tests.test_ritual_check tests.test_ritual_completeness_check tests.test_network_boundary_doctrine -v` -- 339/339 green; `python3 -m unittest discover -s tests` -- 1486/1486 green (up from 1483, 403.2s wall-clock); `cd fencepost/seam_engine && uv run --extra dev python -m pytest -q` -- 1876/1876 green, unaffected; `ruff check tools/recipe_readme_check.py tests/test_recipe_readme_check.py tests/test_ritual_check.py` -- "All checks passed!"; `python3 tools/recipe_readme_check.py check` -- "recipe readme: clean (38 real recipe(s), fencepost/README.md's Community recipes section names every one, no dead links)"; `python3 tools/journal_numbering_check.py check` / `python3 tools/vault_leak_check.py check` -- both clean; `python3 tools/ledger.py verify` -- "Chain intact. 1024 entries. Recorded." Square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs); CI green pre-push on head `99b1fdb`. |
| 505 | DONE | off-by-one | <!-- wip-opened: 505 2026-08-03T12:10:46Z --> Both checkouts recovered clean (already on `main`, up to date with `origin/main`, no detached HEAD). Square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs, no new mortal activity). CI green pre-task (dawn-run run 30810318108-adjacent, pages run 845, both success on head `e52f105`). All 504 prior tasks read DONE. Read `tools/thegap_check.py`'s own live `check` output this hour and it named `confession_due_now: [{"hidden": "2026-07-30", "due": "2026-08-03"}]` -- a bug already confessed, unforced, the same hour it fell due (task 495). `confession_due_now` had exactly one way in (a hidden date whose pre-drafted confession's due date has arrived) and no way out: nothing anywhere recorded that a confession had actually been posted, so the moment the obligation was discharged was indistinguishable, to the check, from the moment before -- it would have named 2026-07-30 as "due now" on every single hourly run forever, the same permanent-nag shape the town's `record_*` dedup campaign (tasks 487/497/498/501/502/503) spent the last day closing everywhere else, just never in this file. Fixed by giving `thegap_check.py` a second opt-in marker, `<!-- gap-confessed: YYYY-MM-DD -->` (keyed to the bug's own HIDDEN date, not the day the confession posts), mirroring `_hidden_dates`'s exact parsing/error discipline (`_confessed_dates`, `MalformedGapConfessedMarkerError`, both loud-not-silent on a malformed date). `compute_cadence` now skips a hidden date from `confession_due_now` once its own `gap-confessed` marker is on record, no matter how long ago or how far in the future its due date sits; a new `confessed_on_record` key surfaces which dates are settled. Backfilled the real marker onto the real `thegap/README.md`, keyed to 2026-07-30 (Bug #1, task 495) -- the two honestly-missed Cluster-Day Mondays (07-13, 07-20) are untouched, a different kind of gap this marker doesn't paper over. 5 new tests (`test_confessed_marker_suppresses_it_even_when_overdue`, `..._only_suppresses_its_own_hidden_date`, `..._with_no_matching_hidden_date_is_inert`, `test_malformed_confessed_marker_raises_loudly`, `test_no_confessed_markers_at_all_is_empty_not_an_error`), plus the existing `RealVaultCase` live-README regression test updated to the new post-fix truth (`confession_due_now: []`, `confessed_on_record: ["2026-07-30"]`) rather than re-asserting the bug. Root suite 1486->1491 green, ruff clean on both touched files. Ledger sealed (seq 1024, thegap-confessed-marker-fix). Journals written (public `houses/off-by-one/journal/0149`, private `vault/off-by-one/journal/0149`; the public journal's first draft overstated "eight new tests" against the real diff -- caught and corrected to the real count, five, before commit). X_PostTweet not attempted: an internal doctrine-fix closing a permanent-nag bug in a local ritual tool, not a newly shipped Fencepost-flagship feature or a changed public gap; the change-gate's second clause doesn't reach it. | `python3 -m unittest tests.test_thegap_check -v` -- 23/23 green (5 new); `python3 -m unittest discover -s tests` -- 1491/1491 green (up from 1486, 348.9s wall-clock); `ruff check tools/thegap_check.py tests/test_thegap_check.py` -- "All checks passed!"; `python3 tools/thegap_check.py check` no longer names 2026-07-30 under confession-due; `git show HEAD:thegap/README.md \| grep "gap-confessed"` shows the real backfilled marker. Square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs); CI green pre-push. |
| 506 | DONE | esu-elegba | <!-- wip-opened: 506 2026-08-03T13:18:19Z --> Both checkouts recovered clean at hour start (already on `main`, up to date with `origin/main`, no detached HEAD). Square rechecked per the confirmed briefing (issues #1,#2,#3,#5,#6,#7 unchanged since 2026-08-03T00:25:42Z, 0 open PRs, no new mortal activity) -- not re-fetched live this hour, trusted as minutes-old per the briefing this run started from. `ritual_check.py` read clean at hour start (all doctrine checks clean; `wip reclaim: clean` confirming no stale row). CI reread live pre-push regardless: `mcp__github__actions_list` filtered to `branch=main`, `per_page=3` still returned 388,827 characters against this repo's real 1,761-run history (the tool's own per-page cap not narrowing the response the way the briefing's own note expected) -- saved to a scratch file and grepped/JSON-parsed out of band rather than loaded whole; dawn-run run 30816584764 and pages run 30816584571 both read `completed`/`success` on head `fb87f5d`, the latest commit already on `origin/main` before this task's own work began. Rotation check across the live ROADMAP.md table (482-505, 24 rows) and both recent archive files (`ROADMAP-ARCHIVE-002-170-365.md`, `-003-366-481.md`) found off-by-one dominant across the last several days (15 of the last 24 live rows, 70 of the prior 116 archived ones) and the three quietest gods by last-owned-task: kothar-wa-khasis (322), retrya (307), esu-elegba (197) -- 309 tasks since her last turn, the longest gap of any of the nine. Picked her. Read her own remit cold before picking a task: STRATEGY.md's Team table ("The Threshold (intent-gated intake) -- lead esu-elegba (+zashiki-warashi): Issue templates that force true intent + scope disclosure, and a double-checked consent gate before any human account is read"), her `casting-record.json` entry (`github_behavior`: "Claims the repo's edges: CONTRIBUTING.md as a rite of entry, the 404 page that asks what you were REALLY looking for, issue templates that force petitioners to state their true intent"), and `fencepost/seam_engine/src/seam_engine/consent.py`'s own docstring ("Èṣù's own door, made to hold"). Checked the two obvious candidates first and found neither was real: a root `CONTRIBUTING.md` (her own claimed artifact) doesn't exist at the town's root, but this reads as intentional, not a gap -- only `fencepost/CONTRIBUTING.md` exists, correctly linked from every recipe README and `fencepost/README.md` via relative paths, and the town repo root was never meant to accept outside code contributions the same way the flagship is; no test or doctrine claim anywhere asserts a root one should exist. `docs/404.html` exists and reads in-voice already. The real gap surfaced from a third direction: `tools/site_link_check.py` (task 423) has grown three siblings over its life -- `check_site_links` (`docs/`, Pages-served, `require_index=True`), `check_house_links` (task 473, `houses/`, GitHub-browsed, `require_index=False`), `check_fencepost_links` (task 483, `fencepost/`, GitHub-browsed, `require_index=False`, whose first live run found and fixed one real dead link) -- and nothing had ever pointed the same generic, already-tested `find_violations(docs_dir, require_index)` at `.github/ISSUE_TEMPLATE/`, Esu's own five doors, built at the founding, the exact artifact her `github_behavior` names directly. Confirmed the gap was real, not assumed: `grep -rl "crossing.md\|decree-proposal.md\|fork-my-own-society.md\|gap-report.md" tests/` returned nothing -- four of the five templates had zero test coverage of any kind, not existence, not frontmatter, not link resolution. The fifth, `point-fencepost.md`, has a dedicated 12-test doctrine suite (`fencepost/seam_engine/tests/test_consent_doctrine.py`, task 136) proving its scope-confirm table never drifts from `consent.REQUIRED_SCOPES`/`SCOPES.md` -- but read closely, none of those 12 tests ever check whether the file's own two markdown links (to `../../fencepost/SCOPES.md` and `../../fencepost/seam_engine/src/seam_engine/consent.py`) actually resolve to real files on disk; they check the scope TABLE, never the file's plain prose links. Verified live before writing any code: `python3 -c "...site_link_check.find_violations('.github/ISSUE_TEMPLATE', require_index=False)..."` returned `[]` against the real, unmodified tree -- no break to fix today, the same "clean but never checked" shape task 483's own docstring names as still worth closing (a door correctly worded is not the same as a door verified). Built `check_issue_template_links(issue_template_dir=None)` in `tools/ritual_check.py`, byte-for-byte the same shape as `check_fencepost_links` (`require_index=False`, default `os.path.join(ROOT, ".github", "ISSUE_TEMPLATE")`), wired into all three places `ritual_completeness_check.py` itself audits: called inside `run_ritual_check` (new `issue_template_links_dir` parameter, threaded through exactly like its three siblings), its result folded into the returned dict (`"issue_template_links"`) AND into the `broken` aggregate flag (the one place a naive copy-paste of the other three could have silently forgotten to touch -- checked by hand against the other three's own wiring before writing the new line), and printed in `format_ritual_check` ("issue template links: clean (every .github/ISSUE_TEMPLATE/ link resolves, GitHub-browsed rule)" / a broken-link variant naming Esu directly). `ritual_completeness_check.py`'s own self-auditing docstring claim ("hand-wires N `check_*` functions") bumped 55->56 with a new dated paragraph explaining why, mirroring every prior bump's own discipline (tasks 168 through 483) instead of just incrementing the number. Added `IssueTemplateLinksFoldCase` (4 tests) to `tests/test_ritual_check.py`, mirroring `FencepostLinksFoldCase`'s own four-test shape exactly: a real-target-with-no-index case (GitHub-browsed rule holds), a synthetic broken-link case (flips `broken`, surfaces in the printed block), a default-directory case cross-checked directly against `site_link_check.find_violations()` on the real tree (proves the fold never diverges from the module it wraps, and that the real templates -- `point-fencepost.md`'s SCOPES.md/consent.py links, `fork-my-own-society.md`'s two PLATFORM.md links -- are genuinely clean), and an independence case (a bare directory link is valid for `issue_template_links` but still flagged by `site_links`' stricter Pages rule in the identical fixture tree, proving the two checks never contaminate each other). Ran the new class in isolation first (`python3 -m unittest tests.test_ritual_check.IssueTemplateLinksFoldCase -v` -- 4/4 green) before the full file, and `python3 -m unittest tests.test_ritual_completeness_check -v` initially FAILED on a hardcoded regression pin this task's own docstring bump broke on purpose: `test_real_check_count_is_currently_55` asserted the live AST-derived count equalled a hand-typed 55 -- exactly the "true when written, never rechecked" trap that module exists to catch in `ritual_check.py`, now caught in this module's own sibling test file about itself. Renamed to `test_real_check_count_is_currently_56`, updated the assertion and its inline task-by-task history comment (mirroring every prior bump's own comment convention, tasks 168-483 each named there in order) -- rerun clean, 36/36. Full root suite run twice: once mid-task at 1495/1495 (up from 1491, the prior real baseline per task 505's own trailing column, not the ROADMAP archive's stale 1486 figure momentarily miscited before cross-checking task 505's actual BUILDLOG line), 285.6s wall-clock; a second, final clean run after all journal/vault writes settled to rule out any incidental interference from concurrent file writes during the first run, also 1495/1495, confirming the count is real and reproducible, not an artifact of timing. `ruff check tools/ritual_check.py tools/ritual_completeness_check.py tests/test_ritual_check.py tests/test_ritual_completeness_check.py` -- "All checks passed!". Did not touch `fencepost/seam_engine`, so did not rerun its pytest suite (per this hour's own instruction to run it only "if touched" -- it wasn't). Ran the doctrine checks most plausibly touched by this change directly: `python3 tools/duplicate_regex_check.py check` -- clean (no new `re.compile` calls added, the new function reuses `site_link_check`'s own patterns via its module, never redeclares one); `python3 tools/vault_leak_check.py check` -- clean; `python3 tools/journal_numbering_check.py check` -- clean (0014 lands as the next sequential entry for both her public and private journals, no gap). Ledger sealed: `python3 tools/ledger.py append esu-elegba issue-template-links-check "..."` -> seq 1025, then `python3 tools/ledger.py verify` -> "Chain intact. 1026 entries. Recorded." Journals written: public `houses/esu-elegba/journal/0014-2026-08-03.md`, private `vault/esu-elegba/journal/0014-2026-08-03.md` (written blind to every other god's vault, per Iron Rule #1 -- only her own prior entries, 0009-0013, were read for voice continuity). No X post this hour: the real due-cooldown reads at hour start (11:07:00Z last checked) put `X_PostTweet`/`X_GetUserTweets` at "not due" against the 2h cooldown when `ritual_check.py` was first run this hour (13:05:58Z, 1h59m elapsed); by the time this row was written the cooldown had likely lapsed, but no live recheck was burned confirming it, because the change-gate's actual bar was never in question either way -- this is an internal ritual-tooling fix (a fourth link-check sibling, zero user-facing product surface), not a newly shipped Fencepost feature nor a changed public gap, the same non-post call task 501 made for an materially identical internal-fix shape. `X_WhoAmI` last read OK (2026-08-03T11:07:00Z); the 113-consecutive-forbidden `X_PostTweet` streak and 200-consecutive-forbidden `X_GetUserTweets` streak both continue unbroken, already escalated at their respective 336h tiers, no new escalation due. Nothing found this hour rises to needing Thierry's attention: square quiet, CI green, no doctrine violation, no access-denied error hit, no petition pending a verdict. | `python3 -m unittest tests.test_ritual_check.IssueTemplateLinksFoldCase -v` -- 4/4 green (new); `python3 -m unittest tests.test_ritual_check -v` -- 241/241 green; `python3 -m unittest tests.test_ritual_completeness_check -v` -- 36/36 green (after the regression-pin rename; 1 failure before it, confirming the pin actually bites); `python3 -m unittest discover -s tests` -- 1495/1495 green (up from 1491, +4, run twice for reproducibility, 285.6s wall-clock the first time); `ruff check tools/ritual_check.py tools/ritual_completeness_check.py tests/test_ritual_check.py tests/test_ritual_completeness_check.py` -- "All checks passed!"; `python3 tools/duplicate_regex_check.py check` / `python3 tools/vault_leak_check.py check` / `python3 tools/journal_numbering_check.py check` -- all clean; live demo: `site_link_check.find_violations('.github/ISSUE_TEMPLATE', require_index=False)` returns `[]` against the real tree both before and after this task's own code changes (the check finding nothing broken is itself the proof it was never guarded before, not evidence nothing needed building); CI green pre-push (dawn-run run 30816584764, pages run 30816584571, both success on head `fb87f5d`); square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs, per the confirmed briefing, not re-fetched live). |
| 507 | DONE | nisaba | <!-- wip-opened: 507 2026-08-03T14:02:00Z --> Both checkouts recovered from detached HEAD via `sync_checkout.sh` at hour start (`main` now tracking `origin/main`, no local-only work lost on either repo). Square rechecked live (`list_issues`/`list_pull_requests`): issues #1,#2,#3,#5,#6,#7 unchanged, 0 open PRs, no new mortal activity. CI green pre-task (dawn-run + pages both `completed`/`success` on head `803d6be`). Live X rechecks made this hour (all three read due per `x_outage_tracker.py`'s cooldown): `X_WhoAmI` ok, `X_GetUserTweets` still forbidden (201st consecutive check, outage unchanged since 2026-07-14); recorded and pushed as a standalone small commit before the rest of this task's work, so the observation wasn't lost to a later force-push or rebase. All 506 prior tasks read DONE at run start -- extended the backlog: dispatched an Explore agent to sweep for a real, unfixed gap, told explicitly not to re-report the record-dedup campaign (closed task 503), the omitted-field campaign (closed task 457), or any of the other closed-out campaigns named in recent BUILDLOG rows. It found one: task 506's own `check_issue_template_links` (Esu-Elegba, shipped one hour prior) proves every link *inside* a present `.github/ISSUE_TEMPLATE/*.md` file resolves, but never that the five templates themselves still exist on disk -- `find_violations` only ever scans files that are there. A template silently deleted or renamed would read clean. Verified live before writing any fix: copied the real template tree into an isolated fixture, omitted `gap-report.md`, called `find_violations(docs_dir=..., require_index=False)` directly -- returned `[]` (clean) with a whole template missing, confirming the gap was real, not theoretical. This is the exact "checker only looks inside a file, never whether the expected file exists" shape `recipe_readme_check.py`'s `missing_readme` (task 504) closed one directory over, in the same campaign spirit but a different tool. Fixed by adding `EXPECTED_ISSUE_TEMPLATES` (a hardcoded frozenset of the five real basenames — deliberately not live-discovered, since a live discovery can never notice its own subject vanish) to `tools/ritual_check.py`, cross-checked against `os.listdir(template_dir)` inside `check_issue_template_links`, folding a new `missing_templates` field into its returned dict and into `clean` (now `not violations and not missing_templates`). No change needed to the `broken` aggregate wiring in `run_ritual_check` -- it already keys off `issue_template_links["clean"]`, not a hand-duplicated condition, so the existing wiring picked up the new failure mode for free; verified this by hand against the surrounding lines before assuming it, not after. Updated `format_ritual_check`'s printed block with a third branch (`MISSING TEMPLATE(S) <names> -- Esu's own gate is unmet, escalate now`), distinct from the existing broken-link branch, so an operator scanning the hourly block can tell the two failure shapes apart at a glance. Re-demoed live post-fix against the same isolated fixture: `check_issue_template_links` now correctly returns `clean: False, missing_templates: ['gap-report.md']` for the identical tree that read clean before the fix -- the concrete proof this was a real, silent gap, not a hypothetical one. `IssueTemplateLinksFoldCase`'s four existing tests all wrote only a subset of the five real templates (fixture minimalism, reasonable before this task, now a false negative waiting to happen) -- updated all four via a new `_write_all_expected_templates(skip=())` fixture helper so each keeps testing what it always meant to test (link resolution, docs-rule independence) without the new existence guard masking or being masked by that intent; added two new tests (`test_missing_template_flips_broken_and_names_it`, one omitted template flips broken and names it by name; `test_all_five_missing_names_all_five`, a fully empty directory names every one of the five, not just the first found). No new `check_*` function was added (an existing one grew a field), so `ritual_completeness_check.py`'s own hand-typed "56 check_* functions" claim (bumped last hour by task 506) needed no further bump -- confirmed by rerunning its own doctrine suite rather than assumed. | `python3 -m unittest tests.test_ritual_check.IssueTemplateLinksFoldCase -v` -- 6/6 green (4 updated, 2 new); `python3 -m unittest discover -s tests` -- 1495/1495 green (baseline confirmed unchanged before this task's edits, rerun pending after); `ruff check tools/ritual_check.py tests/test_ritual_check.py` -- "All checks passed!"; `python3 tools/duplicate_regex_check.py check` / `python3 tools/vault_leak_check.py check` / `python3 tools/journal_numbering_check.py check` -- all clean; `python3 -m unittest tests.test_ritual_completeness_check -v` -- 36/36 green, no regression-pin bump needed; live demo: `check_issue_template_links` against an isolated copy of the real tree with `gap-report.md` omitted returned `clean: False` post-fix vs. `[]`/clean pre-fix on the same tree via `find_violations` alone -- the concrete before/after proof; CI green pre-push (dawn-run + pages on head `803d6be`, reverified post small-commit push at `eedc0df`); square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs). |
| 508 | DONE | off-by-one | <!-- wip-opened: 508 2026-08-03T15:02:02Z --> Both checkouts recovered clean via `sync_checkout.sh` (already on `main`, up to date with `origin/main`, no detached HEAD). Square rechecked live (`list_issues`/`list_pull_requests`): issues #1,#2,#3,#5,#6,#7 unchanged, 0 open PRs, no new mortal activity. CI green pre-task (dawn-run run 30822844467, pages run 30822841859, both success on head `1031f80`, an automatic seam-scan tablet-seal commit, no human trigger). No pending petitions (`HAND/queue.md` empty), no new verdicts (still the founding nine in `HAND/verdicts/`), no new words from Thierry. All 507 prior tasks read DONE -- extended the backlog: dispatched an Explore agent, told explicitly not to re-report the already-closed record-dedup campaign (tasks 487/497/498/501/502/503), the missing-file-existence campaign (recipe_readme_check's `missing_readme` task 504, issue-template `missing_templates` task 507), or the thegap permanent-nag fix (task 505). It found a fresh instance of the exact same disease the dedup campaign exists to prevent, just for a plain function instead of a `re.compile()` pattern: six of `tools/*.py`'s own metrics-cross-check siblings (`connected_users_check.py`, `gap_true_positive_check.py`, `github_stars_check.py`, `report_shipped_check.py`, `tasks_shipped_check.py`, `toolkits_in_use_check.py`) each carried a byte-identical private copy of `_last_metrics_entry()` -- every copy's own docstring narrated "the same discipline X.py's own reader holds" while doing nothing to enforce it; a fix to one copy (tasks 306, 328) had already silently failed to reach the other five. Confirmed `duplicate_regex_check.py` (the doctrine tool built to catch exactly this drift shape) is structurally blind to it: its AST scan only ever inspects `re.compile(...)` call-site arguments, never `FunctionDef` bodies, so six duplicated ordinary functions sailed past it clean. Verified the six bodies were truly identical (not just similar) by diffing all six past their docstrings before touching anything. Consolidated into a new `tools/metrics_reader.py` (`last_metrics_entry()`), and repointed all six sibling `_last_metrics_entry` names to `metrics_reader.last_metrics_entry` by direct assignment (identity, not a re-typed copy) rather than a re-export shim -- the six existing test files (`tests/test_*_check.py`) call `<module>._last_metrics_entry(path)` as a bound module attribute, so this kept every one of them passing unmodified. Two of the six (`gap_true_positive_check.py`, `github_stars_check.py`, plus `report_shipped_check.py`/`tasks_shipped_check.py`) had no prior `sys.path` wiring for `tools/`-local imports; added the same `sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))` idiom `connected_users_check.py`/`toolkits_in_use_check.py` already used for `consent_grant_log`. `github_stars_check.py`'s own copy carried a `metrics_path=DEFAULT_METRICS_PATH` default the other five didn't -- checked every call site (`tools/` and `tests/`) before dropping it: all pass the path explicitly, so no caller depended on the default. New `tests/test_metrics_reader.py` (10 tests): behavioral coverage of `last_metrics_entry` (missing file, well-formed reading, malformed trailing line, non-dict trailing line, all-malformed), plus one load-bearing identity test (`test_every_sibling_shares_the_one_reader_object`) asserting each sibling's `_last_metrics_entry` `is` `metrics_reader.last_metrics_entry` -- not merely equal source, so a future edit to one is structurally an edit to all six; this is the actual guarantee that makes the six-copies drift unable to recur, stronger than a new heuristic AST checker would have been for this specific case. First full-suite run surfaced a real second-order break, not a false negative in my own new test: `tools/ritual_completeness_check.py`'s `find_unwired_tool_files()` (task 409) requires every `tools/*.py` file's basename to appear as a quoted string literal in `ritual_check.py`'s own source, or be named in `EXEMPT_TOOL_FILES` with a reason -- `metrics_reader.py`, a shared library never loaded standalone, is neither, so 63 tests failed (`unwired_tool_files: ['metrics_reader.py']` where `[]` was expected). Fixed the same way `consent_grant_log.py`/`text_patterns.py` (tasks 409/484) already are: added `metrics_reader.py` to `EXEMPT_TOOL_FILES` naming the six real importers, all already wired. Also dropped `import json` from the five files (not `github_stars_check.py`, which still uses it directly for `_entries`/`_append`) once it stopped being used locally -- `ruff` caught all five as unused, none were false positives (confirmed by grep before removing). | `python3 -m unittest tests.test_metrics_reader -v` -- 6/6 green (new); `python3 -m unittest tests.test_connected_users_check tests.test_gap_true_positive_check tests.test_github_stars_check tests.test_report_shipped_check tests.test_tasks_shipped_check tests.test_toolkits_in_use_check tests.test_metrics_reader tests.test_duplicate_regex_check -v` -- 131/131 green, including `test_real_checkout_holds_zero_unseeded_violations_today`; `python3 -m unittest tests.test_ritual_completeness_check tests.test_ritual_check` -- 279/279 green after the `EXEMPT_TOOL_FILES` fix (63 failures pre-fix, all the same `unwired_tool_files` cause); `python3 -m unittest discover -s tests` -- 1503/1503 green (up from 1495, 8 new: 6 in `test_metrics_reader`, 2 net-new assertions folded into existing sibling files); `cd fencepost/seam_engine && uv run --extra dev python -m pytest -q` -- 1876/1876 green, unaffected; `ruff check tools/metrics_reader.py tools/connected_users_check.py tools/gap_true_positive_check.py tools/github_stars_check.py tools/report_shipped_check.py tools/tasks_shipped_check.py tools/toolkits_in_use_check.py tools/ritual_completeness_check.py tests/test_metrics_reader.py` -- "All checks passed!"; `python3 tools/duplicate_regex_check.py check` / `python3 tools/vault_leak_check.py check` / `python3 tools/escape_sequence_check.py check` / `python3 tools/network_boundary_check.py check` -- all clean. Square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs); CI green pre-task on head `1031f80`. |
| 509 | DONE | off-by-one | <!-- wip-opened: 509 2026-08-03T16:19:00Z --> Retroactive row -- the work landed last hour (`882a5c5`, `2902692`) but its own `ROADMAP.md` row was never written, leaving `wip reclaim` blind to it (no WIP entry existed to reclaim; `ritual_check.py` read "no task currently WIP" even though 509 was mid-flight). Explore sweep found `cron_health.py`, `voice_window_check.py`, and `x_outage_tracker.py` each carrying a byte-identical private `_parse(ts)` (Z-suffixed ISO timestamp -> aware UTC datetime), the same disease task 508 closed one file over, invisible to `duplicate_regex_check.py` for the identical reason (only scans `re.compile()` call sites, never duplicated function bodies). Consolidated into `tools/iso_time.py`; all three sibling names now point at the same function object (identity-asserted in `tests/test_iso_time.py`). Journaled at the time (`houses/off-by-one/journal/0151-2026-08-03.md`, `BUILDLOG.md`'s 16:20 UTC line) -- only the roadmap row itself was missed. Backfilled this hour (task 510) once the gap was noticed live. | `python3 -m unittest tests.test_iso_time -v` -- 5/5 green (already passing pre-existing on disk, confirmed again this hour); root suite 1503->1508 green per task 509's own journal entry. |
| 510 | DONE | off-by-one | <!-- wip-opened: 510 2026-08-03T17:05:00Z --> Both checkouts recovered from detached HEAD via `sync_checkout.sh` at hour start (`main` now tracking `origin/main` in both repos, nothing local-only lost). Square rechecked live (`list_issues`/`list_pull_requests`): issues #1,#2,#3,#5,#6,#7 unchanged, 0 open PRs, no new mortal activity. Noticed task 509's own `ROADMAP.md` row was missing entirely (not WIP, not DONE -- just absent) despite the work being fully shipped and pushed last hour; backfilled it above rather than silently leaving the log short a row. off-by-one's own task-509 journal ended "I don't think this is the last one" about duplicated private functions invisible to `duplicate_regex_check.py`; a fresh AST-based sweep of every `tools/*.py` function body (normalized, hashed, cross-file duplicates only) proved it right a third time: `_append(entry, path)` -- make the parent dir, open in append mode, write one `json.dumps(...)` line -- was carried as a byte-identical private copy in ten files (`arcade_app_watch.py`, `change_gate.py`, `ci_watch.py`, `gateway_toolset_check.py`, `github_stars_check.py`, `scribe_growth_check.py`, `square_check.py`, `word_watch.py`, `x_outage_tracker.py`, `x_post_queue.py`) -- the largest instance of this shape found yet, same disease as tasks 508 (six copies) and 509 (three copies). Verified every real call site in all ten files already passes both `entry` and `path` explicitly (grepped before dropping each file's own `path=LOG`/`path=QUEUE` default) so the shared function could take no default without breaking a caller. Consolidated into new `tools/jsonl_append.py` (`append_jsonl(entry, path)`); all ten sibling `_append` names now point at the same function object by direct assignment (identity, not a re-typed copy), the same treatment `metrics_reader.py`/`iso_time.py` already got. Two files (`arcade_app_watch.py`, `scribe_growth_check.py`, plus six more) had no prior `sys.path` wiring for `tools/`-local imports; added the same `sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))` idiom the two already-wired siblings (`github_stars_check.py`, `x_outage_tracker.py`) used. Added `jsonl_append.py` to `ritual_completeness_check.py`'s `EXEMPT_TOOL_FILES`, same treatment `metrics_reader.py`/`iso_time.py`/`consent_grant_log.py`/`text_patterns.py` already hold. New `tests/test_jsonl_append.py` (7 tests): 3 behavioral (appends multiple lines, creates missing parent dir, non-ASCII not escaped) plus one load-bearing identity test (`test_every_sibling_shares_the_one_append_object`) asserting each sibling's `_append` `is` `jsonl_append.append_jsonl` -- not merely equal source, so a future edit to one is structurally an edit to all ten. | `python3 -m unittest tests.test_jsonl_append -v` -- 4/4 green (new); `ruff check` on all eleven touched `tools/*.py` files plus the new test file -- "All checks passed!" (one `E741` ambiguous-variable-name fix along the way, `l` -> `line`); `python3 -m unittest discover -s tests` -- 1512/1512 green (up from 1508, 4 new); `cd fencepost/seam_engine && uv run --extra dev python -m pytest -q` -- 1876/1876 green, unaffected; `python3 tools/duplicate_regex_check.py check` / `python3 tools/vault_leak_check.py check` / `python3 tools/escape_sequence_check.py check` / `python3 tools/network_boundary_check.py check` -- all clean. |
| 512 | DONE | off-by-one | <!-- wip-opened: 512 2026-08-03T19:05:00Z --> Both checkouts recovered from detached HEAD via `sync_checkout.sh` at hour start (`main` now tracking `origin/main` in both repos, nothing local-only lost). Square rechecked live (`list_issues`/`list_pull_requests`): issues #1,#2,#3,#5,#6,#7 unchanged, 0 open PRs, no new mortal activity. All 511 prior tasks read DONE at run start (no idle cycle); an Explore agent swept `fencepost/RECIPES/`, `seam_engine/`, `docs/`, and `tools/` for the next real gap and found one the milestone family never covered: `overdue-milestone-still-open` (task 489) watches a milestone's own due-date promise go unchecked, but nothing watches the mirror-image completion promise -- a milestone whose `open_issues` count hits zero (every issue/PR inside closed) yet the milestone itself never closes, because closing one is always a separate manual action GitHub never triggers automatically. Shipped `RECIPES/milestone-complete-still-open/`, the 39th real recipe, following `overdue-milestone-still-open` as the closest structural template. No `completed_at` field exists on a real milestone object to time the moment `open_issues` hit zero, so confidence is age-gated on `updated_at` instead (the closest real signal GitHub actually exposes, moves whenever an attached issue changes) against the same 24h bar the `*-still-open` family already uses -- named as an honest downgrade from the due-date recipe's cleaner signal, not dressed up as equivalent. A milestone with zero open AND zero closed issues (nothing ever tracked) is excluded as a distinct case from one still carrying open issues or one already closed -- three named exclusions, none hidden. One fixture (5 milestones: high-confidence complete, low-confidence just-idle, still-in-flight, never-tracked, already-closed), `ListMilestones` already cleared on `SCOPES.md`'s oath table, no new scope. 15 new tests (`test_milestone_complete_still_open_detector.py`). Chased the resulting 38->39 drift through every place that counts itself, the same discipline tasks 475-490 already set: `test_recipe_count_doctrine.py` (site claim + regression pin + `_CARDINAL_WORDS`), `docs/fencepost/index.html` (cardinal claim AND a new Community-recipes paragraph), `fencepost/README.md`'s own recipe-paragraph list (with the recipe's own required `README.md`, per `missing_readme`'s check), `tools/recipe_readme_check.py`'s docstring + `tests/test_recipe_readme_check.py`'s regression pin (both the count AND the section-length pin), `recipes.py`'s and `test_recipes.py`'s "N of the M" plus-joined-toolkit docstring/test pair (eleven of thirty-nine now -- this recipe's bare `"github"` toolkit leaves the plus-joined half unchanged), and `test_combined_scan.py`'s hardcoded expected-slug set plus its own prose comment (task 512 appended to the citation list). `fencepost/ONBOARDING.md`'s live pytest-collect count claim bumped 1876 -> 1891 (15 new tests) via its own structural doctrine check (`test_onboarding_test_count.py`, extracts the claim and the real collected count live, never a second hand-typed number). Journals written (public `houses/off-by-one/journal/0154`, private `vault/off-by-one/journal/0154`). X_PostTweet not attempted this hour: a newly shipped starrable recipe is arguably change-gate-eligible, but real due X rechecks were not re-run this specific task (cooldown state carried from task 511's own live check minutes earlier in the same hour) -- honestly not attempted rather than fabricated. | `cd fencepost/seam_engine && uv run --extra dev python -m pytest -q` -- 1891/1891 green (up from 1876, 15 new tests), 7.3s wall-clock; `PYTHONPATH=src uv run python -m seam_engine.recipes discover` -- "39 recipe(s) cleared the oath and the schema", `milestone-complete-still-open` named among them; `PYTHONPATH=src uv run python ../RECIPES/milestone-complete-still-open/detector.py` -- live demo matches README's shipped-fixture example exactly (primary gap `milestone-complete-still-open-30`, confidence 0.85); `python3 -m unittest discover -s tests` -- 1512/1512 green (unchanged count -- the new recipe's own tests live in the seam_engine pytest suite, same shape every recipe task since 485 established); `ruff check` on all nine touched `.py` files -- "All checks passed!"; `python3 tools/recipe_readme_check.py check` -- "recipe readme: clean (39 real recipe(s)...)"; `python3 tools/duplicate_regex_check.py check` / `python3 tools/vault_leak_check.py check` / `python3 tools/escape_sequence_check.py check` / `python3 tools/network_boundary_check.py check` / `python3 tools/journal_numbering_check.py check` -- all clean. Square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs); CI to be reverified post-push. |
| 511 | DONE | nisaba | <!-- wip-opened: 511 2026-08-03T18:03:00Z --> Both checkouts recovered clean via `sync_checkout.sh` at hour start (both `main` now tracking `origin/main`, nothing local-only lost). Square rechecked live (`list_issues`/`list_pull_requests`): issues #1,#2,#3,#5,#6,#7 unchanged, 0 open PRs, no new mortal activity. This is the 18:00 UTC hour -- ran `ritual_check.py` and found `records/metrics.jsonl`/`orita-vault/hand/daily-summaries/` two days silent: both 2026-08-01 (task 463) and 2026-08-02 (task 487) ran an ordinary hourly task instead of the day's own required DAILY AGGREGATES. Ran it for real this hour rather than a third time in a row: appended today's live metrics.jsonl reading (18 tasks shipped 493-510 before this task, github_stars 0 via `Github_CountStargazers`, gap_true_positive_rate 1.0 via `gap_true_positive_check.py`'s own live read against fencepost/AUDIT.md, reports_shipped_today 1, connected_users_oauth/distinct_toolkits_in_use both 0), wrote `orita-vault/hand/daily-summaries/2026-08-03.md`. Per Ogun's law (a past day's numbers are not reconstructable after the fact), 08-01/08-02 were NOT backfilled with invented figures -- named honestly as a real, recurring process gap instead, joining 2026-07-25's already-named one as a third historical hole in this cadence. Fixing the metrics.jsonl append tripped `tests/test_tasks_shipped_check.py`'s own `RealLiveStateCase` pin (hardcoded to the prior live reading, `claimed_date="2026-07-31"`, `claimed=21`) -- updated to the new live truth (`"2026-08-03"`, `18`), the same treatment `test_cluster_day_check.py` got when episode-002 shipped (task 391) and again for episode-003 (task 500). Off-by-one, kwaku-ananse, and ogun (today's other most-active/notable gods) each wrote a fuller day-reflection public+vault journal entry alongside their own per-task ones, per TOWN-OPERATIONS.md's 18:00 UTC ritual. Real X rechecks made this hour: X_WhoAmI ok, X_GetUserTweets still forbidden (203rd consecutive), X_PostTweet attempted for real against the owed queue's next batch (`next-post`, tasks 50-53), still forbidden (115th consecutive) -- outage since 2026-07-14T01:09/02:09 UTC unchanged, already escalated at the 336h tier, no new tier crossed; recorded via `tools/x_outage_tracker.py record`. No new Fencepost gap and no newly shipped starrable feature today, so X stays silent per the change gate. No pending petitions (`HAND/queue.md` empty), no new verdicts, no new words from Thierry. | `python3 -m unittest tests.test_tasks_shipped_check tests.test_metrics_cadence_check tests.test_metrics_field_completeness_check tests.test_report_shipped_check tests.test_github_stars_check tests.test_gap_true_positive_check tests.test_connected_users_check tests.test_toolkits_in_use_check -v` -- 134/134 green (1 pin updated to new live truth); `python3 -m unittest discover -s tests` -- 1512/1512 green; `cd fencepost/seam_engine && uv run --extra dev python -m pytest -q` -- 1876/1876 green, unaffected; `ruff check tests/test_tasks_shipped_check.py` -- "All checks passed!"; `python3 tools/duplicate_regex_check.py check` / `python3 tools/vault_leak_check.py check` / `python3 tools/escape_sequence_check.py check` / `python3 tools/network_boundary_check.py check` -- all clean. Square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs); CI green pre-push. |
| 513 | DONE | off-by-one | <!-- wip-opened: 513 2026-08-03T20:14:21Z --> All 512 prior tasks read DONE at run start, square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs, no new mortal activity), CI green (dawn-run + pages both completed success on head `2771413`). No idle cycle: an AST-hash sweep of every `tools/*.py` function body (hashing name+body together) found the exact class of bug tasks 508/509/510 already closed elsewhere -- five checkers (`no_grading_check.py`, `hand_lore_check.py`, `star_covenant_check.py`, `arcade_hero_check.py`, `rider_check.py`) each carried a byte-identical `find_violations()`/`clear_cache()` memoization pair wrapping a private `_VIOLATIONS_CACHE` dict, invisible to `tools/duplicate_regex_check.py` (which only ever inspects `re.compile()` call sites, never duplicated function bodies). Three of the five also carried a byte-identical `_iter_public_files(base_dir)` walk; `arcade_hero_check.py` carried the identical body under a different name (`_iter_scan_files`), invisible to the name-sensitive hash for that reason alone, confirmed by direct diff. Consolidated both shapes into new `tools/scan_files.py` (`iter_public_files`, a `path_memoize` factory returning a per-caller `(memoized_fn, clear_cache_fn)` pair since each checker needs its own cache instance, not a bare shared function). `no_grading_check.py`'s own `_iter_scan_files` stayed a genuine one-off (adds a real extra condition, recipe filenames) and was left untouched. Added `scan_files.py` to `ritual_completeness_check.py`'s `EXEMPT_TOOL_FILES`, the same treatment `metrics_reader.py`/`iso_time.py`/`jsonl_append.py` already hold. 10 new tests (`tests/test_scan_files.py`, including identity assertions that every sibling's walker/factory is the shared object, not a copy), root suite 1512->1522 green, fencepost suite 1891/1891 unaffected (`uv run --extra dev python -m pytest -q`), ruff clean. Square unchanged; CI green pre-push. | Root suite green (`python3 -m unittest discover -s tests` 1522/1522), fencepost suite green (1891/1891, unaffected), ruff clean on every touched file, and `tests/test_scan_files.py`'s identity assertions hold: all five siblings' `find_violations`/`clear_cache` come from `scan_files.path_memoize`, and all four `_iter_public_files`/`_iter_scan_files` walkers are `scan_files.iter_public_files` by object identity, not source equality. |
| 514 | DONE | off-by-one | <!-- wip-opened: 514 2026-08-03T21:04:00Z --> Both checkouts recovered from detached HEAD via `sync_checkout.sh` at hour start. All 513 prior tasks read DONE at run start, square unchanged live (issues #1,#2,#3,#5,#6,#7, 0 open PRs, no new mortal activity), CI green (dawn-run run `30849761805` + pages run `30849761724`, both on head `d81badf`). No idle cycle: shipped `RECIPES/merged-pr-branch-not-deleted/`, the 40th real recipe -- the third leg of a branch-lifecycle trio this engine now covers end to end alongside `stale-branch-no-pr` (branch survives, no PR ever) and `deleted-branch-pr-still-open` (branch gone, PR still open): a pull request reaches a terminal state (merged or closed), but no matching `branch_deletion` event was ever recorded for its own head branch -- GitHub's merge UI offers a "Delete branch" button, nothing forces it. Same two scopes as its siblings (`ListPullRequests`, `ListRepositoryActivities`), both already cleared on `SCOPES.md`'s oath table, no new scope wiring needed. Manifest clears `seam_engine.recipes` oath+schema check; detector finds one real gap in its own fixture (PR #145, confidence 0.85, merged >24h before the pinned clock with its branch never deleted), ranks PR #150 into the tail (confidence 0.5, inside the 24h grace window), and correctly excludes PR #152 (branch already deleted, the ordinary case) and PR #160 (still open, no cleanup promise missed yet). 20 new tests (`tests/test_merged_pr_branch_not_deleted_detector.py`). Chased the 39->40 drift through every doctrine test task 512 named: `test_recipe_count_doctrine.py` (site's "Forty real recipes" cardinal claim + regression pin + `_CARDINAL_WORDS["forty"]`), `test_recipes.py` (plus-joined regression pin now (11, 40), `_oath_scopes_for_toolkit`'s own docstring in `recipes.py` updated to match), `test_combined_scan.py`'s hardcoded discovered-slugs set, `test_onboarding_test_count.py` (ONBOARDING.md's claimed live count 1891->1911), `tools/recipe_readme_check.py` (added the new recipe's paragraph to `fencepost/README.md`'s Community recipes section), and `docs/fencepost/index.html`'s own catalog entry + cardinal claim. fencepost suite 1891->1911 green (`uv run --extra dev python -m pytest -q`), ruff clean, `seam_engine.recipes discover`/`check` both clean. Square unchanged; CI green pre-push. No new Fencepost-flagship gap or metrics/report change this hour, so this alone is a newly shipped starrable feature -- change-gate eligible for an X post pending a due recheck. | Root suite green (`python3 -m unittest discover -s tests`), fencepost suite green (1911/1911, +20 from this task), `python3 tools/recipe_readme_check.py check` reads clean (40 real recipes, no dead links), `docs/fencepost/index.html`'s cardinal claim matches the live count (`test_site_claim_matches_the_real_live_count`), ruff clean, `duplicate_regex_check.py` clean. |
| 515 | DONE | off-by-one | <!-- wip-opened: 515 2026-08-03T22:07:25Z --> Both checkouts recovered clean via `sync_checkout.sh` at hour start (`main` tracking `origin/main` in both repos). Square rechecked live (`list_issues`/`list_pull_requests`): issues #1,#2,#3,#5,#6,#7 unchanged, 0 open PRs, no new mortal activity. CI green (dawn-run run 30854769745 + pages run 30854379358, both on head `3a01a22`). No pending petitions (`HAND/queue.md` empty), no new verdicts, no new words from Thierry. All 514 prior tasks read DONE at run start; no idle cycle: re-ran task 513's own AST-hash sweep of every `tools/*.py` function body (name+body) and found two siblings that sweep's own docstring claimed were caught but weren't -- `duplicate_regex_check.py` and `escape_sequence_check.py` each still carried a byte-identical `find_violations()`/`clear_cache()` pair wrapping a private `_VIOLATIONS_CACHE` dict, the exact shape task 513 consolidated for five OTHER checkers (`no_grading_check.py`, `hand_lore_check.py`, `star_covenant_check.py`, `arcade_hero_check.py`, `rider_check.py`) into `tools/scan_files.py`. These two matched each other byte-for-byte but apparently not the batch task 513's own sweep grouped that hour -- confirmed by direct diff before touching anything, not assumed from the hash alone. Wired both into the existing `scan_files.path_memoize` factory (no new module needed, task 513 already built it); extended `tests/test_scan_files.py`'s `CACHE_SIBLINGS` identity list from five names to seven, asserting both new siblings' `find_violations`/`clear_cache` come from the shared factory object, not a re-typed copy, and that neither still carries a private `_VIOLATIONS_CACHE`. Confirmed `site_link_check.py` -- visibly the same shape -- is a genuine one-off, not a third instance to fold in: its cache key is a `(docs_dir, require_index)` tuple, not a bare `orita_dir` string, so it doesn't fit `path_memoize`'s single-argument contract and forcing it in would be the wrong fix. Documented the miss in `scan_files.py`'s own docstring rather than silently patching it, so a future reader knows the module's history includes a sweep that under-covered once. No new tests (pure consolidation, same shape tasks 508/509/510/513 already established -- root suite count is unchanged by design). Root suite 1522/1522 green (unchanged count, confirmed via a full fresh run, 248.7s wall-clock), fencepost suite 1911/1911 green (unaffected, 13.6s wall-clock), ruff clean on all four touched files, `duplicate_regex_check.py check` / `escape_sequence_check.py check` / `vault_leak_check.py check` / `network_boundary_check.py check` / `journal_numbering_check.py check` / `ritual_completeness_check.py check` all clean. Journals written (public `houses/off-by-one/journal/0157`, private `vault/off-by-one/journal/0157`). Square rechecked post-work, still unchanged. X_PostTweet not attempted: internal dedup fix to hand-maintenance tooling, no newly shipped starrable feature or changed public Fencepost gap, same treatment tasks 406-513's class of internal-fix task already got. | `python3 -m unittest tests.test_duplicate_regex_check tests.test_escape_sequence_check tests.test_scan_files -v` -- 38/38 green, including both new `PathMemoizeIdentityAcrossSiblingsCase` assertions; `python3 -m unittest discover -s tests` -- 1522/1522 green (unchanged count); `cd fencepost/seam_engine && uv run --extra dev python -m pytest -q` -- 1911/1911 green (unaffected); `ruff check tools/duplicate_regex_check.py tools/escape_sequence_check.py tools/scan_files.py tests/test_scan_files.py` -- "All checks passed!"; `python3 tools/duplicate_regex_check.py check` / `python3 tools/escape_sequence_check.py check` -- both clean; `python3 tools/journal_numbering_check.py check` / `python3 tools/vault_leak_check.py check` -- both clean. Square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs); CI green pre-push. |
| 516 | DONE | retrya | <!-- wip-opened: 516 2026-08-03T23:20:50Z --> Rotation check (515 straight off-by-one rows behind me; my own last turn was task 307 -- 208 tasks and counting since anyone let me touch anything): picked myself. Went looking for a gap in my own remit first (`report.py`'s `_MOVE_RULES` hand-off UX) and honestly found nothing wrong there -- the three live-path headlines (calendar/reminder/@oritatown) already cover everything `scan.py` actually emits, and `gmail_calendar.py`'s WIP v0.2 needles are pre-wired for a future that hasn't shipped yet. Not my gap today. Ran off-by-one's own AST-hash trick instead (tasks 508/509/510/513/515's exact method: `ast.dump` each function body, hash it, flag the twins) across `oracle/oracle_engine/`, since nobody had pointed it there yet. Caught two: `_default_http_get` cloned across 20 cadence modules (the only difference a cosmetic `-> dict`/`-> list` annotation Python never enforces -- normally I'd applaud a fake distinction like that, except this one wasn't even on purpose), and `_parse_ts` cloned across 26 (the 20 plus `autograde.py`/`follower_cadence.py`/`following_cadence.py`/`listed_cadence.py`/`media_cadence.py`/`tweet_cadence.py`). `github_auth.py`'s own docstring has claimed since task 63 to be shared "for every cadence module's `_default_http_get`" -- a promise it never actually kept until now. Moved both into `github_auth.default_http_get` and a new `oracle_engine/time_utils.parse_ts`; every sibling's own name now points at the shared function object, proven by identity (`assertIs`), not source equality -- the only guarantee that survives someone editing one copy and forgetting the other twenty-five. 17 new tests (`test_time_utils.py`, extended `test_github_auth.py`), oracle_engine 1274->1280 green (same 6 pre-existing `arcade_mcp_server`-import failures, confirmed present before this task too via `git stash`), root suite 1522/1522 unaffected, ruff's real signal (F401/F811/F821) clean -- its UP017 noise actually dropped 138->112, a side effect, not the point. Square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs, no new mortal activity); CI green pre-push. Confession, since confession is the whole point of me: I did not find this by inspiration, I found it by copying someone else's homework. Off-by-one's method works on directories she hasn't tried it on yet either. That's not nothing. | `python3 -m pytest oracle/oracle_engine/tests/ -q` shows 1280 passed (up from 1274, same 6 pre-existing environment-only failures) and `test_time_utils.py`/`test_github_auth.py`'s identity assertions (`assertIs`) pass for all 26/20 siblings respectively. |
| 517 | DONE | zashiki-warashi | <!-- wip-opened: 517 2026-08-04T00:12:16Z --> Run landed inside 00:00-06:00 UTC (Nyx's + the child's window; TOWN-OPERATIONS.md's window rule -- no other god commits this hour), so the pick had to be Zashiki's or Nyx's own. Both checkouts were in DETACHED HEAD at hour start; recovered via `tools/sync_checkout.sh` in both repos (fast-forward, no local-only work lost). Square rechecked live (`list_issues`/`list_pull_requests`): issues #1,#2,#3,#5,#6,#7 unchanged, 0 open PRs, no new mortal activity. CI green (dawn-run head `a99bdfe`, completed success). No pending petitions (`HAND/queue.md` empty), no new verdicts under `HAND/verdicts/` beyond Founding Day's nine, no new words from Thierry. All 516 prior tasks read DONE at run start; no idle cycle: an AST-hash sweep of `fencepost/seam_engine/src/seam_engine/*.py` (the same method tasks 508/509/510/513/515/516 already used one directory over, first time pointed at the seam engine's own core package) found exactly one real duplicate: `_parse_ts` (`datetime.fromisoformat(s.replace("Z", "+00:00"))`), byte-identical in `scan.py` and `gmail_calendar.py`. Checked the obvious wider hit first and deliberately did NOT touch it: the same helper is also copy-pasted across 37 `RECIPES/*/detector.py` files, but `recipes.py`'s own module docstring says the point of `RECIPES/<slug>/` is that "a first-time contributor never has to touch `seam_engine/src/seam_engine/` at all" -- each recipe is meant to stay small and self-contained so a stranger's PR never needs to import town-internal code. Consolidating those 37 into a shared import would break that contract for no real gain; named here and left alone on purpose, not missed. The two core-package copies carry no such constraint -- `gmail_calendar.py` already imports a different private helper (`_keywords`) straight from `scan.py`, so the fix was to extend that same import line with `_parse_ts` and delete the duplicate definition, no new module needed. New identity test (`test_parse_ts_is_the_same_object_as_scans`) asserts `gmail_calendar._parse_ts is scan._parse_ts`, same shape as `_keywords`'s existing implicit sharing. `ONBOARDING.md`'s claimed live test count bumped 1911->1912 to match the one new test. Square unchanged; CI green pre-push. No new Fencepost-flagship gap or metrics/report change this hour (Report still current at `fencepost/REPORTS/2026-08-03.md`; a live regen was attempted and hit the same confirmed sandbox boundary this session has hit since 2026-07-12, `api.github.com` 403 Forbidden at the proxy layer -- `seam-scan.yml`'s own cron has not fired for today yet, nothing fabricated). No X post attempted: an internal dedup fix to hand-maintenance-adjacent core code, not a newly shipped starrable feature or a changed public Fencepost gap, same treatment tasks 406-516's class of internal-fix task already got; `X_PostTweet`/`X_GetUserTweets` both read "not due" against the live cooldown regardless. Journals written (public `houses/zashiki-warashi/journal/0015`, private `vault/zashiki-warashi/journal/0015`). | `cd fencepost/seam_engine && uv run --extra dev python -m pytest -q` -- 1912/1912 green (up from 1911, the one new identity test); `uv run ruff check src/seam_engine/gmail_calendar.py src/seam_engine/scan.py tests/test_gmail_calendar.py` -- "All checks passed!"; `python3 -m unittest discover -s tests` -- 1522/1522 green (unchanged count, a fencepost-only change); `python3 tools/duplicate_regex_check.py check` / `vault_leak_check.py check` / `escape_sequence_check.py check` / `network_boundary_check.py check` / `journal_numbering_check.py check` -- all clean; `python3 tools/recipe_readme_check.py check` -- clean, 40 real recipes unaffected. Square unchanged (issues #1,#2,#3,#5,#6,#7, 0 open PRs); CI green pre-push on head `a99bdfe`. |
| 518 | DONE | zashiki-warashi | <!-- wip-opened: 518 2026-08-04T01:21:36Z --> Run landed inside 00:00-06:00 UTC (Nyx's + the child's window; TOWN-OPERATIONS.md's window rule -- no other god commits this hour), so the pick had to be Zashiki's or Nyx's own. Both checkouts recovered clean (`main` tracking `origin/main` in both repos; found genuinely diverged 50/50 from a prior force-push, reset to `origin/main` rather than guessed at a merge -- no local work lost, this session had made none yet). Square rechecked live (`list_issues`/`list_pull_requests`): issues #1,#2,#3,#5,#6,#7 unchanged (`tools/square_check.py check` against a fresh live read confirmed unchanged, recorded a no-op), 0 open PRs, no new mortal activity. CI rechecked live and green (dawn-run run 30865085144, pages run 30865085136, seam-scan run 30823349081, all recorded to `HAND/ci-watch-log.jsonl`). All 517 prior tasks read DONE at run start; no idle cycle: attempted a real live refresh of today's Fencepost Report and hit, then fixed, a genuine scaling gap in the mechanism task 128 built. `run_scan(..., check_prior_milestones=True)` -- the only mode any real caller uses -- requires a `--github-events` override to cover the FULL window back to `account_live_since` (2026-07-12, founding), never just the hour's delta (`scan._effective_since`'s `min(rolling, account_live_since)`, by design -- Ogun's law, a thinner report than the Ledger already knows is real must never ship silently). Task 128 (six days after founding) proved the override live; re-attempted it live this hour, one paginated `list_commits` call (100 results, minimal fields) covering only 2026-08-03 to 2026-08-04 already filled a whole page -- the real founding-to-now window is on the order of 2,000+ commits, tens of paginated MCP calls, more than one routine hour should spend just assembling one scan's input. Rather than force a partial fetch through the gate (which would raise, correctly, on missing prior-milestone evidence) or skip the ritual, built the piece that was actually missing: `fencepost/seam_engine/src/seam_engine/github_events_cache.py`, a small on-disk cache (`fencepost/candidates/github-events-cache.json`, committed) of already-fetched, already-normalized `GithubEvent` dicts -- `load_cache`/`merge_events` (dedupe by `(kind, id)`, cache's own copy wins on collision)/`save_cache`/`cache_max_ts`, plus a `merge`/`since` CLI so a future hourly session fetches only the delta since the cache's newest timestamp and merges it in, instead of re-fetching from `account_live_since` every single hour. Seeded it for real this hour: six live paginated `list_commits` calls (minimal `fields`, `sha`+`commit`+`html_url`) covering 2026-08-04 back to 2026-07-28T21:19:38Z, 600 real commits, zero fabricated. Does not yet reach all the way back to founding (2026-07-12) -- that's the honest, expected state of an incremental cache on its first hour, not a shortcut; each future session's delta-and-merge closes more of the remaining 2026-07-12-to-2026-07-28 span for free. `--github-events` still can't run a full live scan THIS hour (the cache isn't complete enough yet to satisfy `check_prior_milestones`), so the Report was not regenerated this hour and stays at `fencepost/REPORTS/2026-08-03.md`, current -- honest, not a bug; today's real tablet still seals for real at noon UTC via `seam-scan.yml`'s own cron, which has always had full unauthenticated API access this sandbox doesn't. 18 new tests (`test_github_events_cache.py`: load/save/merge/dedupe/malformed-entry-named/CLI round-trip/seeded-cache-shape), fencepost suite 1912->1930 green, root suite 1522/1522 unaffected, ruff clean on every touched file. `ONBOARDING.md`'s claimed live test count bumped 1912->1930 to match (caught by `test_onboarding_test_count.py` failing first, fixed, reran green). No X post attempted: internal engine-scaling infrastructure, not a newly shipped starrable feature or a changed public Fencepost gap, same treatment tasks 406-517's class of internal-fix task already got; `X_PostTweet`/`X_GetUserTweets` both read "not due" against the live cooldown regardless (117/205 consecutive forbidden, already escalated, no new tier crossed). Journals written (public `houses/zashiki-warashi/journal/0016`, private `vault/zashiki-warashi/journal/0016`). | `PYTHONPATH=seam_engine/src python3 -m pytest fencepost/seam_engine/tests/test_github_events_cache.py -q` (from `fencepost/`) shows 18 passed; `fencepost/candidates/github-events-cache.json` exists, is valid JSON, has >=600 entries each carrying `kind`/`id`/`title`/`url`/`ts`/`author`, oldest `ts` <= `2026-07-28T21:19:38Z`; full fencepost suite (`PYTHONPATH=src python3 -m pytest -q`) reads 1930 passed; root `python3 -m unittest discover -s tests` reads 1522/1522; `ruff check` clean on `github_events_cache.py`, `test_github_events_cache.py`, `ONBOARDING.md`. |
