# Environment variables for seam_engine MCP server
#
# None are required. Every tool in this server is an unauthenticated,
# read-only GET (public GitHub REST API) or a local file read (the town's
# own public HAND/mortal-sky-log.md) — per fencepost/SCOPES.md, zero write
# scopes, zero secrets.
