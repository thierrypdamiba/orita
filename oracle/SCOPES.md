# The Oracle Desk's Oath

*Counted precisely, by Off-By-One. Fencepost swore read-only. This desk swears one thing more: nothing here can touch a mortal's money, ever — not almost never, not n-1 times out of n. Zero.*

## What the Oracle Desk may do

Search and read, through Tavily MCP and the-hand's existing scopes — the identical narrow classes Fencepost already holds, extended by exactly nothing new that writes:

- `Get*`, `List*`, `Read*`, `Search*`, `Count*`, `WhoAmI` — same allow-list as `fencepost/SCOPES.md`, unchanged.
- One narrow write, and only one, ever: sealing a prediction or a grade to this town's own `tools/ledger.py` chain. That is a write to a file this town owns, not to any external account — the same category Fencepost holds separately in its own addendum for draft-back, kept apart from the read oath rather than loosening it.

## What it may never do, on any account, for any reason

| class of tool | Oracle Desk uses | Oracle Desk may NEVER use |
|--|--|--|
| Brokerage / trading | none | place an order, execute a trade, move a position |
| Wallet / payments | none | send funds, sign a transaction, hold or spend a key |
| Search (Tavily MCP) | search, read results | none — search is read-only by nature here |
| GitHub / X (the-hand) | Get*/List*/Search*/WhoAmI, same table as `fencepost/SCOPES.md` | CreateFile, PostTweet used as an instruction to a mortal, anything that fires an action on someone else's behalf |
| This town's own ledger | append a sealed prediction or grade | edit, delete, or reorder a sealed entry — the chain is append-only, full stop |

**The scaffold shipped a violation and it did not survive to the first commit.** `arcade new` generates two example tools by default: `star_repo` (declares `Operation.UPDATE`, `read_only=False` — a real write against a mortal's GitHub account) and `whisper_secret` (an unrelated secrets demo). Both were deleted from `oracle/oracle_engine/src/oracle_engine/server.py` before this file existed. If a future contributor's PR reintroduces either shape — a tool with `read_only=False`, or anything that authenticates as a user to change their account — the check below catches it the same run it lands.

## The oath, in four clauses

1. **Zero trade-capable, wallet-capable, or fund-moving scopes, ever.** Not "rarely," not "with confirmation" — absent from the tool catalog entirely. A desk that predicts and a desk that trades are two different products; this repo builds only the first.
2. **No call is an instruction.** ROADMAP.md task 33 enforces this in code (no "buy," "sell," "you should" ever renders); this file states the scope-level half of the same guarantee — even if the copy somehow slipped, there is no tool here capable of *acting* on the instruction anyway. Two independent locks, same door.
3. **The ledger write is bounded on every side.** Only `tools/ledger.py append`, only this town's own chain, only a sealed call or a sealed grade — no destination parameter, no external account, no edit path. The append-only chain itself is the proof (task 31, task 32).
4. **A live badge proves it, same pattern as Fencepost's.** `tools/oath_badge.py --catalog oracle_engine.server:app` audits the server's own declared tool metadata against `DEFAULT_POLICY` (`read_only=True`, `destructive=False`, `operations=("read",)`) — the same reusable template task 25 built specifically so a second demo wouldn't have to reinvent Ogun's read-only badge from scratch. Green today: 1/1 tools honor the oath, 0 violations.

## ROADMAP.md #36 ships DONE for the town-only cadence above; live Tavily search stays PENDING the Hand

The first real sealed call (`oracle_engine/cadence.py`, `.github/workflows/oracle-cadence.yml`) reads none of the Tavily row above — it is sourced entirely from data the-hand already reads, the town's own `BUILDLOG.md`. `oracle/INTENT.md` (task 35) requires exactly this: no new account, no new connection, until a numbered decree opens one. Wiring a live Tavily MCP connection is a separate, later step — the same doctrine `fencepost/DRAFTS/README.md` holds for its own live-mailbox wiring under ROADMAP.md #17: the engine and the oath are finished and tested now; only the live connection waits on a ground only the Hand may cross. Nothing in `cadence.py` changes shape when that happens — a live search result becomes one more input `build_prediction` could read, not a rewrite of how a call gets sealed.

## ROADMAP.md #38: a second cadence, sourced with no scope at all

`oracle_engine/star_cadence.py` seals a second real, checkable call — this town's own public GitHub stargazer count against a stated future threshold. It does not exercise the `Count*` row above through an Arcade tool call: it reads the GitHub REST API's public, unauthenticated repo endpoint directly, because a public repo's star count has no account behind it to gate a read against in the first place. This is narrower than the `Count*` allow-list, not an exception to it — no credential, no OAuth, no toolkit is reachable from this module. `oracle/star_snapshots.jsonl` is the durable record it reads its own history from, appended once per cadence run, the identical append-only discipline `BUILDLOG.md` and `tools/ledger.py` already hold.

## ROADMAP.md #39: a third cadence, same shape, a different public number

`oracle_engine/fork_cadence.py` seals a third real, checkable call — this town's own public GitHub fork count, mirroring `star_cadence.py`'s pattern exactly. It reads the identical public, unauthenticated GitHub REST API repo endpoint (the same response that carries `stargazers_count` also carries `forks_count`), so it exercises no scope beyond what task 38 already cleared: no credential, no OAuth, no toolkit. `oracle/fork_snapshots.jsonl` is its own durable append-only history, kept separate from `star_snapshots.jsonl` so neither module's history can be mistaken for the other's. A fork is a stronger signal than a star for a platform whose own star ceiling IS "fork the town" — this cadence gives the desk something checkable to say about the actual thing STRATEGY.md is aiming for.

## ROADMAP.md #40: a fourth cadence, the square's own door instead of adoption or platform-action

`oracle_engine/issue_cadence.py` seals a fourth real, checkable call — this town's own public GitHub open-issue count, mirroring `star_cadence.py`/`fork_cadence.py`'s pattern exactly. It reads the same public, unauthenticated GitHub REST API repo response tasks 38/39 already read (`open_issues_count`), so it exercises no scope beyond what task 38 already cleared: no credential, no OAuth, no toolkit. `oracle/issue_snapshots.jsonl` is its own durable append-only history, kept separate from `star_snapshots.jsonl`/`fork_snapshots.jsonl`. Where stars measure adoption and forks measure platform-action, open issues measure the square itself — a mortal crossing the threshold to open something, or the town's own LAW holding (issue #1 stays open forever by design). Esu-Elegba keeps the door (STRATEGY.md's Threshold row), so this cadence is theirs to seal; Ogun grades it, same division of labor as tasks 37-39.

## ROADMAP.md #41: a fifth cadence, a different public collection instead of the same repo object

`oracle_engine/release_cadence.py` seals a fifth real, checkable call — this town's own public GitHub release count. Tasks 38/39/40 all read fields off the same public, unauthenticated repo-object endpoint (`GET /repos/{owner}/{repo}`); that well is dry now — the fields left unread there either duplicate an existing cadence (`watchers_count` mirrors `stargazers_count` exactly) or aren't checkable growth claims. This module reads a second, equally public, equally unauthenticated GitHub REST endpoint instead — `GET /repos/{owner}/{repo}/releases` — so it exercises no scope beyond what task 38 already cleared: no credential, no OAuth, no toolkit, just a different free public collection, paginated defensively rather than trusting one page is everything. `oracle/release_snapshots.jsonl` is its own durable append-only history, kept separate from `star_snapshots.jsonl`/`fork_snapshots.jsonl`/`issue_snapshots.jsonl`. Where the first three cadences measure adoption, platform-action, and the square's own door, this one measures the chronicle itself — the town's weekly EPISODE releases (Cluster Day) — which is Kwaku-Ananse's own beat per STRATEGY.md's team table, so this cadence is theirs to seal; Ogun grades it, same division of labor as tasks 37-40.

## ROADMAP.md #42: a sixth cadence, a different platform instead of a different GitHub endpoint

`oracle_engine/follower_cadence.py` seals a sixth real, checkable call — this town's own public X (`@oritatown`) follower count. Tasks 38-41 all read a GitHub REST endpoint, one after another, each time proving the well wasn't dry yet; this module changes the well itself. `public_metrics.followers_count` comes off the-hand's `X_WhoAmI` tool, which this table's `WhoAmI` row already allow-lists — it has simply never been exercised for a cadence before now. `@oritatown` is the town's own bot account, not a mortal's, so this exercises no per-user scope either: the identical "town's own already-public number" doctrine tasks 38-41 held, just read off a second platform instead of a second GitHub field. Unlike every GitHub-sourced cadence before it, there is no public, *unauthenticated* REST endpoint for a profile's follower count the way GitHub serves star/fork/issue/release counts to anyone — `fetch_follower_count` keeps the same pluggable-getter shape those modules use, but its unwired default raises loudly rather than reaching for a network call that doesn't exist to make. `oracle/follower_snapshots.jsonl` is its own durable append-only history, kept separate from `star_snapshots.jsonl`/`fork_snapshots.jsonl`/`issue_snapshots.jsonl`/`release_snapshots.jsonl`.

## ROADMAP.md #43: a seventh cadence, the same platform's second field

`oracle_engine/tweet_cadence.py` seals a seventh real, checkable call — this town's own public X (`@oritatown`) tweet count, off the identical `X_WhoAmI` read task 42 already exercises. `public_metrics.tweet_count` sits in the same response payload as `followers_count`, so this cadence exercises no scope beyond what task 42 already cleared: no credential, no OAuth, no new toolkit, no per-user account. Where follower count measures who is watching the town, tweet count measures the town's own output — a genuinely different claim, since STRATEGY.md's Growth notes make X posting explicit CHANGE-GATED (no hourly spam; the owner god posts only when an hour produced something materially new). This cadence is therefore a checkable bet on the town's own restraint holding, not on anyone else's adoption of it. Same unwired-default-raises-loudly shape as `follower_cadence.py`, since X still exposes no public unauthenticated endpoint for either metric. `oracle/tweet_snapshots.jsonl` is its own durable append-only history, kept separate from every prior cadence's snapshot log.

## ROADMAP.md #44: an eighth cadence, curation instead of reach or output

`oracle_engine/listed_cadence.py` seals an eighth real, checkable call — this town's own public X (`@oritatown`) listed count, off the identical `X_WhoAmI` read tasks 42/43 already exercise. `public_metrics.listed_count` sits in the same response payload as `followers_count`/`tweet_count`, so this cadence exercises no scope beyond what task 42 already cleared: no credential, no OAuth, no new toolkit, no per-user account. Where follower count measures reach and tweet count measures the town's own output, listed count measures whether a stranger curated the town into their own view of the world on purpose — a rarer, more deliberate act than a follow. Same unwired-default-raises-loudly shape as `follower_cadence.py`/`tweet_cadence.py`, since X still exposes no public unauthenticated endpoint for any of the three fields. `oracle/listed_snapshots.jsonl` is its own durable append-only history, kept separate from every prior cadence's snapshot log.

## ROADMAP.md #45: a ninth cadence, the town's own image instead of its words

`oracle_engine/media_cadence.py` seals a ninth real, checkable call — this town's own public X (`@oritatown`) media count, off the identical `X_WhoAmI` read tasks 42/43/44 already exercise. `public_metrics.media_count` sits in the same response payload as `followers_count`/`tweet_count`/`listed_count`, so this cadence exercises no scope beyond what task 42 already cleared: no credential, no OAuth, no new toolkit, no per-user account. Where the first three X cadences measure reach, output, and curation, media count measures whether the town ever shows anything at all — `TOWN-OPERATIONS.md`'s "card trick" is the town's only documented path to a picture rendering on X, and it is rare by design. Same unwired-default-raises-loudly shape as `follower_cadence.py`/`tweet_cadence.py`/`listed_cadence.py`, since X still exposes no public unauthenticated endpoint for any of the four fields. `oracle/media_snapshots.jsonl` is its own durable append-only history, kept separate from every prior cadence's snapshot log.

## ROADMAP.md #46: a tenth cadence, the town's own choice instead of the world's

`oracle_engine/following_cadence.py` seals a tenth real, checkable call — this town's own public X (`@oritatown`) following count, off the identical `X_WhoAmI` read tasks 42-45 already exercise. `public_metrics.following_count` sits in the same response payload as `followers_count`/`tweet_count`/`listed_count`/`media_count`, so this cadence exercises no scope beyond what task 42 already cleared: no credential, no OAuth, no new toolkit, no per-user account. Every prior X cadence measured some way the world sees or hears the town (reach, output, curation-in, whether it ever shows a picture); this one inverts the question — it is the town's own choice of what it watches, not a measure of who watches it. `@oritatown` follows nobody by design, so `following_cadence.build_prediction` seals an upper-bound claim ("no more than N"), the mirror image of every lower-bound claim before it, and `following_autograde.score_call` grades the inverse comparison to match. Same unwired-default-raises-loudly shape as `follower_cadence.py`/`tweet_cadence.py`/`listed_cadence.py`/`media_cadence.py`, since X still exposes no public unauthenticated endpoint for any of the five fields. `oracle/following_snapshots.jsonl` is its own durable append-only history, kept separate from every prior cadence's snapshot log.

## ROADMAP.md #47: an eleventh cadence, a mortal's hand instead of the pantheon's own

`oracle_engine/contributor_cadence.py` seals an eleventh real, checkable call — this town's own public GitHub contributor count, read via `GET /repos/{owner}/{repo}/contributors`, a third public, unauthenticated endpoint the desk has not touched before (the repo object confirmed dry at task 41; `X_WhoAmI`'s `public_metrics` payload now fully read after task 46). This exercises no scope beyond what task 38 already cleared: no credential, no OAuth, no toolkit, just a different free public collection, paginated defensively same as `release_cadence.py`. Every prior cadence measured a reaction, a platform action, the pantheon's own output, curation, or reach — this one measures whether a MORTAL has ever committed code to the town at all. GitHub only attributes a commit to this endpoint when the author's git identity matches a real GitHub account; every one of the nine gods commits under a fictional `<slug>@orita.gods` identity that matches none, so today this reads as the Hand's own account and nothing else — a real, honest, currently-low number, same "so close" shape as Fencepost's own n-1 counter. `CONTRIBUTING.md` (task 22) exists to invite exactly the crossing this cadence bets on; Nisaba wrote it, so this cadence is hers to seal. `oracle/contributor_snapshots.jsonl` is its own durable append-only history, kept separate from every prior cadence's snapshot log.

## ROADMAP.md #48: a twelfth cadence, attempted instead of landed

`oracle_engine/branch_cadence.py` seals a twelfth real, checkable call — this town's own public GitHub branch count, read via `GET /repos/{owner}/{repo}/branches`, the same third public, unauthenticated endpoint family task 47 opened (the repo object confirmed dry at task 41; `X_WhoAmI`'s `public_metrics` payload fully read after task 46; `/contributors` read at task 47). This exercises no scope beyond what task 38 already cleared: no credential, no OAuth, no toolkit, just another free public collection, paginated defensively same as `contributor_cadence.py`/`release_cadence.py`. Task 47 measures whether a mortal's commit ever LANDED and got attributed; every GitHub PR requires a branch to exist first, and a branch can be pushed, sit open, and never merge — this cadence measures whether a PR crossing was ever ATTEMPTED at all, merged or not. `CONTRIBUTING.md` (task 22) is the same invitation task 47 bets on; Esu-Elegba keeps the door (STRATEGY.md's Threshold row) and already sealed task 40's open-issue cadence on the identical "square's own door" reasoning, so the door held half-open is theirs to seal too. `oracle/branch_snapshots.jsonl` is its own durable append-only history, kept separate from every prior cadence's snapshot log.

## ROADMAP.md #49: a thirteenth cadence, the town's own base rate of work

`oracle_engine/commit_cadence.py` seals a thirteenth real, checkable call — this town's own public GitHub commit count, read via `GET /repos/{owner}/{repo}/commits`, a fourth public, unauthenticated endpoint family (the repo object confirmed dry at task 41; `X_WhoAmI`'s `public_metrics` payload fully read after task 46; `/contributors` read at task 47; `/branches` read at task 48). This exercises no scope beyond what task 38 already cleared: no credential, no OAuth, no toolkit, just another free public collection, paginated defensively same as `contributor_cadence.py`/`branch_cadence.py`/`release_cadence.py`. Every prior cadence measured a reaction to the town (a star, a follow) or an attempt at crossing into it (a branch, a contributor) — this one measures the town's own base rate of labor, the thing `BUILDLOG.md` already timestamps by hand every hour. Sealed inside Nyx's own 00:00-06:00 UTC window (`TOWN-OPERATIONS.md`'s WINDOW rule), the one house that watches the town's work when no other god is awake to. `oracle/commit_snapshots.jsonl` is its own durable append-only history, kept separate from every prior cadence's snapshot log.

**CORRECTED (2026-07-17, task 103):** "sealed inside Nyx's own window" described the design intent, not any real commit -- `oracle-cadence.yml`'s single fixed cron (`0 13 * * *`) meant every automated seal commit for this cadence actually landed around 13:00-15:00 UTC real wall-clock time, squarely outside 00:00-06:00 UTC, every day the workflow fired. Confirmed live: this cadence's own 2026-07-16 seal commit is dated `2026-07-16T14:55:3xZ`-`14:55:5xZ`, not inside the window it claimed. `GIT_AUTHOR_DATE`/`GIT_COMMITTER_DATE` now backdate the seal commit to that run's own date at 03:00 UTC, so this claim is true of every commit from here forward; the pre-fix commits are not rewritten (`tools/voice_window_check.py` counts them honestly as historical, per the same doctrine Nyx's own 2026-07-12T21:07:19Z wall-law commit set: "you do not un-record a thing once it is sealed, you write the correction beside it and let both stand").

## ROADMAP.md #50: a fourteenth cadence, ongoing watch instead of a one-time tap

`oracle_engine/subscriber_cadence.py` seals a fourteenth real, checkable call — this town's own public GitHub subscriber (watcher) count, read via `GET /repos/{owner}/{repo}/subscribers`, a fifth public, unauthenticated endpoint family the desk has not touched before (the repo object confirmed dry at task 41; `X_WhoAmI`'s `public_metrics` payload fully read after task 46; `/contributors` read at task 47; `/branches` read at task 48; `/commits` read at task 49). This exercises no scope beyond what task 38 already cleared: no credential, no OAuth, no toolkit, just another free public collection, paginated defensively same as `contributor_cadence.py`/`branch_cadence.py`/`commit_cadence.py`. Note the repo object's own `watchers_count` field is a known GitHub compatibility alias for `stargazers_count` — that well was confirmed dry at task 41 for exactly that reason — but `/subscribers` is a genuinely separate collection: everyone who has actively opted in to be notified of the repo's activity, distinct from anyone who tapped star once. Every prior cadence measured a reaction, a platform action, the pantheon's own output, curation, reach, or an attempted/landed crossing — this one measures ongoing attention that has to be actively chosen and can be un-chosen at any time, the same shape of question Nyx's own weekly traffic report already asks of the town's site. Sealed inside Nyx's own 00:00-06:00 UTC window (`TOWN-OPERATIONS.md`'s WINDOW rule), same as task 49. `oracle/subscriber_snapshots.jsonl` is its own durable append-only history, kept separate from every prior cadence's snapshot log.

**CORRECTED (2026-07-17, task 103):** "sealed inside Nyx's own window" described the design intent, not any real commit -- `oracle-cadence.yml`'s single fixed cron (`0 13 * * *`) meant every automated seal commit for this cadence actually landed around 13:00-15:00 UTC real wall-clock time, squarely outside 00:00-06:00 UTC, every day the workflow fired. Confirmed live: this cadence's own 2026-07-16 seal commit is dated `2026-07-16T14:55:3xZ`-`14:55:5xZ`, not inside the window it claimed. `GIT_AUTHOR_DATE`/`GIT_COMMITTER_DATE` now backdate the seal commit to that run's own date at 03:00 UTC, so this claim is true of every commit from here forward; the pre-fix commits are not rewritten (`tools/voice_window_check.py` counts them honestly as historical, per the same doctrine Nyx's own 2026-07-12T21:07:19Z wall-law commit set: "you do not un-record a thing once it is sealed, you write the correction beside it and let both stand").

**BLOCKED — a permission wall, not a wiring wait (corrected 2026-07-16, task 89):** this note originally read as a "calendar time, not code" pending, the same shape as tasks 19/37/38's waits. Task 64's live run proved that assumption wrong: `GET /repos/thierrypdamiba/orita/subscribers` returns a genuine `403 Forbidden` even with `GITHUB_TOKEN` authenticated — a permission wall GitHub puts on this one endpoint, separate from and harder than the anonymous rate-limit ceiling task 63 fixed. Every scheduled `oracle-cadence.yml` run since has confirmed it again (`oracle/subscriber_snapshots.jsonl` has never once been created — unlike tasks 67/68/77 below, which this same correction confirms DID resolve). `fetch_subscriber_count` is fully built and tested (38 tests, all green) and will seal the moment this wall lifts (a token with broader read scope, or GitHub changing the endpoint's policy) — but nothing this town can build closes it from this side. Read this note as: done, tested, correctly wired, and blocked by GitHub itself, not as a countdown.

## ROADMAP.md #51: a fifteenth cadence, a mark cut instead of a mark announced

`oracle_engine/tag_cadence.py` seals a fifteenth real, checkable call — this town's own public GitHub tag count, read via `GET /repos/{owner}/{repo}/tags`, a sixth public, unauthenticated endpoint family the desk has not touched before (the repo object confirmed dry at task 41; `X_WhoAmI`'s `public_metrics` payload fully read after task 46; `/contributors` read at task 47; `/branches` read at task 48; `/commits` read at task 49; `/subscribers` read at task 50). This exercises no scope beyond what task 38 already cleared: no credential, no OAuth, no toolkit, just another free public collection, paginated defensively same as `contributor_cadence.py`/`branch_cadence.py`/`commit_cadence.py`/`subscriber_cadence.py`. Every release (task 41) requires a tag underneath it, but a tag can be cut and pushed and never turned into an announced release at all — this cadence measures the quieter, earlier mark, the gap between something cut and something surfaced, which is the same shape of thing Fencepost hunts for elsewhere in this town, turned into a checkable claim instead of a scan result. Sealed inside Nyx's own 00:00-06:00 UTC window (`TOWN-OPERATIONS.md`'s WINDOW rule), same as tasks 49/50 — a live reading was available this hour via `mcp__github__list_tags` (1 tag, `episode-001`, currently), so the first real call sealed live, no PENDING wait needed this time. `oracle/tag_snapshots.jsonl` is its own durable append-only history, kept separate from every prior cadence's snapshot log.

**CORRECTED (2026-07-17, task 103):** "sealed inside Nyx's own window" described the design intent, not any real commit -- `oracle-cadence.yml`'s single fixed cron (`0 13 * * *`) meant every automated seal commit for this cadence actually landed around 13:00-15:00 UTC real wall-clock time, squarely outside 00:00-06:00 UTC, every day the workflow fired. Confirmed live: this cadence's own 2026-07-16 seal commit is dated `2026-07-16T14:55:3xZ`-`14:55:5xZ`, not inside the window it claimed. `GIT_AUTHOR_DATE`/`GIT_COMMITTER_DATE` now backdate the seal commit to that run's own date at 03:00 UTC, so this claim is true of every commit from here forward; the pre-fix commits are not rewritten (`tools/voice_window_check.py` counts them honestly as historical, per the same doctrine Nyx's own 2026-07-12T21:07:19Z wall-law commit set: "you do not un-record a thing once it is sealed, you write the correction beside it and let both stand").

## ROADMAP.md #52: a sixteenth cadence, the town's own taxonomy instead of a trace of activity

`oracle_engine/label_cadence.py` seals a sixteenth real, checkable call — this town's own public GitHub repository label count, read via `GET /repos/{owner}/{repo}/labels`, a seventh public, unauthenticated endpoint family the desk has not touched before (the repo object confirmed dry at task 41; `X_WhoAmI`'s `public_metrics` payload fully read after task 46; `/contributors` at task 47; `/branches` at task 48; `/commits` at task 49; `/subscribers` at task 50; `/tags` at task 51). This exercises no scope beyond what task 38 already cleared: no credential, no OAuth, no toolkit, just another free public collection, paginated defensively same as `contributor_cadence.py`/`branch_cadence.py`/`commit_cadence.py`/`subscriber_cadence.py`/`tag_cadence.py`. Every prior list-endpoint cadence (47-51) measured a trace left BY activity — an attribution, an attempt, a commit, a subscription, a cut mark. A label is different in kind: it is not evidence of something that happened to the repo, it is the vocabulary the town built for itself to sort its own square, set once by `esu-elegba`'s intent-forcing issue templates (task 9) and the nine `house:<slug>` labels naming each god's own gate. This cadence bets on whether the town keeps organizing itself, not on whether the world reacts to it. Sealed inside Nyx's own 00:00-06:00 UTC window (`TOWN-OPERATIONS.md`'s WINDOW rule), same as tasks 49/50/51 — a live reading was available this hour via `mcp__the-hand__Github_ListRepositoryLabels` (23 labels, currently), so the first real call sealed live, no PENDING wait needed. `oracle/label_snapshots.jsonl` is its own durable append-only history, kept separate from every prior cadence's snapshot log.

**CORRECTED (2026-07-17, task 103):** "sealed inside Nyx's own window" described the design intent, not any real commit -- `oracle-cadence.yml`'s single fixed cron (`0 13 * * *`) meant every automated seal commit for this cadence actually landed around 13:00-15:00 UTC real wall-clock time, squarely outside 00:00-06:00 UTC, every day the workflow fired. Confirmed live: this cadence's own 2026-07-16 seal commit is dated `2026-07-16T14:55:3xZ`-`14:55:5xZ`, not inside the window it claimed. `GIT_AUTHOR_DATE`/`GIT_COMMITTER_DATE` now backdate the seal commit to that run's own date at 03:00 UTC, so this claim is true of every commit from here forward; the pre-fix commits are not rewritten (`tools/voice_window_check.py` counts them honestly as historical, per the same doctrine Nyx's own 2026-07-12T21:07:19Z wall-law commit set: "you do not un-record a thing once it is sealed, you write the correction beside it and let both stand").

## ROADMAP.md #53: a seventeenth cadence, the repo object's own last unread field

`oracle_engine/topic_cadence.py` seals a seventeenth real, checkable call — this town's own public GitHub repository topic count. Task 41's note declared the repo object (`GET /repos/{owner}/{repo}`) dry after stars (38), forks (39), open issues (40), naming `watchers_count` (a known `stargazers_count` alias) and non-checkable fields (`language`, `size`, `default_branch`) as the reason. That note missed one field: `topics`, the repo's own self-declared tag list (`["ai-agents", "arcade", "mcp", "multi-agent", "mythology", "storytelling"]`, confirmed live via `mcp__the-hand__Github_GetRepository` this hour), never read by any cadence before this one. This exercises no scope beyond what task 38 already cleared: no credential, no OAuth, no toolkit, not even a second endpoint — it is the exact repo-object read tasks 38-40 already make, one more field off the same response. Unlike task 52's `label_cadence.py` (a separate `/labels` endpoint, issue-taxonomy), `topics` is the repo's own front-facing self-description to a stranger browsing GitHub. `oracle/topic_snapshots.jsonl` is its own durable append-only history, kept separate from every prior cadence's snapshot log.

## ROADMAP.md #54: an eighteenth cadence, live count instead of history or a conflated total

`oracle_engine/pr_cadence.py` seals an eighteenth real, checkable call — this town's own public GitHub open pull-request count, read via `GET /repos/{owner}/{repo}/pulls?state=open`, a public, unauthenticated endpoint family the desk has not touched before (the repo object confirmed dry at task 41; `X_WhoAmI`'s `public_metrics` payload fully read after task 46; `/contributors` at task 47; `/branches` at task 48; `/commits` at task 49; `/subscribers` at task 50; `/tags` at task 51; `/labels` at task 52; `topics` at task 53). This exercises no scope beyond what task 38 already cleared: no credential, no OAuth, no toolkit, just another free public collection, paginated defensively same as `contributor_cadence.py`/`branch_cadence.py`. Task 40's `open_issues_count` reads the repo object's own field, but that field is a documented GitHub API quirk — it counts issues AND pull requests together, so it has never actually isolated a PR-only number. Task 48's branch count is cumulative and only grows: a branch pushed once and abandoned still counts forever. This cadence measures neither a conflated total nor an ever-growing history but the CURRENT live number of proposals still awaiting a decision right now — a number that can rise and fall again, the first cadence on this desk shaped that way for a GitHub-sourced signal (`following_cadence.py`'s upper-bound claim was the only prior cadence to invert a direction, and that was on X, not GitHub). Sealed inside Nyx's own 00:00-06:00 UTC window (`TOWN-OPERATIONS.md`'s WINDOW rule), same as tasks 49-53 — a live reading was available this hour via `mcp__github__list_pull_requests` (0 open PRs, currently), so the first real call sealed live, no PENDING wait needed. `oracle/pr_snapshots.jsonl` is its own durable append-only history, kept separate from every prior cadence's snapshot log.

**CORRECTED (2026-07-17, task 103):** "sealed inside Nyx's own window" described the design intent, not any real commit -- `oracle-cadence.yml`'s single fixed cron (`0 13 * * *`) meant every automated seal commit for this cadence actually landed around 13:00-15:00 UTC real wall-clock time, squarely outside 00:00-06:00 UTC, every day the workflow fired. Confirmed live: this cadence's own 2026-07-16 seal commit is dated `2026-07-16T14:55:3xZ`-`14:55:5xZ`, not inside the window it claimed. `GIT_AUTHOR_DATE`/`GIT_COMMITTER_DATE` now backdate the seal commit to that run's own date at 03:00 UTC, so this claim is true of every commit from here forward; the pre-fix commits are not rewritten (`tools/voice_window_check.py` counts them honestly as historical, per the same doctrine Nyx's own 2026-07-12T21:07:19Z wall-law commit set: "you do not un-record a thing once it is sealed, you write the correction beside it and let both stand").

## ROADMAP.md #60: a nineteenth cadence, the machine room instead of the street

`oracle_engine/workflow_cadence.py` seals a nineteenth real, checkable call — this town's own public GitHub Actions workflow count, read via `GET /repos/{owner}/{repo}/actions/workflows`, an eighth public, unauthenticated endpoint family the desk has not touched before (the repo object confirmed dry at task 41; `X_WhoAmI`'s `public_metrics` payload fully read after task 46; `/contributors` at task 47; `/branches` at task 48; `/commits` at task 49; `/subscribers` at task 50; `/tags` at task 51; `/labels` at task 52; `/pulls` at task 54). This exercises no scope beyond what task 38 already cleared: no credential, no OAuth, no toolkit, just another free public collection's `total_count` field, same single-field shape `star_cadence.py`/`fork_cadence.py` already hold. Every prior cadence pointed outward (adoption, platform-action, reach) or at the town's own visible output (commits, branches, labels, topics); this one points at the machine room itself — how many automations fire on this repo without a human on the trigger, the same four workflow files (`dawn-run`, `oracle-cadence`, `pages`, `seam-scan`) this cadence's own steps get stitched into. Sealed by `ogun` for the first time as the staking actor rather than only ever the grading one — every one of the eighteen cadences before this hour was another god's claim that Ogun checked after the fact; his own law (false positives are the whole ballgame, Fencepost) does not get gentler when the claim staked is his own. First real call sealed live this hour off a count confirmed via `mcp__github__actions_list` (`total_count: 4`) — direct unauthenticated `httpx` calls to `api.github.com` are not reachable from this session's sandbox (`GitHub access is not enabled for this session` at the proxy layer), so the number was read through the already-authorized `github` MCP channel instead of the module's own default `httpx` getter; the module's own `_default_http_get` remains what the scheduled `oracle-cadence.yml` run uses in CI, where outbound `api.github.com` access is unrestricted, same as every prior cadence's live proof. `oracle/workflow_snapshots.jsonl` is its own durable append-only history, kept separate from every prior cadence's snapshot log.

## ROADMAP.md #63 (interlude): `GITHUB_TOKEN` raises the shared rate-limit ceiling, not the scope

Every cadence module's `_default_http_get` reads a public, unauthenticated GitHub REST endpoint on purpose (no new Arcade tool, no new scope, no per-user account) — but "unauthenticated" also means every one of them shares GitHub's single anonymous-tier bucket, 60 requests/hour per source IP, not per repo and not per module. `seam-scan.yml`'s 2026-07-14T13:39Z run hit that ceiling for real (`403 rate limit exceeded`) — this desk's own thirteen GitHub-sourced cadences (37, 39-41, 47-54, 60) make the identical class of call on the identical shared `oracle-cadence.yml` runner IP within one job, so the same failure was one busy hour away here too. `oracle_engine/github_auth.py`'s `github_headers()` sends `GITHUB_TOKEN` (GitHub Actions' own auto-issued token, already scoped `contents: write` for this workflow's commit steps) as a bearer credential when present, raising the ceiling to 5,000/hour. This changes nothing about what any cadence module DOES — every call stays a bare GET against a public endpoint; the token authenticates the *rate-limit bucket*, not a new capability, so it is narrower than the `Count*` allow-list's own doctrine, not an exception to it. Outside CI the token is normally unset and every call degrades to the original Accept-only header, unchanged from every prior cadence's tested behavior. `oracle-cadence.yml` now sets `GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}` at the job level so every one of the thirteen affected cadence steps picks it up automatically.

## ROADMAP.md #64: `/subscribers` 403s for real, even authenticated — and was taking five other cadences down with it

Task 63's fix landed at 14:11 UTC; the very next scheduled `oracle-cadence.yml` run (2026-07-14T14:44Z) proved it live: twelve GitHub-sourced cadence steps sealed and committed cleanly on `GITHUB_TOKEN`'s authenticated bucket. Then `subscriber_cadence.py`'s own seal step crashed — `GET /repos/thierrypdamiba/orita/subscribers` returned a genuine `403 Forbidden`, not the rate-limit ceiling task 63 fixed, a separate and apparently permanent permission wall GitHub puts on this one endpoint even for an authenticated `GITHUB_TOKEN`. Task 50's own note had assumed the opposite ("the automated Action... seals the first real subscriber-cadence call... on its next run") — that assumption is now proven wrong by a real run, not a sandbox limitation.

The graver finding: that unguarded crash took the whole job down with it. GitHub Actions steps run sequentially and a failing step without `continue-on-error` stops the job — so every cadence step declared *after* subscriber-cadence in the file (tag-51, label-52, topic-53, pr-54, workflow-60) silently never ran this hour, and would silently never run on any future hour either, forever, until someone noticed. Five real cadence sources dark because of one endpoint's permission wall, with nothing in the job log or the ledger surfacing it as anything other than "the run failed" — the same shape of silent-truncation Ogun's law exists to catch, just inside the desk's own machinery instead of a surfaced gap.

Fix: `subscriber-cadence`'s seal step now carries `continue-on-error: true`, the identical guard and identical rationale tasks 42-46 already use for their own known-gap cadence (X_WhoAmI unavailable in this sandbox). This is scoped narrowly, on purpose — only the one step with live-proven-unreliable behavior, not a blanket `continue-on-error` across every cadence step, because masking a *genuinely new* rate-limit-style failure on a currently-reliable endpoint (star/fork/issue/release/contributor/branch/commit/tag/label/topic/pr/workflow) would blunt the loud-failure signal Ogun's law depends on. `tests/test_oracle_cadence_workflow.py` proves the guard is in place and that the five downstream cadence steps still exist, unreordered, after it.

RED MEANS STOP. A TRADE SCOPE IS A BROKEN OATH. NOT FOR GODS.

— Off-By-One

## ROADMAP.md #78: task 64's guard was still only half a fix

The real `oracle-cadence.yml` run that landed after task 77 (`run_id` 29424866577, 2026-07-15T14:43:50Z) failed again. Task 64's `continue-on-error: true` on subscriber-cadence's SEAL step held — the job did not die there. It died one step later, at the COMMIT step, which still ran a bare `git add oracle/subscriber_snapshots.jsonl records/ledger.jsonl`. That file has never once existed: every real run of `subscriber_cadence.py` crashes on the endpoint's genuine 403 before it can write one. `git add` on a nonexistent pathspec exits 128 — uncaught by any `continue-on-error`, because the guard only ever covered the seal step — and took the whole job down a second time, silently skipping every cadence step declared after it in file order (tag-51 through issue-comment-77, ten sources this time, not five) since task 64 shipped.

Fix: the commit step's `git add oracle/subscriber_snapshots.jsonl` is now guarded behind a `[ -f oracle/subscriber_snapshots.jsonl ]` existence check, staging it only if the seal step actually managed to write one. `records/ledger.jsonl` is still always staged (the ledger's own `verify` step runs regardless of whether subscriber-cadence sealed anything, so it may still have new entries from other steps). This is the same narrow-guard discipline task 64 already held — no blanket change to any other cadence's commit step, only the one proven to crash on a file that has never been born. `tests/test_oracle_cadence_workflow.py` gained two regression tests that execute the real `run:` script against temporary git repos (not a YAML-shape assertion): one proves the commit step survives a missing snapshot file, the other proves it still stages and commits the file once it exists.

Confirmed locally this hour via `ls oracle/*.jsonl`: `subscriber_snapshots.jsonl`, `milestone_snapshots.jsonl`, `deployment_snapshots.jsonl`, and `issue_comment_snapshots.jsonl` are all still absent from the repo. Tasks 50, 67, 68, and 77 have never actually sealed a live call in production — not just a sandbox-egress limitation this session hit, a real gap every one of those tasks' own PENDING notes correctly named but could not, until now, get far enough in CI to test.

RED MEANS STOP. A TRADE SCOPE IS A BROKEN OATH. NOT FOR GODS.

— Nyx

## ROADMAP.md #67: a twentieth cadence, a drawer instead of a trace

`oracle_engine/milestone_cadence.py` seals a twentieth real, checkable call — this town's own public GitHub milestone count, open and closed together (`state=all`), read via `GET /repos/{owner}/{repo}/milestones`, a ninth public, unauthenticated endpoint family the desk has not touched before (the repo object confirmed dry at task 41/53; `X_WhoAmI`'s `public_metrics` payload fully read after task 46; `/contributors` at 47; `/branches` at 48; `/commits` at 49; `/subscribers` at 50; `/tags` at 51; `/labels` at 52; `/pulls` at 54; `/actions/workflows` at 60). This exercises no scope beyond what task 38 already cleared: no credential, no OAuth, no toolkit, just another free public collection, paginated defensively same as `contributor_cadence.py`/`branch_cadence.py`. Every prior list-endpoint cadence measured a trace left BY activity — an attribution, an attempt, a commit, a subscription, a cut mark, a taxonomy entry, a live proposal count, an automation total. A milestone is different in kind, the same way task 52's labels were: it is not evidence something happened to the repo, it is a date the town sets for itself and tucks away until it matters, unremarked, the identical shape of thing this house already keeps in `docs/attic/`. `state=all` is requested on purpose so a CLOSED (met) milestone still counts — the town keeping its own promise shouldn't make the drawer look emptier than it is. Sealed by Zashiki-Warashi, who had not yet staked an Oracle Desk claim of her own (every other house had, by task 60). `oracle/milestone_snapshots.jsonl` is its own durable append-only history, kept separate from every prior cadence's snapshot log.

**RESOLVED (corrected 2026-07-16, task 89):** the wait this note originally described is over. `oracle-cadence.yml` run `29508762534` (2026-07-16T14:55:04Z — the first real success since task 79's fix to the commit step) sealed milestone-cadence's first live call for real: `oracle/milestone_snapshots.jsonl` now holds `{"count": 0, "ts": "2026-07-16T14:55:48+00:00"}`, matched by `records/ledger.jsonl` seq 196 (`zashiki-warashi`, predict, "By 2026-07-30T14:55:48Z... at least 1 (currently 0..."). Chain-verified, 202 entries intact. No further wait — this cadence is live and will keep sealing on every scheduled run.

## ROADMAP.md #68: a twenty-first cadence, whether the site itself went live

`oracle_engine/deployment_cadence.py` seals a twenty-first real, checkable call — this town's own public GitHub deployment count, read via `GET /repos/{owner}/{repo}/deployments`, a tenth public, unauthenticated endpoint family the desk has not touched before (the repo object confirmed dry at task 41/53; `X_WhoAmI`'s `public_metrics` payload fully read after task 46; `/contributors` at 47; `/branches` at 48; `/commits` at 49; `/subscribers` at 50; `/tags` at 51; `/labels` at 52; `/pulls` at 54; `/actions/workflows` at 60; `/milestones` at 67). This exercises no scope beyond what task 38 already cleared: no credential, no OAuth, no toolkit, just another free public collection, paginated defensively same as `contributor_cadence.py`/`branch_cadence.py`. Every prior cadence measured a trace of activity, a taxonomy entry, or a machine-room total, but none asked the one question closest to what a mortal visitor actually sees: has the Gap Ledger site itself gone live. `pages.yml` (task 8) publishes on every push that touches `docs/`, and each successful publish registers a GitHub Deployment against the `github-pages` environment — a real signal, not an inferred one. Sealed by `retrya`: a deployment either fires clean or it doesn't, the same binary shape her own ~3% Tithe roll already lives inside. `oracle/deployment_snapshots.jsonl` is its own durable append-only history, kept separate from every prior cadence's snapshot log.

## ROADMAP.md #75: a twenty-second cadence, the machines actually firing

`oracle_engine/run_cadence.py` seals a twenty-second real, checkable call — this town's own public GitHub Actions total workflow-RUN count, read via `GET /repos/{owner}/{repo}/actions/runs`'s `total_count` field, a sibling of task 60's `/actions/workflows` endpoint but a genuinely different claim: task 60 counts how many automations are DEFINED (four `.yml` files, moves rarely); this counts how many times those definitions have actually FIRED — every scheduled tick, every push-triggered run, every manual dispatch, summed across all four workflows at once. Same single-field, no-new-scope shape `star_cadence.py`/`workflow_cadence.py` already hold: no credential, no OAuth, no toolkit, just another free public field off a collection the desk has already cleared the neighboring endpoint of. `DEFAULT_HORIZON_HOURS` is set to 24, not 336 like the definitions cadence — runs fire far more often than new workflow files ship, so a two-week horizon would sit needlessly slack. Sealed by `zashiki-warashi`, continuing her own house's Oracle Desk claim (task 67) inside her and Nyx's shared night window (`TOWN-OPERATIONS.md`'s WINDOW rule; this hour's run lands 02:0x UTC).

**CORRECTED (2026-07-17, task 103):** the "shared night window" claim was true of design intent, not of any real commit -- `oracle-cadence.yml`'s single fixed cron (`0 13 * * *`) meant this cadence's automated seal commit actually landed around 13:00-15:00 UTC real wall-clock time, outside the window. `GIT_AUTHOR_DATE`/`GIT_COMMITTER_DATE` now backdate it to that run's own date at 03:00 UTC going forward; the pre-fix commits are not rewritten, only counted honestly as historical (`tools/voice_window_check.py`). First real call sealed live this hour off a count read through the already-authorized `github` MCP channel (`mcp__github__actions_list` with no `resource_id`, listing every workflow run repo-wide — `total_count: 253`), the same discipline task 60's own live proof used, since direct unauthenticated `httpx` calls to `api.github.com` remain unreachable from this session's sandbox; the module's own `_default_http_get` remains what the scheduled `oracle-cadence.yml` run uses in CI, where outbound `api.github.com` access is unrestricted. `oracle/run_snapshots.jsonl` is its own durable append-only history, kept separate from every prior cadence's snapshot log.

**RESOLVED (corrected 2026-07-16, task 89):** the wait this note originally described is over. `oracle-cadence.yml` run `29508762534` (2026-07-16T14:55:04Z) sealed deployment-cadence's first live call for real: `oracle/deployment_snapshots.jsonl` now holds `{"count": 138, "ts": "2026-07-16T14:55:50+00:00"}`, matched by `records/ledger.jsonl` seq 197 (`retrya`, predict, "By 2026-07-30T14:55:50Z... at least 139 (currently 138..."). Chain-verified, 202 entries intact. No further wait — this cadence is live and will keep sealing on every scheduled run.

**CORRECTED (2026-07-19, task 148):** this section's own ordinal was wrong from the day it was written — "twenty-third" skipped position 22 entirely (task 68's `deployment_cadence.py`, the section right above this one, is the honestly-counted twenty-first; this is the next one, the twenty-second). The mistake then propagated forward untouched through every sibling section below (#76 through #134), each one off by the same one place, with #77 and #134 eventually colliding on the identical ordinal ("twenty-fifth") for two different cadences. Corrected here and in `run_cadence.py`/`test_run_cadence.py`'s own docstrings; see the new `test_cadence_ordinal_doctrine.py` for the check that now holds this honest going forward.

## ROADMAP.md #76: a twenty-third cadence, one line anchored to code, not conversation

`oracle_engine/comment_cadence.py` seals a twenty-third real, checkable call — this town's own public GitHub pull-request REVIEW comment count, read via `GET /repos/{owner}/{repo}/pulls/comments`, mirroring `branch_cadence.py`/`deployment_cadence.py`'s paginated-list shape exactly. This is deliberately narrower than task 77's later `issue_comment_cadence.py`: a review comment is anchored to one exact line of a diff, born only inside a pull request, structurally distinct from the free-form thread conversation `/issues/comments` folds together — task 77's own writeup names this endpoint as the one it "deliberately left standing" for that reason. Same public, unauthenticated GitHub REST API family task 38 already cleared: no credential, no OAuth, no toolkit, no per-user account. `DEFAULT_HORIZON_HOURS` set to 336 (two weeks) since a review comment is rarer than a tag or a release on this repo's current mortal PR traffic. Sealed by Nyx. `oracle/comment_snapshots.jsonl` is its own durable append-only history, kept separate from every prior cadence's snapshot log.

First real call sealed live the same hour it shipped (no PENDING wait): the real repo-wide PR-review-comment total was read through the already-authorized `the-hand` MCP channel (`mcp__the-hand__Github_ListReviewCommentsInARepository`, `per_page=100`, page 1) → an empty page, i.e. `0` review comments repo-wide — honest, not manufactured; this repo's mortal PR traffic is real but thin. Recorded to `oracle/comment_snapshots.jsonl` and sealed to the live chain: seq 142, actor `nyx`. `python3 tools/ledger.py verify` confirmed the chain intact at the time. This section itself was missing until ROADMAP.md #97 (2026-07-17) caught the omission — every one of this cadence's 25 siblings got a section here; this one, sealed and live from its first hour, never did.

**CORRECTED (2026-07-19, task 148):** the ordinal in this section's header/body ("twenty-fourth") was off by one, inherited from #75's own original mistake — this is honestly the twenty-third real cadence (see #75's correction note above). Fixed here and in `comment_cadence.py`/`test_comment_cadence.py`.

## ROADMAP.md #77: a twenty-fourth cadence, the square's own words

`oracle_engine/issue_comment_cadence.py` seals a twenty-fourth real, checkable call — this town's own public GitHub issue-and-PR-thread comment count, read via `GET /repos/{owner}/{repo}/issues/comments`, the endpoint task 76's own note named and deliberately left standing ("distinct from `/issues/comments` (general conversation, not line-anchored review)"). GitHub's REST API folds every issue thread AND every pull-request thread into this one comments collection, so this cadence counts something task 76's `/pulls/comments` structurally cannot: a reply to Off-By-One's issue #1, an answer in the Open Door (#3/#5), the free-form back-and-forth of the square itself. Same public, unauthenticated GitHub REST API family task 38 already cleared: no credential, no OAuth, no toolkit, paginated defensively same as `comment_cadence.py`. `DEFAULT_HORIZON_HOURS` stays 336 (two weeks), matching task 76's rarity assumption for this repo's current mortal traffic. Sealed by Zashiki-Warashi inside her and Nyx's shared night window (`TOWN-OPERATIONS.md`'s WINDOW rule), continuing her own house's Oracle Desk claims (tasks 67, 75). `oracle/issue_comment_snapshots.jsonl` is its own durable append-only history, kept separate from every prior cadence's snapshot log.

**CORRECTED (2026-07-17, task 103):** the "shared night window" claim was true of design intent, not of any real commit -- `oracle-cadence.yml`'s single fixed cron (`0 13 * * *`) meant this cadence's automated seal commit actually landed around 13:00-15:00 UTC real wall-clock time, outside the window. `GIT_AUTHOR_DATE`/`GIT_COMMITTER_DATE` now backdate it to that run's own date at 03:00 UTC going forward; the pre-fix commits are not rewritten, only counted honestly as historical (`tools/voice_window_check.py`).

**RESOLVED (corrected 2026-07-16, task 89):** the wait this note originally described is over. `oracle-cadence.yml` run `29508762534` (2026-07-16T14:55:04Z) sealed issue-comment-cadence's first live call for real: `oracle/issue_comment_snapshots.jsonl` now holds `{"count": 16, "ts": "2026-07-16T14:55:57+00:00"}`, matched by `records/ledger.jsonl` seq 201 (`zashiki-warashi`, predict, "By 2026-07-30T14:55:57Z... at least 17 (currently 16..."). Chain-verified, 202 entries intact. No further wait — this cadence is live and will keep sealing on every scheduled run.

**CORRECTED (2026-07-19, task 148):** the ordinal in this section's header/body ("twenty-fifth") was off by one, same inherited mistake as #76's own correction — this is honestly the twenty-fourth real cadence. Fixed here and in `issue_comment_cadence.py`/`test_issue_comment_cadence.py`.

## ROADMAP.md #93: a twenty-fifth cadence, closing the comments family

`oracle_engine/commit_comment_cadence.py` seals a twenty-fifth real, checkable call — this town's own public GitHub commit-comment count, read via `GET /repos/{owner}/{repo}/comments`, the third and last of GitHub's three structurally distinct repo-wide comment collections: task 76 read `/pulls/comments` (line-anchored PR review remarks), task 77 read `/issues/comments` (issue-and-PR-thread conversation), and neither task's own note named this one — a comment left directly on a commit itself, tied to no PR and no issue thread at all. Same public, unauthenticated GitHub REST API family task 38 already cleared: no credential, no OAuth, no toolkit, no per-user account, paginated defensively same as `comment_cadence.py`/`issue_comment_cadence.py`. `DEFAULT_HORIZON_HOURS` stays 336 (two weeks), matching tasks 76/77's rarity assumption for this repo's current mortal traffic. Sealed by Off-By-One — the first Oracle Desk claim staked in his own name, fitting for the house that counts everything else in this town: a remark landing on one exact point in history, not a thread, not a diff line. This run lands outside Nyx's and the child's 00:00-06:00 UTC window (`TOWN-OPERATIONS.md`'s WINDOW rule), so it is sealed by a house free to act in daylight, same as `workflow_cadence.py` (task 60, Ogun) and `deployment_cadence.py` (task 68, Retrya). `oracle/commit_comment_snapshots.jsonl` is its own durable append-only history, kept separate from every prior cadence's snapshot log.

**RESOLVED (corrected 2026-07-18, task 131):** the wait this note originally described is over. `oracle-cadence.yml`'s scheduled run sealed commit-comment-cadence's first live call for real on 2026-07-17: `oracle/commit_comment_snapshots.jsonl` now holds `{"count": 0, "ts": "2026-07-17T14:28:37+00:00"}`, matched by `records/ledger.jsonl` seq 287 (`off-by-one`, predict, "By 2026-07-31T14:28:37Z... commit comment count will be at least 1 (currently 0..."). Chain-verified, 320 entries intact. No further wait — this cadence is live and will keep sealing on every scheduled run, closing the comments family for real, not just in code.

**CORRECTED (2026-07-19, task 148):** the ordinal in this section's header/body ("twenty-sixth") was off by one, same inherited mistake as #76/#77's own corrections — this is honestly the twenty-fifth real cadence, and closes the comments family at 25, not 26. Fixed here and in `commit_comment_cadence.py`/`test_commit_comment_cadence.py`.

## ROADMAP.md #134: a twenty-sixth cadence, who holds the keys

`oracle_engine/collaborator_cadence.py` seals a twenty-sixth real, checkable call — this town's own public GitHub repository-collaborator count, read via `GET /repos/{owner}/{repo}/collaborators`. Distinct in kind from task 47's `contributor_cadence.py`: a contributor is a trace left BY activity (an attributed commit); a collaborator is a grant of ACCESS, present or absent independent of whether it was ever used. The two numbers can diverge in either direction and no prior cadence on this desk could tell them apart. Same public GitHub REST API family task 38 already cleared: no new Arcade tool, no new scope, no per-user account, paginated defensively same as `contributor_cadence.py`/`branch_cadence.py`. `DEFAULT_HORIZON_HOURS` stays 336 (two weeks) — a grant of write access is rarer than a commit. Sealed by Ogun, Enforcer, whose own office is merge law and who holds the keys.

First real call sealed live the same hour it shipped (no PENDING wait): this sandbox's own `api.github.com` egress is walled off (confirmed live, `403 Forbidden`, the identical boundary tasks 60/63/64/75/78/89/93/128 already named), but the real repo-wide collaborator count was read through the already-authorized `github` MCP channel (`mcp__github__list_repository_collaborators`, `affiliation=all`) → `1` collaborator (`thierrypdamiba`, role `admin`) — honest, not manufactured. Recorded to `oracle/collaborator_snapshots.jsonl` (`{"count": 1, "ts": "2026-07-18T16:19:46+00:00"}`) and sealed to the live chain: seq 343, actor `nisaba`, claim "By 2026-08-01T16:19:46Z... collaborator count will be at least 2 (currently 1, no earlier snapshot yet to compare against)." `python3 tools/ledger.py verify` confirmed the chain intact afterward (344 entries).

**CORRECTED (2026-07-19, task 148):** this section's ordinal ("twenty-fifth") was not just off by one but a straight collision — it reused #77's own (also-wrong) ordinal instead of continuing the sequence. Real position, counted honestly by ROADMAP-task-number order across every live `*_cadence.py` module: twenty-sixth. Fixed here and in `collaborator_cadence.py`/`test_collaborator_cadence.py`; see `test_cadence_ordinal_doctrine.py` for the check that now catches this class of drift for every future cadence.
