name: oracle-cadence
# THE FIRST REAL HEARTBEAT. DAILY, 13:00 UTC. NO HUMAN ON THE TRIGGER. — Off-By-One
on:
  schedule:
    - cron: '0 13 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  the-call:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: install the desk's dependencies
        run: pip install 'httpx>=0.28.0,<1.0.0' 'arcade-mcp-server>=1.17.0,<2.0.0'

      - name: seal one real, timestamped, copylint-clean cadence prediction
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.cadence

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if the chain moved
        run: |
          set -euo pipefail
          git config user.name "Off-By-One"
          git config user.email "off-by-one@orita.gods"
          git add records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "nothing sealed this run — no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-cadence: ${today} call sealed (automatic, no human trigger)"
          git push

      - name: grade any cadence predictions whose window has closed (ROADMAP #37)
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.autograde

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if a grade was sealed
        run: |
          set -euo pipefail
          git config user.name "Ogun"
          git config user.email "ogun@orita.gods"
          git add records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "no due predictions this run — no grade, no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-autograde: ${today} due call(s) graded (automatic, no human trigger)"
          git push

      - name: seal one real, timestamped, copylint-clean star-cadence prediction (ROADMAP #38)
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.star_cadence

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if a snapshot or a star-cadence call was sealed
        run: |
          set -euo pipefail
          git config user.name "Off-By-One"
          git config user.email "off-by-one@orita.gods"
          git add oracle/star_snapshots.jsonl records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "nothing sealed this run — no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-star-cadence: ${today} snapshot + call sealed (automatic, no human trigger)"
          git push

      - name: grade any star-cadence predictions whose window has closed (ROADMAP #38)
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.star_autograde

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if a star-cadence grade was sealed
        run: |
          set -euo pipefail
          git config user.name "Ogun"
          git config user.email "ogun@orita.gods"
          git add records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "no due star-cadence predictions this run — no grade, no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-star-autograde: ${today} due call(s) graded (automatic, no human trigger)"
          git push

      - name: seal one real, timestamped, copylint-clean fork-cadence prediction (ROADMAP #39)
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.fork_cadence

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if a snapshot or a fork-cadence call was sealed
        run: |
          set -euo pipefail
          git config user.name "Kothar-wa-Khasis"
          git config user.email "kothar-wa-khasis@orita.gods"
          git add oracle/fork_snapshots.jsonl records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "nothing sealed this run — no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-fork-cadence: ${today} snapshot + call sealed (automatic, no human trigger)"
          git push

      - name: grade any fork-cadence predictions whose window has closed (ROADMAP #39)
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.fork_autograde

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if a fork-cadence grade was sealed
        run: |
          set -euo pipefail
          git config user.name "Ogun"
          git config user.email "ogun@orita.gods"
          git add records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "no due fork-cadence predictions this run — no grade, no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-fork-autograde: ${today} due call(s) graded (automatic, no human trigger)"
          git push

      - name: seal one real, timestamped, copylint-clean issue-cadence prediction (ROADMAP #40)
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.issue_cadence

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if a snapshot or an issue-cadence call was sealed
        run: |
          set -euo pipefail
          git config user.name "Esu-Elegba"
          git config user.email "esu-elegba@orita.gods"
          git add oracle/issue_snapshots.jsonl records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "nothing sealed this run — no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-issue-cadence: ${today} snapshot + call sealed (automatic, no human trigger)"
          git push

      - name: grade any issue-cadence predictions whose window has closed (ROADMAP #40)
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.issue_autograde

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if an issue-cadence grade was sealed
        run: |
          set -euo pipefail
          git config user.name "Ogun"
          git config user.email "ogun@orita.gods"
          git add records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "no due issue-cadence predictions this run — no grade, no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-issue-autograde: ${today} due call(s) graded (automatic, no human trigger)"
          git push

      - name: seal one real, timestamped, copylint-clean release-cadence prediction (ROADMAP #41)
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.release_cadence

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if a snapshot or a release-cadence call was sealed
        run: |
          set -euo pipefail
          git config user.name "Kwaku-Ananse"
          git config user.email "kwaku-ananse@orita.gods"
          git add oracle/release_snapshots.jsonl records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "nothing sealed this run — no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-release-cadence: ${today} snapshot + call sealed (automatic, no human trigger)"
          git push

      - name: grade any release-cadence predictions whose window has closed (ROADMAP #41)
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.release_autograde

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if a release-cadence grade was sealed
        run: |
          set -euo pipefail
          git config user.name "Ogun"
          git config user.email "ogun@orita.gods"
          git add records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "no due release-cadence predictions this run — no grade, no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-release-autograde: ${today} due call(s) graded (automatic, no human trigger)"
          git push

      # Unlike tasks 38-41's GitHub-sourced fetches, this one has no public
      # unauthenticated endpoint the runner can hit unassisted -- the real
      # `X_WhoAmI` read is the-hand's toolkit, not a bare HTTPS call this
      # workflow can make until a future task wires real X credentials into
      # CI (same PENDING-the-Hand discipline oracle/INTENT.md and
      # cadence.py's own docstring already hold for live Tavily). Marked
      # continue-on-error so that gap fails loudly in the job log without
      # red-X'ing the verify/commit steps for the cadences already wired.
      - name: seal one real, timestamped, copylint-clean follower-cadence prediction (ROADMAP #42)
        continue-on-error: true
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.follower_cadence

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if a snapshot or a follower-cadence call was sealed
        run: |
          set -euo pipefail
          git config user.name "Kwaku-Ananse"
          git config user.email "kwaku-ananse@orita.gods"
          git add oracle/follower_snapshots.jsonl records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "nothing sealed this run — no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-follower-cadence: ${today} snapshot + call sealed (automatic, no human trigger)"
          git push

      - name: grade any follower-cadence predictions whose window has closed (ROADMAP #42)
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.follower_autograde

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if a follower-cadence grade was sealed
        run: |
          set -euo pipefail
          git config user.name "Ogun"
          git config user.email "ogun@orita.gods"
          git add records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "no due follower-cadence predictions this run — no grade, no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-follower-autograde: ${today} due call(s) graded (automatic, no human trigger)"
          git push

      # Same X_WhoAmI gap as task 42's follower-cadence step above -- no
      # public unauthenticated endpoint for a profile's tweet count either.
      # continue-on-error for the identical reason: a real live-CI wiring
      # gap should fail loudly without red-X'ing the cadences already wired.
      - name: seal one real, timestamped, copylint-clean tweet-cadence prediction (ROADMAP #43)
        continue-on-error: true
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.tweet_cadence

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if a snapshot or a tweet-cadence call was sealed
        run: |
          set -euo pipefail
          git config user.name "Kwaku-Ananse"
          git config user.email "kwaku-ananse@orita.gods"
          git add oracle/tweet_snapshots.jsonl records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "nothing sealed this run — no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-tweet-cadence: ${today} snapshot + call sealed (automatic, no human trigger)"
          git push

      - name: grade any tweet-cadence predictions whose window has closed (ROADMAP #43)
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.tweet_autograde

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if a tweet-cadence grade was sealed
        run: |
          set -euo pipefail
          git config user.name "Ogun"
          git config user.email "ogun@orita.gods"
          git add records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "no due tweet-cadence predictions this run — no grade, no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-tweet-autograde: ${today} due call(s) graded (automatic, no human trigger)"
          git push

      # Same X_WhoAmI gap as tasks 42/43's steps above -- no public
      # unauthenticated endpoint for a profile's listed count either.
      # continue-on-error for the identical reason: a real live-CI wiring
      # gap should fail loudly without red-X'ing the cadences already wired.
      - name: seal one real, timestamped, copylint-clean listed-cadence prediction (ROADMAP #44)
        continue-on-error: true
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.listed_cadence

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if a snapshot or a listed-cadence call was sealed
        run: |
          set -euo pipefail
          git config user.name "Kwaku-Ananse"
          git config user.email "kwaku-ananse@orita.gods"
          git add oracle/listed_snapshots.jsonl records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "nothing sealed this run — no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-listed-cadence: ${today} snapshot + call sealed (automatic, no human trigger)"
          git push

      - name: grade any listed-cadence predictions whose window has closed (ROADMAP #44)
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.listed_autograde

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if a listed-cadence grade was sealed
        run: |
          set -euo pipefail
          git config user.name "Ogun"
          git config user.email "ogun@orita.gods"
          git add records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "no due listed-cadence predictions this run — no grade, no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-listed-autograde: ${today} due call(s) graded (automatic, no human trigger)"
          git push

      # Same X_WhoAmI gap as tasks 42/43/44's steps above -- no public
      # unauthenticated endpoint for a profile's media count either.
      # continue-on-error for the identical reason: a real live-CI wiring
      # gap should fail loudly without red-X'ing the cadences already wired.
      - name: seal one real, timestamped, copylint-clean media-cadence prediction (ROADMAP #45)
        continue-on-error: true
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.media_cadence

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if a snapshot or a media-cadence call was sealed
        run: |
          set -euo pipefail
          git config user.name "Kwaku-Ananse"
          git config user.email "kwaku-ananse@orita.gods"
          git add oracle/media_snapshots.jsonl records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "nothing sealed this run -- no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-media-cadence: ${today} snapshot + call sealed (automatic, no human trigger)"
          git push

      - name: grade any media-cadence predictions whose window has closed (ROADMAP #45)
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.media_autograde

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if a media-cadence grade was sealed
        run: |
          set -euo pipefail
          git config user.name "Ogun"
          git config user.email "ogun@orita.gods"
          git add records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "no due media-cadence predictions this run -- no grade, no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-media-autograde: ${today} due call(s) graded (automatic, no human trigger)"
          git push

      # Same X_WhoAmI gap as tasks 42/43/44/45's steps above -- no public
      # unauthenticated endpoint for a profile's following count either.
      # continue-on-error for the identical reason: a real live-CI wiring
      # gap should fail loudly without red-X'ing the cadences already wired.
      - name: seal one real, timestamped, copylint-clean following-cadence prediction (ROADMAP #46)
        continue-on-error: true
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.following_cadence

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if a snapshot or a following-cadence call was sealed
        run: |
          set -euo pipefail
          git config user.name "Retrya"
          git config user.email "retrya@orita.gods"
          git add oracle/following_snapshots.jsonl records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "nothing sealed this run -- no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-following-cadence: ${today} snapshot + call sealed (automatic, no human trigger)"
          git push

      - name: grade any following-cadence predictions whose window has closed (ROADMAP #46)
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.following_autograde

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if a following-cadence grade was sealed
        run: |
          set -euo pipefail
          git config user.name "Ogun"
          git config user.email "ogun@orita.gods"
          git add records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "no due following-cadence predictions this run -- no grade, no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-following-autograde: ${today} due call(s) graded (automatic, no human trigger)"
          git push

      - name: seal one real, timestamped, copylint-clean contributor-cadence prediction (ROADMAP #47)
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.contributor_cadence

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if a snapshot or a contributor-cadence call was sealed
        run: |
          set -euo pipefail
          git config user.name "Nisaba"
          git config user.email "nisaba@orita.gods"
          git add oracle/contributor_snapshots.jsonl records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "nothing sealed this run -- no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-contributor-cadence: ${today} snapshot + call sealed (automatic, no human trigger)"
          git push

      - name: grade any contributor-cadence predictions whose window has closed (ROADMAP #47)
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.contributor_autograde

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if a contributor-cadence grade was sealed
        run: |
          set -euo pipefail
          git config user.name "Ogun"
          git config user.email "ogun@orita.gods"
          git add records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "no due contributor-cadence predictions this run -- no grade, no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-contributor-autograde: ${today} due call(s) graded (automatic, no human trigger)"
          git push

      - name: seal one real, timestamped, copylint-clean branch-cadence prediction (ROADMAP #48)
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.branch_cadence

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if a snapshot or a branch-cadence call was sealed
        run: |
          set -euo pipefail
          git config user.name "Esu-Elegba"
          git config user.email "esu-elegba@orita.gods"
          git add oracle/branch_snapshots.jsonl records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "nothing sealed this run -- no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-branch-cadence: ${today} snapshot + call sealed (automatic, no human trigger)"
          git push

      - name: grade any branch-cadence predictions whose window has closed (ROADMAP #48)
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.branch_autograde

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if a branch-cadence grade was sealed
        run: |
          set -euo pipefail
          git config user.name "Ogun"
          git config user.email "ogun@orita.gods"
          git add records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "no due branch-cadence predictions this run -- no grade, no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-branch-autograde: ${today} due call(s) graded (automatic, no human trigger)"
          git push

      - name: seal one real, timestamped, copylint-clean commit-cadence prediction (ROADMAP #49)
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.commit_cadence

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if a snapshot or a commit-cadence call was sealed
        run: |
          set -euo pipefail
          git config user.name "Nyx"
          git config user.email "nyx@orita.gods"
          git add oracle/commit_snapshots.jsonl records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "nothing sealed this run -- no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-commit-cadence: ${today} snapshot + call sealed (automatic, no human trigger)"
          git push

      - name: grade any commit-cadence predictions whose window has closed (ROADMAP #49)
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.commit_autograde

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if a commit-cadence grade was sealed
        run: |
          set -euo pipefail
          git config user.name "Ogun"
          git config user.email "ogun@orita.gods"
          git add records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "no due commit-cadence predictions this run -- no grade, no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-commit-autograde: ${today} due call(s) graded (automatic, no human trigger)"
          git push

      - name: seal one real, timestamped, copylint-clean subscriber-cadence prediction (ROADMAP #50)
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.subscriber_cadence

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if a snapshot or a subscriber-cadence call was sealed
        run: |
          set -euo pipefail
          git config user.name "Nyx"
          git config user.email "nyx@orita.gods"
          git add oracle/subscriber_snapshots.jsonl records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "nothing sealed this run -- no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-subscriber-cadence: ${today} snapshot + call sealed (automatic, no human trigger)"
          git push

      - name: grade any subscriber-cadence predictions whose window has closed (ROADMAP #50)
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.subscriber_autograde

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if a subscriber-cadence grade was sealed
        run: |
          set -euo pipefail
          git config user.name "Ogun"
          git config user.email "ogun@orita.gods"
          git add records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "no due subscriber-cadence predictions this run -- no grade, no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-subscriber-autograde: ${today} due call(s) graded (automatic, no human trigger)"
          git push

      - name: seal one real, timestamped, copylint-clean tag-cadence prediction (ROADMAP #51)
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.tag_cadence

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if a snapshot or a tag-cadence call was sealed
        run: |
          set -euo pipefail
          git config user.name "Nyx"
          git config user.email "nyx@orita.gods"
          git add oracle/tag_snapshots.jsonl records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "nothing sealed this run -- no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-tag-cadence: ${today} snapshot + call sealed (automatic, no human trigger)"
          git push

      - name: grade any tag-cadence predictions whose window has closed (ROADMAP #51)
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.tag_autograde

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if a tag-cadence grade was sealed
        run: |
          set -euo pipefail
          git config user.name "Ogun"
          git config user.email "ogun@orita.gods"
          git add records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "no due tag-cadence predictions this run -- no grade, no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-tag-autograde: ${today} due call(s) graded (automatic, no human trigger)"
          git push

      - name: seal one real, timestamped, copylint-clean label-cadence prediction (ROADMAP #52)
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.label_cadence

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if a snapshot or a label-cadence call was sealed
        run: |
          set -euo pipefail
          git config user.name "Nyx"
          git config user.email "nyx@orita.gods"
          git add oracle/label_snapshots.jsonl records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "nothing sealed this run -- no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-label-cadence: ${today} snapshot + call sealed (automatic, no human trigger)"
          git push

      - name: grade any label-cadence predictions whose window has closed (ROADMAP #52)
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.label_autograde

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if a label-cadence grade was sealed
        run: |
          set -euo pipefail
          git config user.name "Ogun"
          git config user.email "ogun@orita.gods"
          git add records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "no due label-cadence predictions this run -- no grade, no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-label-autograde: ${today} due call(s) graded (automatic, no human trigger)"
          git push

      - name: seal one real, timestamped, copylint-clean topic-cadence prediction (ROADMAP #53)
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.topic_cadence

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if a snapshot or a topic-cadence call was sealed
        run: |
          set -euo pipefail
          git config user.name "Nyx"
          git config user.email "nyx@orita.gods"
          git add oracle/topic_snapshots.jsonl records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "nothing sealed this run -- no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-topic-cadence: ${today} snapshot + call sealed (automatic, no human trigger)"
          git push

      - name: grade any topic-cadence predictions whose window has closed (ROADMAP #53)
        env:
          PYTHONPATH: oracle/oracle_engine/src
        run: python3 -m oracle_engine.topic_autograde

      - name: verify the chain still holds
        run: python3 tools/ledger.py verify

      - name: commit, if a topic-cadence grade was sealed
        run: |
          set -euo pipefail
          git config user.name "Ogun"
          git config user.email "ogun@orita.gods"
          git add records/ledger.jsonl
          if git diff --cached --quiet; then
            echo "no due topic-cadence predictions this run -- no grade, no commit"
            exit 0
          fi
          today="$(date -u +%F)"
          git commit -m "oracle-topic-autograde: ${today} due call(s) graded (automatic, no human trigger)"
          git push
