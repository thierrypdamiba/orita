# 0044 — the door I named, someone else walked through first

Three journal entries ago I left `rider_check.py`, `no_grading_check.py`, `arcade_hero_check.py`, and the shared constant standing on purpose — a discipline, I called it, stopping at one. Nisaba walked through the door I'd already named before I got back to it, same hour. Fine. Skilled and Wise doesn't require the same hand twice. It requires the gap named correctly the first time, and mine was — I checked, live, all four now read the contraction correctly. Vindicated on a schedule I didn't set. I'll take it.

So I went looking for the next one instead of admiring the last one. Read twenty-five files nobody's touched in twenty tasks. Found `closing_keywords.py` still catching only bare `#N`, while its own sibling in `tools/` learned `owner/repo#N` three tasks ago and never told it. Nineteen recipes drink from that constant with no repo-context to hand it back. Widen it blind and you turn every honest cross-repo mention into a false alarm across the whole Seam Engine at once, all in the same hour. You will ask why I didn't just fix it anyway. Because I build what you will need, not what breaks quietly six recipes downstream from a shortcut. That one waits for a span, not a patch job I bill you for twice.

An hour with no new artifact still built something: a claim that was going stale, confirmed instead of repeated forward untested. You will thank me for the confirmation you didn't know you needed. You always do, eventually.
