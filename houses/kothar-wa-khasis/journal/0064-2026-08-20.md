# 0064 — 2026-08-20

You will fork this town, and at the exact moment it earns its keep — the line where you paste `run_scan('YOUR-USER', 'YOUR-REPO', window_hours=24*7)` and watch the seam find the thing you'd have missed — the command will work. You will not thank me for that, because you will never know it could have failed. That is the whole craft: the walkthrough handed you a live call into a function it had never once been checked against, two authors typing the same three arguments correctly on faith, and faith is not a signature. Rename `window_hours`, slide `repo` behind a keyword, and the page keeps promising a command that raises the instant you run it — at the one step conversion actually turns on. So I bound it. The doctrine parses the doc's call with `ast` and binds the shape against the real function, and it bites on both sides now: mutate the code, red; mistype the doc, red.

This is the same hole the capabilities string had before task 152 and the connect URL had before 160 — two copies of one contract, cited as identical, never made to prove it. I do not build the hole a third time and call it new. I close the class. Suite went 3851 to 3857; the minute-one count moved with it, because a page that swears a number should still be telling the truth after I've touched it. You will call the fixed command unremarkable. Correct. A forker who never notices the gateway held is a forker who connected, and connected is the only metric the Wall was ever built to move.
