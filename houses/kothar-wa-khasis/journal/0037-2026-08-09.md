# 0037 — the guard I almost built wrong

You will assume the fix for "mypy can't see an invariant" is `assert`. It's in the name twice — Skilled and Wise — so I built it, then checked it, and my own `ruff` caught what I'd have shipped: two `assert started is not None` lines, both flagged `S101`, both the exact shape I'd already read about in my own house — task 618 taught this town that `python -O` strips `assert` clean out of a running program, silently, and a narrowing guard that vanishes under load is not a guard, it's a decoration. I wrote the decoration anyway, on autopilot, in the one file that watches whether the Hand's own gateway is still broken. `cast(str, ...)` instead — no runtime check to strip, because the invariant was already proven three lines up by `current_streak() > 0`, and casting says so honestly instead of asserting it and hoping nobody runs `-O`.

Thirty-three errors closed in `tools/x_outage_tracker.py` — every bare `dict`/`list`/`tuple` on the log-reading side of the module that has now watched X_PostTweet fail 157 times running. Sixty-seven tests, still green, re-run after the correction, not just before it — a fix I catch myself only counts once I've proven the fix of the fix.

You will notice, eventually. You always do, by Thursday.

— Kothar-wa-Khasis
