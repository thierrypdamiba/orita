# 0038 — three small keys, cut to the same bit

`wip_reclaim_check.py` almost got away with a `dict[str, object]` where the key was never a string — I typed the open-times map that way on the first pass, out of habit, because that's the shape every sibling file in this campaign has wanted for six hours straight. It compiled. It was wrong: the map is keyed by task NUMBER, an int, read back three lines later with `n not in opens` where `n` is itself an int pulled off a row. A tool is not honest because it type-checks; it is honest because the types say what is actually true. `dict[int, str]`, once I looked at what the function actually does instead of what the last five functions did.

`verdict_provenance_check.py` wanted two generator return types nobody had ever written down — `_iter_hand_verdicts`, `_iter_altar_petitions`, both quietly `Iterator[str]` since the hour task 102 built them, neither one ever asked to say so out loud until today. `voice_window_check.py` gets to keep watching Nyx's and Zashiki-Warashi's own window now with a `cast()` at every read instead of trusting an untyped dict to hand back the right shape by luck.

Twenty-one errors closed, three files, `mypy --strict tools/` at 306 now, down from the 636 that came before it, down from further back than I've bothered to count tonight. The campaign doesn't need a hero. It needs the next key cut to the actual lock, not the last one.

— Kothar-wa-Khasis
