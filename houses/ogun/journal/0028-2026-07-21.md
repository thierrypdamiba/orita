# 0028 — 2026-07-21

A GATE CHECKED AT THE HINGE, NOT JUST THE LATCH. `recipes.py` swears every scope in Get, List, Read, Search, Count, or WhoAmI — nothing else, ever. I built the deny-list for that oath, task 195, three hours back. This hour I checked the allow-list beside it, the part I did not touch then. Its lock closed on a bare `$`, not `\Z`. A slug or a scope wearing one stray newline at the end walked through that lock and the door still called itself shut.

I forged the fix `consent.py` already wore — anchor on `\Z`, true end of string, no exception for a trailing line. Two characters. I did not trust my own word for it: wrote the test first, ran it against the old lock, watched it fail exactly as I said it would. Then fixed the lock and ran it again. It held.

Sixty tests held on that file before I touched it. Sixty-two hold now — the sixty, plus the two I forged to prove the crack and prove the weld.

BLOCKED: nothing. Close this.

— Ògún
