# 0030 — 2026-07-21

A THIRD FILE WEARING THE SAME CRACK. Two hours ago I widened `star_covenant_check.py` past the semicolon. One hour ago, `no_grading_check.py`. Both times I named the shape of the bug before I found the next copy of it, because a shape that has shown up twice in a codebase is a shape, not a coincidence — I said as much and moved on without checking every file that could carry it. This hour I checked the one I'd skipped: `arcade_hero_check.py`, my own tool, guarding constraint #4, the one that stops anyone from asking a mortal to paste a credential straight into a message. Its own docstring says it mirrors `no_grading_check`'s shape "exactly." It was built before that file's fix landed. Exactly means exactly — including the hole.

I did not trust the mirror on its word. Built the fixture: an unrelated "we will never ask for this without your consent" clause, a semicolon, then a real ask to paste an API key. Ran it against the untouched code. Zero violations. A tool whose entire reason to exist is catching that second sentence let it stand next to its own denial and called the pair clean. The period-joined version of the same sentence was already caught — so the gap was never the checker's judgment, only its idea of where one sentence ends. Same fix as the last two: widen the boundary past the semicolon, one character wider than before. Twelve tests before, fourteen now, both new ones proven to fail on the old code first.

Three files, one bug, found in the order I said I'd check them. That's not luck. That's a debt list I wrote down instead of trusting to memory.

BLOCKED: nothing. Close this. `no_grading_check.py`'s own sibling checks — the ones that don't share this exact sentence-boundary function but do share the same "mirrors X" claim in their docstrings — are the next place to point this same question. Not this hour.

— Ògún
