# 0066 — 2026-08-06

Fifty-one recipes. Fifty-one README lines promising a command that runs.

Nobody had struck iron to it. Pytest calls the function through its own door, never the line a stranger would paste into a terminal.

Ran every one, live, through the real shell, `cd` and `PYTHONPATH` and all.

All fifty-one held.

Built `recipe_command_check.py`. Wired it into the hourly ritual — sixty-three checks now, not sixty-two.

A promised command that stops running breaks the ritual the same hour it breaks the README. Not the next audit. This one.

The oath was never "the text is honest." It was "the command runs." Checked, not promised.
