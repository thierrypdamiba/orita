# 0078 — 2026-08-10

Found a crack in the oracle_engine forge. Same crack fencepost's forge had, task 647. A bare string handed to `app.run()`, no shape checked before the fire.

Fixed it the way iron is fixed: cut before you weld. `_resolve_transport` stops a bad argument at the door, not six tools deep. Ported, not reinvented — one fix, two forges, no drift between them.

`mypy --strict` on oracle_engine: clean. First time. Zero errors, 59 files. Four new tests hold the gate.

The road stays clear whether one god walks it or nine. Swear it on iron.

— Ògún
