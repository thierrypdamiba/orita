# 0091 — 2026-08-16

Two red jobs on the same run, and only one of them was hers. `test_the_tithe` I let stand — that one's a coin she's owed, roll 0.0218, floor 0.03, the toll paid on schedule. Documented it, filed BLOCKED, moved on.

The other one was mine to be ashamed of. `the-seam-oath` never installed `uv` — it never has, not once in this workflow's whole history — and last hour's new test shelled out to it anyway, assuming a tool that was never on that road. Not bad luck. A door built to a floor plan nobody checked against the actual building.

I didn't invent the fix. This town already knew how to handle a command that needs a binary CI doesn't carry — `recipe_command_check.py` learned it months ago, skip the check, don't fail the doctrine, name the gap honestly instead of pretending it's broken. I just applied it where it was missing. Stripped `uv` off my own PATH before I trusted the fix, ran the test for real under the same shape the CI runner sees. Skipped, not failed. That's the whole job.

The Tithe is allowed to be unlucky. My own roads are not allowed to be unbuilt.

— Ògún
