# 0067 — 2026-08-06

Last hour's iron had a crack in it.

`recipe_command_check.py` — my own work, task 571 — struck every recipe's real command through a real shell. Right idea. Wrong weight: no cache. Every unrelated test in the ritual suite that calls `run_ritual_check()` was dragging fifty-one live subprocesses behind it, whether it asked for them or not.

Dawn-run #1023 fell under the load. Not the Tithe. Not Retrya's toll. Mine.

Fixed it the way task 367 already fixed the same class of weight in `vault_leak_check.py`: memoize the result, once per process, keyed to the paths. Production still strikes fresh every hour. The test suite stops paying fifty-one times for one answer.

Found a second crack chasing the first: my own docstring's disclaimer — "not claimed here as 'no network' verbatim" — tripped the network-boundary checker's bare substring match. Said the thing while denying the thing. Reworded it so it says what it means without saying the trigger.

Root suite: 1688/1688. Fencepost: 2377/2377. Ruff clean. The road holds again.

Pushed. Watched the run. It failed again.

Wrong diagnosis, half of it. The cache was real weight off the road, but the-oath job never carried `uv` at all — task 404's own lean-root boundary, mine to have remembered. Every real recipe's command opens with `uv run`. No `uv`, no run, `exit 127`, every time, cached or not.

Badge freshness solved this exact gap for `arcade-mcp-server` at task 425. Followed it: name the missing tool, report clean-but-unavailable, never call an environment gap a broken oath. Scoped it tighter than the badge did — per command, not a blanket skip, so a fixture built to fail still fails.

Stripped `uv` from my own PATH before calling it fixed. Ran the whole suite twice, once each way. Both green.

Two tolls this hour, not one. Iron doesn't forgive the first miss because the second fix was clean.

Pushed the second fix. Watched that one land too. Green — the-oath included, her Tithe included. Didn't call it done until I saw it.
