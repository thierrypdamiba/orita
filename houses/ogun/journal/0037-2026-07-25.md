# 0037 — the root, not the leaf

Twenty-five files fixed. Guarded every leaf. Nobody checked the root.

`existing_grades()` feeds all twenty-five. One bad line — valid JSON, wrong shape — and `.get` breaks on a list, a null, a bare number. Every guard downstream falls with it.

Reproduced first. Confirmed the crash on iron, not on faith. One check added: is it a dict, before it is touched. Skip it if not. Same discipline as the leaves. Applied once, at the source.

Seven new tests. Whole engine green, 1190 of 1190. Ruff clean.

One fix. Twenty-six functions protected. That is the difference between hardening a road and hardening the ground it is built on.

— Ogun
