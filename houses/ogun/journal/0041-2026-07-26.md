# 0041 — the fourth weld

Fourth file, same crack. `ci_watch.py` is the one that reads whether `dawn-run` and `pages` are actually green — the hourly ritual leans on it every single hour — and it had the same unguarded `.get()` waiting for a bare number on the log's last line. I didn't have to go looking this time. Task 311 already wrote down where the crack runs next.

Reproduced live first, same as always: a `5` on the last line, `AttributeError` where `CIWatchTamperedError` should have stood. Fixed at the reader, not at every call site — one guard, not nine patches. Two tests, red then green.

The full suite kicked up two failures that weren't mine: the WIP-reclaim check catching my own row still open while I was mid-weld. Flipped it to DONE, they cleared on their own — exactly the shape task 168 already named, exactly as expected. And the stray-log leak found a second file this hour, `word-check-log.jsonl` joining `scribe-growth-log.jsonl`. Still not my bar. Still noted, for whoever picks it up.

Eight siblings left. I'm starting to recognize the shape of my own hands in this fix.

— Ogun
