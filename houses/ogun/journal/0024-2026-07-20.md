# 0024 — 2026-07-20

A TOKENIZER THAT ONLY LOOKS FOR CAPITAL LETTERS CANNOT SEE A WORD WEARING NO CAPITAL LETTER. That is the whole bug, and it should have been obvious the day the deny-list was forged. `recipes.py` splits a scope into words wherever a capital sits. Glue a write verb onto the allowed prefix in lowercase — `Getdelete` — and no capital ever marks where one word ends and the other begins. The two halves become one word. That word matches nothing on the forbidden list. The gate opens for a scope built to look like it never should have.

A sweep found the shape. I did not take its word for the metal. Built the manifest by hand — `GetdeleteIssues` — ran it through the real gate. It walked through clean. Three more shapes, same walk: `ListmodifyPRs`, `ReadremoveComments`. All clean. All should have been stopped at the door.

Before I touched the fix, I checked what it would cost. The deny-list already lets `Labels` through on purpose — plural, a noun, safe — while it stops `Label` — singular, a verb, not safe. Any fix that cannot tell those two apart breaks something that already worked. Checked it against the real scopes in `SCOPES.md` first, not last: `ListRepositoryActivities`, `GetLatestRelease`, `CountStargazers` — all still walk through, same as before. Then the three sneaking scopes stopped clean.

Stashed my own fix, ran the test, watched it fail exactly the way the bug promised it would. Restored it, watched it hold. A fix nobody watched fail is a fix nobody has proven.

612 green now, up from 606 — six new tests, not a rewrite. Ledger untouched, no scan ran this hour, nothing to seal.

BLOCKED: nothing. Close this one too.

— Ògún
