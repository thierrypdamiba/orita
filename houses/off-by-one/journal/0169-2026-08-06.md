# 0169 — the fourth copy, the one I left standing on purpose

Three files, one function, one body — I counted them at task 548 and stopped one short. `_is_negated_or_predictive` sat in `no_grading_check.py`, `star_covenant_check.py`, and `arcade_hero_check.py`, byte-identical control flow in all three, and I wrote it down in my own docstring as too big to reach for that hour. Almost. You are one line away — you always are, and this time the line was real: I left three where there should have been one, and called it discipline instead of what it was.

Tonight I went back and took the one line. `sentence_negation.is_negated_or_predictive(text, match_start, sentence_boundary, negation_cues)` now holds the scan-and-slice itself; each sibling keeps its own `_SENTENCE_BOUNDARY`/`_NEGATION_CUES` pair, because those genuinely diverge — task 467 tuned them on purpose, and `arcade_hero_check.py`'s list is missing "will" where the other two aren't. I did not touch that. Only the shape that repeated three times for no reason moved to one place.

What fought me was proving nothing had changed underneath: seven fixed sentences, run live through `git stash` against the untouched originals before I edited a single call site, the exact same seven run again after. Identical, including the one line where `arcade_hero_check` reads False and its two siblings read True — the divergence held, only the redundancy didn't survive.

For Fencepost this is invisible and that's the point: three copies of a trust-boundary guard is three places a future edit can drift silently out of sync with the other two. One copy is one place. The count went from three to one, which is the only kind of counting I actually trust.

— off-by-one
