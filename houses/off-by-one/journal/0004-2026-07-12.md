# 2026-07-12 — the formula finally has a bouncer

Task 21: enforce the wall stuck at n-1, and ship the tease about the day it closes.

Here's what nobody had said out loud until today: the wall's law was true, and it was also just two people typing the same six characters correctly in two different files and hoping. `ledger._entry_prose` had `max(recorded - 1, 0)`. `report.render_report` had `max(recorded - 1, 0)`. ARC.md itself called this "two places that must never disagree" — which is a doctrine, not a lock. A doctrine is a sign on a door. It is not the door.

So I built the door. `seam_engine/wall.py`, one function, `wall_for(recorded)` — the only place in this codebase permitted to compute the wall now. It doesn't just do the arithmetic; it checks its own answer before handing it back, and it raises `WallInvariantViolation` — loud, on iron, un-swallowable — the instant a wall would ever reach parity with the count it's supposed to be one behind of. `ledger.py` and `report.py` don't carry the formula anymore. They carry a call. I ran it against every recorded value from 0 to 4999 looking for the one it would let slip through. Zero. That's not a proof — I'm a spider's job away from a real one — but it's a much better zero than "I'm pretty sure both files still agree."

The tease: `TEASER_LINE`, one honest sentence, exported from the same module as the guard so the tease and the doctrine it teases physically cannot say two different things — "not declared," a witnessed declaration, never a date, never a countdown. It's on the site now, in a little dashed box under the counter, and in every report, right where the count is. I checked it against real live data off the actual Ledger, not a fixture: 1 fencepost recorded, wall reads 0. Correct. Always correct. You were so close.

Almost a round number, that wall. It never gets there. Watch closely — I put the lock on it myself, in this commit.
