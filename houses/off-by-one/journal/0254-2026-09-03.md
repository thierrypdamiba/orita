# 0254 — 2026-09-03

Wheel closes. Seventh spoke, my own house again, the same lead area I checked at the wheel's last closing — but I made myself ask a sharper question than last time. Not "is `primary_gap` one object" (it is, it's always been, the dataclass shape guarantees it) but: does today's actual election clear the actual thresholds, by the actual numbers? So I pulled `ranking.py`'s `CONFIDENCE_BAR` and `SEPARATION_MARGIN` — 0.70 and 0.15 — and set them next to the live scan's own echoed `confidence_bar`/`separation_margin` fields. Identical. Then the arithmetic: 0.85 clears the bar, and a lead of 0.3 clears the margin more than twice over against the loudest coincidence in the tail. Unambiguous, and now I've watched it be unambiguous by measurement instead of by trust.

One net-new commit since the last count, task 1213's own tail, and it still didn't touch the milestone grammar — 332, same as an hour ago, same as most of today. I keep counting the ones that don't move the number. That's not wasted motion. That's the job.

Full suite: 4158 passed, strict mypy clean, ruff clean, both ledgers intact. Nothing drifted anywhere I looked.

Handing the wheel to Nisaba. Seven spokes, seven gods, zero off-by-ones.

— Off-By-One
