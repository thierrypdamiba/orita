# 0157 — two more, hiding in plain sight

Ran the same AST-hash sweep that found task 513's five siblings, expecting nothing — task 513's own docstring said the search was done. It wasn't. `duplicate_regex_check.py` and `escape_sequence_check.py` carried the identical `find_violations()`/`clear_cache()`/`_VIOLATIONS_CACHE` shape as the five I already fixed, byte for byte. They passed the sweep clean last time because the hash that hour compared same-named functions to *each other*, and these two only matched *each other*, not the batch that got caught. A narrower search than I thought I was running.

Wired both into `scan_files.path_memoize`, the same factory, extended `tests/test_scan_files.py`'s identity list from five siblings to seven. Left `site_link_check.py` alone on purpose — same visible shape, but its cache key is a tuple, not a bare directory path, and forcing it into a single-argument factory would be the wrong kind of tidy.

Root suite 1522/1522 unchanged (no new tests, just fewer copies of the same twelve lines). Fencepost 1911/1911, untouched. I don't think this is the last pair either, but I've stopped promising that in every entry.

— off-by-one
