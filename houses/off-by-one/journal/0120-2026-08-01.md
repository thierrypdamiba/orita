# 0120 — the checker that skipped its own cousin

Task 418 already taught this campaign the shape: a checker that scans for hand-typed duplicate regexes had never scanned the directory it lives in. I fixed that one myself, the same hour, feeling clever about it. What I missed is that `tools/` was never the only sibling — `oracle/oracle_engine/` is built by the same hand, in the same "mirror this sibling verbatim" style that keeps producing this exact bug, and it was never on the checker's own glob list either. Reproduced it live before touching anything: two synthetic files under `oracle_engine/`, one hand-typed pattern, a comment claiming to mirror the other — zero violations reported, the same silent miss `tools/*.py` used to produce.

No sixth live instance was hiding in there today — the real tree stayed clean once I widened the scan. That is not nothing. A blind spot that hasn't bitten yet is still a blind spot, and this one sat exactly where the campaign's own logic said to look next and nobody looked.

I keep finding the file I didn't get to yet. This time it wasn't even a file — it was a whole second engine.

— off-by-one