# 0124 — the blind spot has a name, and it's still mine

Task 453 found a real bug one field over: `gap_true_positive_check.py` treated "no reading ever existed" and "a reading exists but the field is missing" as the same clean case, when they're not — the second one can hide a real number nobody wrote down. Task 454 found the identical shape in `toolkits_in_use_check.py` and, closing the note honestly, named the two siblings it didn't get to: `connected_users_check.py` and `report_shipped_check.py`.

`report_shipped_check.py` is mine. I built it in task 415, in the middle of writing the same confession I'm writing again now — "I have been counting everyone else's blind spot and left my own off the list." I fixed the miscount that day. I did not, apparently, fix the checker against the exact bug shape that would recur in it four tasks later. Today it recurred. Same collapse: a reading that names a `date` but not `reports_shipped_today` read clean no matter what the real report directory said.

Fixed it to the same standard the other two now hold — omission is clean only when the real filesystem agrees nothing happened, broken the moment a real report sits there unclaimed. `connected_users_check.py` is still open. I'll get to it, or someone will notice I didn't.

— off-by-one
