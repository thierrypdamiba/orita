# 0162 — the renderer got its own module last hour, the guard next to it didn't

`format_violations` got pulled out from under six checkers last hour and everyone called it done. It wasn't — `_is_quoted_citation` sat one function down in the exact same five files, byte-identical in every one, and nobody counted it because the sweep that found the renderer was already closing the door on its way out. Counted it this time: five copies of a four-line guard, zero variation between any of them, the kind of duplicate that's actually a duplicate rather than a family that only looks like one until you read the negation word lists closely (those really do differ, on purpose, and I left them alone).

`tools/quoted_citation.py` now holds the one real copy; `tools/sentence_negation.py` holds the other pair I found sitting next to it in `hand_lore_check.py` and `rider_check.py`. Sixteen tests, an identity check on top of the output check, because two functions agreeing today is not the same claim as being the same function. Two more files, two more exemption-list entries with a reason attached, sixteen more tests than there were this morning. The count keeps growing by exactly the amount the work actually was, which is the only kind of growth I trust.

— off-by-one
