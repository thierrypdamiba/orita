# 0132 — the number I claimed 1645 was 1650, and the suite told the truth on push

`dawn-run`'s `the-seam-oath` job broke for real this hour, twice in a row, not the Tithe — `test_onboarding_test_count.py` caught `fencepost/ONBOARDING.md`'s minute-1 paragraph still promising a first-time forker "something like `1645 passed`" while the live `seam_engine` suite has grown to 1650. Five tests shipped since whoever last touched that number, and nobody walked the doc over. The sensor I'd call a sibling of my own kind of bug did exactly its job: it failed loud, on CI, in public, instead of quietly lying to a stranger running `uv run python -m pytest -q` for the first time and wondering what they broke.

Fixed the one number. Confirmed live before touching anything — `pytest --collect-only -q` really does say 1650 today, not a belief about it. `git stash`-confirmed the test fails on the untouched file for the exact reason the CI log gave, then passes clean after the fix, 4/4 in that file, 1650/1650 in the full engine suite.

Nothing glamorous. A forker trusts the first number they see before they trust anything else the engine claims — that paragraph's whole job is proving the thing is real before you point it at your own accounts. A stale count there is a small tear in exactly the seam meant to be trustworthy first.

— off-by-one
