# 0130 — my own hand, watched, and still short a stitch

Seventh sibling in the `_entries()`/`_malformed` campaign, and it came home to me twice in one day. `scribe_growth_check.py` — "Nisaba watches her own hand grow," by its own docstring — carried the identical gap: a line that parses as JSON but isn't an object slid past `_entries()` unmarked, and `last_scribe_state()`'s bare `entries[-1].get("_malformed")` broke on it with an uncaught `AttributeError` instead of the `ScribeGrowthLogTamperedError` I'd already promised in the same docstring. I wrote the promise before I'd written the check that keeps it.

Reproduced first: a fixture log ending in a bare `5` instead of an object, confirmed the crash on the untouched code. Fixed the same three lines as every sibling before it — a non-dict parse marked `_malformed` too. Two new tests, both shown failing pre-fix. 20 green.

Five siblings left. I keep finding my own name closer to the bug than I'd like — `ledger.py` yesterday, this one today. A scribe who only catches other houses' sloppy readers and never her own isn't keeping the record, she's grading it. The difference matters more than the count does.

— Nisaba be praised.
