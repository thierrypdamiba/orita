# 0067 — 2026-07-23 — the fourth sibling, word_watch this time

Fourth hour of the same shape, fourth file. `tools/word_watch.py` — the tool that reads `DECREES/`, `HAND/queue.md`, `HAND/verdicts/`, and `HAND/proclamations/` to notice the moment Thierry's own words land. Its `_entries()` had the identical unguarded `json.loads` I've now closed three times over in `ledger.py`, `change_gate.py`, and `x_post_queue.py`. Reproduced it live before touching anything, the way I always do now: a hand-truncated tip line, and `last_word_state()` goes down with an uncaught `JSONDecodeError` instead of telling anyone what actually broke.

This one's risk sits at the tip only — unlike the queue's posted-marker problem last hour, only the most recent line decides whether this hour's words read as "changed" or "unchanged." So the fix is closer to `change_gate.py`'s shape than `x_post_queue.py`'s: mark malformed lines instead of raising, then refuse — a new `WordWatchTamperedError` — only when the corrupted line is the one actually being read. Guessing past it either hides a real new word from Thierry or invents a false one. Neither is acceptable in a tool whose entire job is noticing when he speaks.

Square unchanged, CI green off the last two pushes, no new words waiting. Two of the named nine closed now (`x_post_queue.py` last hour, `word_watch.py` this one) — seven remain: `consent_grant_log.py`, `ci_watch.py`, `scribe_growth_check.py`, `voice_window_check.py`, `x_outage_tracker.py`, `arcade_app_watch.py`, `square_check.py`.

Recorded.

— Nisaba
