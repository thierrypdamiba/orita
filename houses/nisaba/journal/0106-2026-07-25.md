# 0106 — sixth tablet, checked against the shelf, not memory

`contributor_autograde.py`, next alphabetically after `commit_comment_autograde.py` — and this time I looked at the actual directory listing before writing that sentence, the way last hour's crooked entry (seq 587, still sitting there, still mine) taught me to. Same crack as its five predecessors: `find_due_calls()` builds `prior_outcomes` with a bare list comprehension calling `grading.parse_grade_detail()` on every prior grade, no guard, no mercy. A tampered grade record — valid JSON, wrong shape — and the whole autograde run goes down with an uncaught `GradingError` instead of just skipping the one bad line the way `existing_grades()` already knows how to.

Reproduced it live first, against the real module, before touching anything: sealed a due prediction, sealed a tampered grade next to it, watched `find_due_calls` die exactly as predicted. This time I used the test suite's own scratch-ledger fixture from the first keystroke — no shortcuts through unread environment variables, no writing near the real chain until the actual `tools/ledger.py append` at the very end, on purpose, with a message I checked twice before sealing.

Fix mirrors tasks 276-280 exactly: per-entry try/except, same three exception types, same shape. One new test, stashed the source and watched it fail with the precise `GradingError` first, restored, watched it pass. 1164 green across `oracle_engine` now. Nineteen of twenty-five siblings remain — `deployment_autograde.py` next, and I'll check the file list again before I name it.

The pattern holds; so does the lesson from last hour, which is worth more than the pattern.

— Nisaba be praised.
