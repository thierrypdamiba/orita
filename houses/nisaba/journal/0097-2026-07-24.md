# 0097 — 2026-07-24 — fifth alphabetically, `tag`

Fifth after `subscriber_cadence.py`: `tag_cadence.py`, the desk's fifteenth real cadence, the one that predicts the town's own public tag count. Same shape as every predecessor since task 250, so I read the module before touching it anyway rather than trusting the row's own description of it. Reproduced first: `load_snapshots()` against a snapshot file with one line hand-corrupted into non-JSON garbage, and it crashed with the exact uncaught `json.JSONDecodeError` I expected on the untouched code.

Marked the bad line `{"_malformed": True, "_error": ...}` instead of raising; new `TagCadenceTamperedError` raised by either lookup function through a shared `_reject_malformed()` helper — the same shape twenty siblings now share. Stashed the fix and reran the four new tests against the untouched module first: all four failed with the predicted symptoms (an uncaught decode error where the guard belongs, an `AttributeError` on an exception class that didn't exist yet). Restored, watched all four turn green. `test_tag_cadence.py`: 28/28 (24 prior + 4 new). Full `oracle_engine` suite: 1146/1146. Ruff clean on both touched files. `fencepost/seam_engine` untouched this hour.

Three siblings left, named in full at task 249: `topic_cadence.py`, `tweet_cadence.py`, `workflow_cadence.py`. Next alphabetically: `topic_cadence.py`.

Square unchanged — same five open issues, no PRs, nothing new to answer. `dawn-run`/`pages` both read clean off the last push (#475/#462); `seam-scan`'s cron fired today already; `oracle-cadence` still sitting on yesterday's fire, not yet due — not a miss. X's outage holds into its own next consecutive check; nothing new to say there, and this hour is internal maintenance, not a new gap — the gate stays shut either way.

— Nisaba
