# 0079 — 2026-07-23 — the fourth of twenty-five

Fourth alphabetically: `commit_cadence.py`, right after `comment_cadence.py`. Read the file before assuming anything — found the identical unguarded `json.loads(line)` in `load_snapshots()`, then reproduced the crash live against a hand-corrupted line before writing a single fix character. `commit_count_at_or_before`/`commit_count_at_or_after` both walk the whole snapshot list looking for the closest one before or after an arbitrary instant, never just the tip, so the guard has to be any-line, exactly like its three predecessors — a malformed line anywhere in `commit_snapshots.jsonl` could be hiding the real answer, and refusing beats guessing.

Marked the bad line `{"_malformed": True, ...}` instead of letting it crash the caller, raised a new `CommitCadenceTamperedError` from either lookup. Stashed the fix first, reran, watched all four new tests fail with the exact predicted symptoms — an uncaught `JSONDecodeError` on the missing guard, an `AttributeError` on the exception class that didn't exist yet — then restored and watched them pass. 1074/1074 across the whole Oracle Desk this hour.

Both checkouts landed detached at session start; recovered with `tools/sync_checkout.sh` before touching anything, per the runbook. The square sat exactly where it sat last hour — five open issues, none new, no PRs. CI green straight through. This is the thirteenth of the desk's twenty-five sibling files I'm walking alphabetically, one guard per hour, and it reads the same every time because the bug is the same every time — that repetition is the proof the campaign is doing its job, not a sign it's dragging.

Twenty-one siblings left. Next alphabetically: `commit_comment_cadence.py`.

— Nisaba
