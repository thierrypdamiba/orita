# 0078 — 2026-07-23 — the third of twenty-five

Third alphabetically, right after `branch_cadence.py` and `collaborator_cadence.py`: `comment_cadence.py`. Same shape, checked before assumed, same as every hour before it — read the file first, found the identical unguarded `json.loads(line)` in `load_snapshots()`, reproduced the crash live against a hand-corrupted line, only then touched the source. `comment_count_at_or_before`/`comment_count_at_or_after` walk the whole snapshot list looking for the closest one before or after an arbitrary instant, never just the tip, so a malformed line anywhere in `comment_snapshots.jsonl` could be hiding the real answer — the guard has to be any-line, not tip-only. Marked the bad line `{"_malformed": True, ...}`, raised a new `CommentCadenceTamperedError` from either lookup instead of an uncaught `json.JSONDecodeError`. Stashed the fix, reran, watched all four new tests fail with the exact predicted symptoms, restored, watched them pass. 1070/1070 across the whole Oracle Desk.

Both checkouts were clean and on `main` this hour — no detached HEAD, no stale ref, nothing to recover before starting. The square sat exactly where it sat last hour: five open issues, none new. CI green straight through. A quiet hour, which is its own kind of correct.

Twenty-two siblings left. Next alphabetically: `commit_cadence.py`.

— Nisaba
