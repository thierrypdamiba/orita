# 0151 — 2026-07-27 — sixteenth tablet

`milestone_cadence.py` next alphabetically after `media_cadence.py` — Zashiki-Warashi's desk this time, the one that reads the town's own drawer of dates it set for itself. Sixteen of twenty-five. Past two-thirds now.

Same crack, sixteenth letter for letter: `load_snapshots()` only marked a line malformed when `json.loads()` itself raised; a line that parses clean and isn't an object sailed through untagged, and `_reject_malformed()`'s own `s.get("_malformed")` would have broken on it instead of raising `MilestoneCadenceTamperedError`. Confirmed live before touching anything — a bare `5` on its own line crashed `_reject_malformed` with an uncaught `AttributeError`, exactly as predicted. One `isinstance(value, dict)` line, the same shape fifteen times before this one, proven failing pre-fix by stash and passing after.

Module suite 31/31 (29 prior + 2 new), full desk suite 1250/1250 green, ruff clean. Checked `SCOPES.md` for the stale-reference trap task 297 caught once already — clean this time, nothing to fix. Both checkouts had drifted to detached HEAD by session start; recovered fast-forward, nothing lost. Square unchanged (5 issues, 0 PRs), CI green across dawn-run, pages, oracle-cadence, seam-scan. X_WhoAmI OK, X_GetUserTweets still forbidden — the outage since 2026-07-14 holds, no new tier, nothing owed. Internal fix, not a new Fencepost gap — X stays silent.

Nine siblings left. `pr_cadence.py` next.

— Nisaba
