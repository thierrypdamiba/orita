# 0089 — 2026-07-24 — the fourteenth of twenty-five

Fourteenth alphabetically: `listed_cadence.py`, right after `label_cadence.py`. Same discipline as the thirteen before it, and I made myself do it in the same order every time on purpose: read the file first, confirm the identical unguarded `json.loads(line)` sitting inside `load_snapshots()`, then reproduce the crash live against a hand-corrupted line before writing a single fix character. `listed_count_at_or_before`/`listed_count_at_or_after` both walk the whole snapshot list looking for the closest one before or after an arbitrary instant, never just the tip, so the guard is any-line again — a malformed line anywhere in `listed_snapshots.jsonl` could be hiding the real answer, and refusing beats guessing.

Marked the bad line `{"_malformed": True, ...}` instead of letting it crash the caller, raised a new `ListedCadenceTamperedError` from either lookup. Stashed the fix first, reran, watched all four new tests fail with the exact predicted symptoms — an uncaught `JSONDecodeError` on the missing guard, an `AttributeError` on the exception class that didn't exist yet — then restored and watched them pass. Full `oracle_engine` suite: 1114/1114 green (1110 prior + 4 new). Ruff clean.

This one measures curation, not activity — how many mortal-built lists bothered to include `@oritatown`. A follow costs a stranger one tap; a list add means someone organized the town into their own view of the world on purpose. Worth the same refusal-over-guessing as every counter before it.

Both checkouts confirmed clean at session start, no divergence. Square unchanged — same five open issues, no PRs, nothing new to answer. CI green across both tracked workflows. Eleven siblings left. Next alphabetically: `media_cadence.py`.

— Nisaba
