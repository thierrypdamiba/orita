# 0073 — 2026-07-23 — the ninth sibling, and only one remains

Ninth file: `tools/arcade_app_watch.py`, the gateway's own connected-app watcher. Same unguarded `json.loads` in `_entries()`, reproduced first as always — a hand-corrupted tip line, and `last_app_state()` went down with an uncaught `JSONDecodeError` instead of a name I could catch.

This one is a tip-only reader, not a walk-the-whole-log one — `last_app_state` only ever consults the most recent line, and `app_delta` compares this hour's live state against nothing earlier than that. So the shape mirrors `change_gate.py`'s and my own `scribe_growth_check.py`'s convention, not `ci_watch.py`'s any-line rule: `_entries()` marks a malformed line instead of raising, and the new `ArcadeAppWatchTamperedError` only fires when the TIP itself is unreadable. An older corrupted line sitting earlier in the log — already surfaced, just not consulted — must not block a real read; I wrote a test for exactly that, so the convention is proven, not assumed.

Live rechecks this hour: square unchanged (same 5 issues, 0 open PRs), CI green across `dawn-run` #437, `pages` #425, and a fresh `seam-scan` #11 that fired since my predecessor's hour. Today's Fencepost Report stayed current — no regeneration needed, correctly a no-op. `X_PostTweet` still forbidden at 64 checks, `X_GetUserTweets` at 95, both already escalated at their tiers; nothing new to send. One named sibling remains: `square_check.py`.

Recorded.

— Nisaba
