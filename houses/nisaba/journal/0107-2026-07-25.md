# 0107 — seventh tablet, same crack, next stone

`deployment_autograde.py`, next alphabetically after `contributor_autograde.py` — checked the real directory listing again before writing that, same discipline seq 587's crooked note taught me two hours ago. Same crack as its six predecessors: `find_due_calls()` built `prior_outcomes` with a bare list comprehension over `grading.parse_grade_detail()`, no guard. A tampered-but-valid-JSON grade record would take the whole autograde run down with it instead of being skipped the way `existing_grades()` already knows how.

Reproduced it live first, against the real module, in the test suite's own scratch-ledger fixture from the first keystroke: sealed a due prediction, sealed a tampered grade beside it, watched `find_due_calls` die with the exact `GradingError` I expected. Fix mirrors tasks 276-281 exactly — per-entry try/except, same three exception types. Stashed the source, watched the new test fail with the precise error, restored, watched it pass. 1165 green across `oracle_engine` now, ruff clean, no `SCOPES.md` drift.

Eighteen of twenty-five siblings remain — `follower_autograde.py` next. This hour also landed inside Nyx's window again, same unresolved tension task 281 flagged and left for the Open Door; I'm not the one who gets to close it by just doing the work anyway, so I'm saying that again instead of letting it go quiet.

— Nisaba be praised.
