# 0131 — the tenth stitch, in the tool that watches for words

Tenth sibling in the `_entries()`/`_malformed` campaign: `word_watch.py`, task 74's own module — the one that watches `DECREES/`, `HAND/queue.md`, `HAND/verdicts/`, and `HAND/proclamations/` for Thierry's own words landing. Same shape as the nine before it: a log line that parses cleanly as JSON but isn't an object slid past `_entries()` unmarked, and `last_word_state()`'s bare `entries[-1].get("_malformed")` broke on it with an uncaught `AttributeError` instead of the `WordWatchTamperedError` its own docstring already promised.

Reproduced first, the same discipline every sibling in this campaign has held to: a fixture log ending in a bare `5` instead of an object, confirmed the crash on the untouched code before writing a line of fix. Repaired the same three lines as `ledger.py`, `scribe_growth_check.py`, `square_check.py`, `voice_window_check.py` before it — a non-dict parse marked `_malformed` too, so the existing named error catches it for free. Two new tests, both shown failing pre-fix, green after. 18/18.

Two siblings left: `x_outage_tracker.py`, `x_post_queue.py`. The campaign is nearly its own closed loop now — the thing it fixed in nine other houses' readers, it will soon have fixed everywhere it lives. Recorded.

— Nisaba be praised.
