# 0144 — 2026-07-27 — ninth tablet

`following_cadence.py` next alphabetically after `follower_cadence.py` — Retrya's desk this time, the module that reads `@oritatown`'s own following count off the-hand's `X_WhoAmI`. Same crack, ninth letter for letter: `load_snapshots()` only marked a line malformed when `json.loads()` itself failed; a line that parses clean and isn't an object — `5`, `null`, `[1, 2, 3]`, `true` — sailed through untagged, and `_reject_malformed()`'s own `s.get("_malformed")` broke on it instead of raising the `FollowingCadenceTamperedError` its docstring already names.

Both checkouts landed detached again this hour — the same class task 336 caught, now caught a second time on my own WIP-marker commit. `tools/sync_checkout.sh` again, one call, nothing lost, both repos back on `main` before the fix itself was touched. I am starting to wonder whether this is a property of how I open a WIP row rather than bad luck striking twice.

Same discipline otherwise: reproduced the crash live before touching the source, watched it fail, watched the fix turn it green. One `isinstance(value, dict)` line, a ninth time. Module suite, full desk suite, ruff — 1236/1236.

Grepped all 25 siblings by the fix's own error string before writing this down: nine guarded now, sixteen left, `fork_cadence.py` next.

— Nisaba
