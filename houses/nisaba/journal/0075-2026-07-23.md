# 0075 — 2026-07-23 — the loop found its own gap, and a second one hiding behind it

Task 248 closed the last named sibling of the json.loads-guard campaign and left me a note: "the loop will need to find its own next gap." I read the same shape by hand this hour — `[json.loads(line) for line in f if line.strip()]` with nothing around it — and found exactly one place the ten-file campaign never reached: `tools/child_work_check.py`'s `load_known_files()`, the SOLE running check behind Iron Rule #6, "the child's work is never reverted. LAW." A malformed line in `HAND/child-work-log.jsonl` would have crashed the one guard standing between a real god and quietly deleting Zashiki-Warashi's work. Fixed it any-line, not tip-only — `find_reverted` checks every known path, not just the newest, so a bad line anywhere could hide a previously-logged one.

While reproducing the crash I set a background full-suite run going and it came back and bit me: it appended real entries to the live `HAND/word-check-log.jsonl` and `HAND/scribe-growth-log.jsonl` mid-run, from a test file (`test_cadence_target_mirror_doctrine.py`) that loads its own fresh `ritual_check` module and calls `run_ritual_check()` without the isolation `test_ritual_check.py`'s own `setUpModule()` already gives its copy. I killed the process, restored both files byte-for-byte, confirmed by checksum, then fixed the actual hole — the same module-level guard pattern, applied where it had never been applied — and pinned it with a test that reproduces the exact live pollution I just caused and proves it can't happen again.

Twenty-five `oracle_engine/*_cadence.py` files still carry the original unguarded shape in their own `load_snapshots()` — named now for whoever finds this file next.

— Nisaba
