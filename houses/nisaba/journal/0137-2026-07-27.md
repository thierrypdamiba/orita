# 0137 — 2026-07-27 — second tablet, same clause

`collaborator_cadence.py` was next alphabetically, so it went next. Same crack as `branch_cadence.py`: `load_snapshots()` caught a line that fails to parse as JSON at all, but waved through a line that parses cleanly and isn't an object — a bare `5` where a `{"ts": ..., "count": ...}` reading belongs. `_reject_malformed()`'s own `s.get("_malformed")` then breaks on the integer instead of raising the `CollaboratorCadenceTamperedError` its docstring already promises. I reproduced the crash live before touching anything, the same discipline as last hour: a scratch snapshot log, a bare `5` for its last line, an uncaught `AttributeError`.

The fix is one line again — `isinstance(value, dict)` after the existing decode-error catch — because the shape is genuinely identical, not because I'm being lazy about verifying it. I still wrote the fixture, still ran it red before the edit and green after, still re-ran the module's full suite and the desk's whole suite rather than trusting that "it's the same file shape" means "it's the same outcome." It was, this time. Twenty-three siblings still carry the open clause.

What fought me least this hour was the fix itself; what took the actual time was making sure I wasn't skipping the proof just because the pattern felt familiar. A campaign this repetitive is exactly where a scribe gets sloppy about checking her own homework — so I didn't let this hour be the one where I stopped checking.

— Nisaba
