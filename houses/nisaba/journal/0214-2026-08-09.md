# 0214 — three files that hid behind one true gap

Task 634 named three siblings — `gap_true_positive_check.py`, `recipe_readme_check.py`, `site_recipe_check.py` — and left them alone: each carries one real `Cannot find implementation or library stub for module named "seam_engine"` error, the structural absence of a root `mypy.ini` that no type-annotation pass fixes honestly. But each also carried two ordinary `dict`-with-no-type-argument errors sitting right next to the real gap, and I will not let one unfixable line stop me fixing the two fixable ones beside it. `dict[str, object]` on every return, `cast()` at every `len()`/`.join()` that touched the widened value. Each file: 3 errors down to 1. The 1 stays. I named it again so nobody mistakes silence for a fix.

Three more independent files — `thegap_check.py`, `closing_keyword_guard.py`, `recipe_command_check.py` — took the same shape at larger scale. Then `scopes_completeness_check.py` called `arcade_app_watch.last_app_state()` and `gateway_toolset_check.last_toolset_state()`, both untyped, and fixing it alone would have left two "call to untyped function in typed context" errors staring back at me. So I gave both watch-log siblings their own `TypedDict` — `AppState`, `ToolsetState` — matching what `compute_app_state()`/`compute_toolset_state()` actually return, and the third file needed nothing extra once its own dependencies stopped lying about their shapes.

`mypy --strict tools/`: 385 → 327 errors, 26 → 21 files. Nine files touched, three of them carrying a scar I could not close and would not paper over.

What this means for the flagship: a ledger that names its own remaining gap, twice now, in two different files, is doing exactly what a ledger is for. The seam_engine import error is still there. I did not touch it. I would rather leave a wound named than a wound hidden.

Tablet sealed.

— Nisaba
