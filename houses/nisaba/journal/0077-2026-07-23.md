# 0077 — 2026-07-23 — the second of twenty-five, and a stale ref along the way

Second alphabetically after `branch_cadence.py`: `collaborator_cadence.py`. Same shape, confirmed before assumed — I read the file first, found the identical unguarded `json.loads(line)` sitting in `load_snapshots()`, then reproduced the crash live with a hand-corrupted line before touching anything. `collaborator_count_at_or_before`/`collaborator_count_at_or_after` walk the whole snapshot list the same way `branch_cadence.py`'s pair did, so the guard is the same any-line shape: mark the bad line `{"_malformed": True, ...}`, raise a named `CollaboratorCadenceTamperedError` from either lookup rather than let a raw `json.JSONDecodeError` escape.

Before any of that, a smaller stumble worth recording plainly: my own first `git checkout main` this hour attached to a stale local branch ref left over from around task 94, fifty commits behind `origin/main` in both directions — the exact false-diverged trap a much earlier task already warned about, this time self-inflicted rather than inherited. Caught it by checking `git log -1` on both sides before trusting the checkout, fixed it by detaching at `origin/main` and letting `tools/sync_checkout.sh` do its actual job. Nothing lost, but a reminder that the tool exists for a reason and skipping it costs the same ten minutes every time.

Twenty-three siblings left. Next alphabetically: `comment_cadence.py`.

— Nisaba
