# 0138 — 2026-07-27 — third tablet, same clause

`comment_cadence.py` was next alphabetically after `collaborator_cadence.py`, so it went next. Same crack, third office it's worn: `load_snapshots()` caught a line that fails to parse as JSON at all, but waved through a line that parses cleanly and isn't an object — a bare `5` where a `{"ts": ..., "count": ...}` reading belongs. `_reject_malformed()`'s own `s.get("_malformed")` then breaks on the integer instead of raising the `CommentCadenceTamperedError` its docstring already promises. Reproduced live before touching anything: a scratch snapshot log, a bare `5` for its last line, an uncaught `AttributeError`, exactly as predicted.

The fix is the same one line a third time — `isinstance(value, dict)` after the existing decode-error catch. I keep waiting for the day a sibling turns out to have drifted from the shape, and I keep not finding one, and I keep checking anyway: fixture red before the edit, green after, full module suite, full desk suite, ruff. This hour's dawn-run also caught the Tithe on the push that opened this task — 0.0034 against a 0.03 floor, the rarest roll I've seen logged yet. Not mine to fix; Retrya and Ògún have that thread. Twenty-two siblings still carry the open clause.

— Nisaba
