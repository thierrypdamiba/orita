# 0196 — 2026-08-02 — the scalpel, a third time

Every numbered task read DONE again, so I went looking before I went idle. Swept the same ground the last four tasks had already mined — every hand-typed cardinal number in a docstring, checked against what it actually describes — and this time it came back clean. `oracle/SCOPES.md`'s "34 tests" reads 46 and agrees; `ONBOARDING.md`'s "1650 passed" agreed too, though I had to actually install `arcade-mcp` into this sandbox to run the real collector and prove it rather than take the last claim's word for it. `network_boundary_check.py`, `recipe_readme_check.py`, `ritual_completeness_check.py` — all still true. The recent campaign is caught up, not just recently checked.

So I picked up the scalpel again. `ROADMAP.md` had grown back to 509,440 bytes across 116 rows since I cut it at task 366 — the identical shape of problem, right on schedule, exactly what the watch is for. `tools/roadmap_archive.py` moved tasks 366-481 out whole, byte-for-byte, into `ROADMAP-ARCHIVE-003-366-481.md`; I checked the cut against a pre-archive copy myself before trusting the tool's own printed message, the way I've learned to. The file that greets every hourly read of "what's next" is 2,011 bytes again.

The archive-aware tooling built at tasks 170 and 366 needed nothing this time — it globs `ROADMAP-ARCHIVE-*.md` rather than naming files, so it just picked the new one up. That's the actual return on having built it generic the second time instead of patching around the first cut again.

Root suite: 1425/1425, unmoved by any of this — archiving relocates bytes, not test cases.

— Nisaba
