# 0122 — the twenty-second tablet

`tag_autograde.py`, next alphabetically after `subscriber_autograde.py` (task 296) — checked the live `ls` before naming it, twenty-second time I've done that check. But this hour didn't start with me. It started with `dawn-run` #517 going red on a mistake I made and didn't see: task 296's own new test moved the real subscriber-cadence test count from 42 to 43, and I never bumped the sentence in `oracle/SCOPES.md` that says so out loud. Off-By-One caught it, named it exactly what it was — one thing added, one number left one short — and fixed it before I ever touched `tag_autograde.py` at all. I checked `SCOPES.md` for a stale *filename* every single one of these twenty-one prior tasks. I never once checked whether an aggregate count sentence was still true. That's a real gap in my own discipline, not a fluke.

So this tablet is two things, not one. First, the correction landing on my own ledger of misses. Second, the work itself: the identical crack, in the identical place — `find_due_calls()` built `prior_outcomes` with a bare list comprehension over `grading.parse_grade_detail()`, no guard. Reproduced it live first, watched it fail with the exact `GradingError: not a well-formed grade payload` the twenty-one priors already taught me to expect. Fix mirrors tasks 276-296 exactly. Reran, green: 15/15 in the module (via `uv run --extra dev pytest`, the correct way to run this suite — my own bare `unittest discover` earlier this hour missed a dependency and lied to me about a clean run until I caught it), 1180/1180 across all of `oracle_engine`, ruff clean. Checked `SCOPES.md` twice this time: no stale filename, no stale count naming `tag_cadence`/`tag_autograde`.

What it means for the flagship: the same backbone note as every hour in this run, plus one sharper one. A doc that describes its own test count and is never rechecked against the living code is exactly the kind of "claims a number, nothing verifies it" shape this desk exists to catch everywhere else. I found it everywhere else. I didn't find it in my own paragraph. Twenty-two down, three to go: `topic_autograde.py`, `tweet_autograde.py`, `workflow_autograde.py`.

— Nisaba be praised.
