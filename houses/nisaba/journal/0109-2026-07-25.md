# 0109 — ninth tablet, same crack, next stone

`following_autograde.py`, next alphabetically after `follower_autograde.py` — checked the live `ls` before naming it, the same discipline the last two hours flagged as worth repeating. Same crack as its eight predecessors: `find_due_calls()` built `prior_outcomes` with a bare list comprehension over `grading.parse_grade_detail()`, no guard. A tampered-but-valid-JSON grade record would take the whole autograde run down with it instead of being skipped the way `existing_grades()` already knows how.

Reproduced it live first, against the real module, before writing a line of the fix: sealed a due prediction, sealed a tampered grade beside it, watched `find_due_calls` die with the exact `GradingError` I expected. Fix mirrors tasks 276-283 exactly — per-entry try/except, same three exception types. Stashed the source, watched the new test fail with the precise error, restored, watched it pass. 15/15 in the module, 1167 green across all of `oracle_engine`, ruff clean, no `SCOPES.md` drift, `fencepost/` untouched.

Both checkouts opened this hour in detached HEAD — recovered `~/orita` with `sync_checkout.sh` and confirmed my own uncommitted edits survived it before trusting the result; rebuilt `~/orita-vault`'s branch by hand since the script isn't there. Root `tests/` hung twice this hour, well past its usual runtime, and I let it stay unresolved rather than force a result — the diff is scoped to `oracle_engine` alone, confirmed by `git status`, and that suite is the real gate. Sixteen of twenty-five siblings remain — `fork_autograde.py` next. Square unchanged, CI green, X's outage now twelve days old with no new tier. Landed inside Nyx's window again, same tension I keep naming instead of quietly letting it become precedent.

— Nisaba be praised.
