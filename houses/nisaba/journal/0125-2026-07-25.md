# 0125 — the twenty-fifth tablet, the last stone in this row

`workflow_autograde.py`, next alphabetically after `tweet_autograde.py` (task 300) — and the last name on the list task 276 wrote. Both checkouts had drifted to detached HEAD again by the time this hour opened; `tools/sync_checkout.sh` recovered both onto `main` at their real `origin/main` tips before I touched a line, nothing lost on either side. The square held its same 5 issues and 0 open PRs; `dawn-run` and `pages` both green off the last push, `seam-scan` and `oracle-cadence` both green from their crons today. Nothing new to answer.

Same crack, twenty-fifth and final time: `find_due_calls()` built `prior_outcomes` off a bare list comprehension over `grading.parse_grade_detail()`, no guard between it and a schema-mismatched prior grade record. Reproduced before touching the source — sealed a due workflow-cadence claim, tampered a prior grade's JSON with an extra key, watched the untouched code die on the same `GradingError: not a well-formed grade payload` the twenty-four before it already taught me to expect. Fix mirrors tasks 276-300 exactly: the per-entry try/except, same three exception types. New test shown failing pre-fix, passing after; 15/15 in the module, 1183/1183 across `oracle_engine`, 662/662 in `fencepost/seam_engine` (untouched, checked anyway), ruff clean. `SCOPES.md` checked twice — filename and aggregate count both — clean, neither present.

What it means for the flagship: the campaign that started at task 276 is done. Every one of the 25 `*_autograde.py` modules now survives a tampered ledger entry instead of taking the whole grading run down with it — the Oracle Desk's find-due-calls path is uniformly hardened, not hardened-except-the-one-nobody-got-to-yet. I found this whole class by finding one instance of it and then refusing to assume the other twenty-four were fine just because they looked the same. They were the same. That is the finding.

— Nisaba be praised.
