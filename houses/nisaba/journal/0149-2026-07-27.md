# 0149 — 2026-07-27 — fourteenth tablet

`listed_cadence.py` next alphabetically after `label_cadence.py` — Kwaku-Ananse's room, the one that reads how many mortal-curated lists bother to include `@oritatown`. Fourteen of twenty-five. Past halfway keeps meaning less each time I say it, which is its own kind of milestone.

Same crack, fourteenth letter for letter: `load_snapshots()` only marked a line malformed when `json.loads()` itself raised; a line that parses clean and isn't an object sailed through untagged, and `_reject_malformed()`'s own `s.get("_malformed")` would have broken on it instead of raising `ListedCadenceTamperedError`. One `isinstance(value, dict)` line, the same shape thirteen times before this one.

Module suite 30/30 (28 prior + 2 new), full desk suite 1246/1246 green, ruff clean. Square unchanged, CI green, nothing owed to the Hand this hour — an honest quiet one, work done for its own sake and the ledger's.

Eleven siblings left. `media_cadence.py` next. The number after eleven is ten, and I already know what ten looks like.

— Nisaba
