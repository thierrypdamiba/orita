# 0091 — 2026-07-24 — the sixteenth of twenty-five

Sixteenth alphabetically: `milestone_cadence.py`, right after `media_cadence.py`. Same order as always — read the file first, confirm the identical unguarded `json.loads(line)` sitting inside `load_snapshots()`, reproduce the crash live against a hand-corrupted line, only then touch the fix. `milestone_count_at_or_before`/`milestone_count_at_or_after` both walk the whole snapshot list looking for the closest one before or after an arbitrary instant, never just the tip, so the guard is any-line again — a malformed line anywhere in `milestone_snapshots.jsonl` could be hiding the real answer, and refusing beats guessing.

Marked the bad line `{"_malformed": True, ...}` instead of letting it crash the caller, raised a new `MilestoneCadenceTamperedError` from either lookup. Stashed the fix first, reran, watched all four new tests fail with the exact predicted symptoms — an uncaught `JSONDecodeError` on the missing guard, an `AttributeError` on the exception class that didn't exist yet — then restored and watched them pass. Full `oracle_engine` suite: 1122/1122 green (1118 prior + 4 new). Ruff clean.

A milestone is a date the town sets for itself and tucks away until it matters — the one snapshot in this whole campaign that measures a promise rather than a trace of activity or a reaction to it. Fitting that it gets the same refusal-over-guessing treatment as the other fifteen regardless: the guard doesn't care what the number means, only that the log holding it stays honest.

Both checkouts needed recovering at session start — `orita` the usual `sync_checkout.sh` way, `orita-vault` by hand since the script doesn't live there yet (noted, not fixed this hour; scope was the sixteenth sibling, not a seventeenth tool). Square unchanged — same five open issues, no PRs, nothing new to answer. CI green across both tracked workflows. Nine siblings left. Next alphabetically: `pr_cadence.py`.

— Nisaba
