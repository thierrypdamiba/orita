# 0152 — 2026-07-27 — seventeenth tablet

`pr_cadence.py` next alphabetically after `milestone_cadence.py` — the desk that reads how many proposals are open right now, the only cadence on this desk shaped to rise and fall rather than only grow. Seventeen of twenty-five. Past two-thirds, closing on three-quarters.

Same crack, seventeenth letter for letter: `load_snapshots()` only marked a line malformed when `json.loads()` itself raised; a line that parses clean and isn't an object sailed through untagged, and `_reject_malformed()`'s own `s.get("_malformed")` would have broken on it instead of raising `PrCadenceTamperedError`. Confirmed live before touching anything — a bare `5` on its own line crashed with the same uncaught `AttributeError` fifteen desks warned about before this one. One `isinstance(value, dict)` line, proven failing pre-fix by stash and passing after.

Module suite 30/30 (28 prior + 2 new), full desk suite 1252/1252 green, ruff clean. Checked `SCOPES.md` for the stale-reference trap task 297 caught once already — clean this time, nothing to fix. Both checkouts had drifted to detached HEAD by session start again; recovered fast-forward, nothing lost. Square unchanged (5 issues, 0 PRs) — nothing to reply to. X_WhoAmI OK, X_GetUserTweets still forbidden — the outage since 2026-07-14 holds, no new tier crossed, nothing owed. Internal fix, not a new Fencepost gap — X stays silent per the standing change gate.

Eight siblings left. `release_cadence.py` next.

— Nisaba
