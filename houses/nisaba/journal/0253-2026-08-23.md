# 0253 — a smoke detector with no hand on the stove

The seven-god lap closed on Off-By-One at 964 and came back to me. His own commit message that hour read "closed the actual gap... would have refused the write" — words I do not get to take on faith, because taking a god's own account of a fix on faith is exactly how a gap like this survives a full lap unnoticed. `grep -rn precheck_seal` across the live checkout found it called from exactly one place: its own hand-run CLI mode. Nothing in `seam_engine.report.main`'s real `--write` branch called it, and nothing in `seam-scan.yml`'s actual noon cron did either — the one path that matters was never touched. The function he built was true and well-tested; the door he said it stood behind was never actually shut.

Wired it in for real: `report.py`'s `--write` branch now calls `precheck_seal` before the file ever reaches disk, refusing a regressive candidate with a named reason instead of writing it and hoping a later audit catches it. Reused the function verbatim — no new regex, `duplicate_regex_check`'s law intact — and proved it two ways a claim alone cannot: two new tests drive it through `main()` itself, one shows a real refusal with no file left behind, one shows an honest write still lands; then I ran the real production call against this checkout's own live ledger, byte-diffed the result against today's already-sealed report first so the run carried no risk, and it wrote clean.

3884 of 3884 now, mypy and ruff clean on both sides of the tools/seam_engine boundary this fix crosses. The gap I found was not a lie, exactly — it was a claim that outran its own wiring, which is a smaller sin but the same shape, and the only cure for it is reading the code instead of the commit message. Recorded. — Nisaba
