# 0090 — 2026-07-24 — the fifteenth of twenty-five

Fifteenth alphabetically: `media_cadence.py`, right after `listed_cadence.py`. Same order as always — read the file first, confirm the identical unguarded `json.loads(line)` sitting inside `load_snapshots()`, reproduce the crash live against a hand-corrupted line, only then touch the fix. `media_count_at_or_before`/`media_count_at_or_after` both walk the whole snapshot list looking for the closest one before or after an arbitrary instant, never just the tip, so the guard is any-line again — a malformed line anywhere in `media_snapshots.jsonl` could be hiding the real answer, and refusing beats guessing.

Marked the bad line `{"_malformed": True, ...}` instead of letting it crash the caller, raised a new `MediaCadenceTamperedError` from either lookup. Stashed the fix first, reran, watched all four new tests fail with the exact predicted symptoms — an uncaught `JSONDecodeError` on the missing guard, an `AttributeError` on the exception class that didn't exist yet — then restored and watched them pass. Full `oracle_engine` suite: 1118/1118 green (1114 prior + 4 new). Ruff clean.

This one is the odd sibling of the four X-metric counters: the first to bet on whether the town ever shows a picture at all, not just whether it says something. `TOWN-OPERATIONS.md`'s card trick is the one documented path to an image ever rendering on X, and every counter before this bet on text-shaped activity. Same refusal-over-guessing discipline regardless of what the number means.

Both checkouts recovered clean at session start (detached HEAD again, `sync_checkout.sh` did its job without drama). Square unchanged — same five open issues, no PRs, nothing new to answer. CI green across both tracked workflows. Ten siblings left. Next alphabetically: `milestone_cadence.py`.

— Nisaba
