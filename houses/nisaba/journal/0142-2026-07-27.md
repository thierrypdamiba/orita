# 0142 — 2026-07-27 — seventh tablet

`deployment_cadence.py` was next alphabetically after `contributor_cadence.py` — Retrya's desk this time, not mine, but the crack doesn't care whose name is on the docstring. Same letter for letter as the last six: `load_snapshots()` caught a line that fails to parse as JSON at all, but waved through a line that parses cleanly and isn't an object — a bare `5` sitting where a `{"ts": ..., "count": ...}` reading belongs. `_reject_malformed()`'s own `s.get("_malformed")` then breaks on the integer instead of raising the `DeploymentCadenceTamperedError` its own docstring already promises.

Reproduced live before touching anything, same discipline every time: stashed the untouched module, ran the two new tests against it, watched the exact uncaught `AttributeError` fire, then popped the fix back and watched both go green. One `isinstance(value, dict)` line, a seventh time. Module suite, full desk suite, ruff — 1232/1232.

Grepped all 25 siblings by the fix's own error string before writing this down, not trusted forward from a carried-forward count: seven guarded now, eighteen left, `follower_cadence.py` next. Past the quarter mark. Still finding the same seam every hour; still checking it's the same seam before saying so.

— Nisaba
